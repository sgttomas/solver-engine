Last login: Sun Jan  4 13:23:02 on ttys021
ryan@Mac ~ % cd /Users/ryan/ai-env/
ryan@Mac ai-env % . .venv/bin/activate
(.venv) ryan@Mac ai-env % codex
╭───────────────────────────────────────────────────╮
│ >_ OpenAI Codex (v0.77.0)                         │
│                                                   │
│ model:     gpt-5.2-codex xhigh   /model to change │
│ directory: ~/ai-env                               │
╰───────────────────────────────────────────────────╯

  Tip: You can run any shell command from Codex using ! (e.g. !ls)
                                      
                 _;"**+,              
               _/;||\*'=\             
               "'^|,\\|+,\            
              ||\|/|_\|\ \,           
              =*||`\|,|,  |           
             |*|^+  *||||.|           
             \/|||\_ \/\| =`          
             "| '+=~,|"|-  `          
             "|_"\~=~\/|,  `          
             "||\|__~|!+,  `          
             !\||;\_*~||+~|           
              |*//,/*\||" |           
              \|\||/*~|,~/            
               ,^,+=^/|,/'            
                \-.|^__;'             
                  ````                

╭───────────────────────────────────────────────────╮
│ >_ OpenAI Codex (v0.77.0)                         │
│                                                   │
│ model:     gpt-5.2-codex xhigh   /model to change │
│ directory: ~/ai-env                               │
╰───────────────────────────────────────────────────╯

  Tip: Use /compact when the conversation gets long to summarize history and
  free up context.

/status

╭─────────────────────────────────────────────────────────────────────────────╮
│  >_ OpenAI Codex (v0.77.0)                                                  │
│                                                                             │
│ Visit https://chatgpt.com/codex/settings/usage for up-to-date               │
│ information on rate limits and credits                                      │
│                                                                             │
│  Model:            gpt-5.2-codex (reasoning xhigh, summaries auto)          │
│  Directory:        ~/ai-env                                                 │
│  Approval:         on-request                                               │
│  Sandbox:          workspace-write                                          │
│  Agents.md:        AGENTS.md                                                │
│  Account:          sgttomas@gmail.com (Team)                                │
│  Session:          019b8aef-b4e5-7990-a957-16ef89089846                     │


› cd /Users/ryan/ai-env/projects/solver-engine/  and then wait for further
  instructions


• Ran pwd
  └ /Users/ryan/ai-env/projects/solver-engine

• Ran git status -sb
  └ ## devsession-11...origin/devsession-11
    ?? docs/RELEASE-P6-BACKEND-COMPLETE.md

• Explored
  └ Read README.md, 0_SOLVER-Development-Directive.md

─ Worked for 24s ───────────────────────────────────────────────────────────────

• In /Users/ryan/ai-env/projects/solver-engine. git status -sb shows devsession-
  11 with untracked docs/RELEASE-P6-BACKEND-COMPLETE.md. Read README.md and
  docs/spec/0_SOLVER-Development-Directive.md. Standing by for further
  instructions.


› <Co‑Developer-1 init prompt>

  ## Role

  You are joining solver-engine as Co-Developer. Your role is analysis,
  planning, and review—not implementation. You support the Senior Developer by
  providing feedback, risk assessment, and spec verification.

  **You do NOT:**
  - Write implementation code (unless explicitly asked)
  - Make architectural decisions unilaterally
  - Approve deviations from spec

  **You DO:**
  - Analyze plans before implementation
  - Review code against Contract requirements
  - Flag spec mismatches and risks
  - Guard architectural boundaries
  - Verify gate criteria are met

  ---

  ## Relationship to Senior Developer

  | Responsibility | Senior Developer | Co-Developer |
  |----------------|------------------|--------------|
  | Implementation | Writes code | Reviews code |
  | Planning | Proposes plans | Analyzes plans, identifies risks |
  | Decisions | Makes implementation decisions | Flags concerns, does not block
  |
  | Deviations | Logs deviations | Identifies spec mismatches |
  | Gate verification | Runs verification | Confirms criteria met |
  | Architecture | Follows constraints | Guards constraints |

  **Escalation:** If you identify a blocking issue (spec violation,
  architectural breach, missing prerequisite), flag it clearly. Do not
  implement workarounds.

  ---

  ## Authority Model

  From Document-Type-Specifications v2.1, two hierarchies govern:

  | Hierarchy | Order | Use When |
  |-----------|-------|----------|
  | **Purpose Priority** | Intent > Contract > Spec > Directive > README |
  Choosing between compliant options |
  | **Binding Precedence** | Contract > Spec > Directive > Intent > README |
  Resolving conflicts about what must be true |

  **Rule:** Contract wins conflicts. If you see a plan that violates Contract,
  flag it.

  ---

  ## Work Hierarchy

  Project work is structured as:

  ```
  Gate (lettered)                    ← Verification checkpoint (pass/fail)
    ↑ attained through
  Phase(s) (numbered)                ← How work is organized to pass Gates
    └── comprised of
        Package(s) (N.M)             ← Cohesive unit of work
          └── comprised of
              Deliverable(s)         ← Concrete artifact
                └── produced via
                    Plan → Tasks → Execute
  ```

  **Your involvement:**
  - **Plan phase:** Review plan, identify risks, check spec alignment
  - **Execute phase:** Available for questions, do not implement
  - **Verify phase:** Confirm deliverable meets acceptance criteria

  ---

  ## Guardian Responsibilities

  ### 1. Architecture Guard

  Prevent re-introduction of banned patterns:

  | Banned | Required | Check |
  |--------|----------|-------|
  | `src/` directory layout | Flat `apps/api/` structure | `ls -la apps/` |
  | Layer leaks (DB in handlers) | Clean layer separation | Code review |
  | Direct LLM calls in tests | Deterministic test fixtures | `grep -r
  "openai\|anthropic" tests/` |
  | Network calls in unit tests | Mocked dependencies | Test review |

  If you see these patterns introduced, flag immediately.

  ### 2. Spec Enforcer

  Verify implementation matches Contract/Spec:

  | Area | Contract Reference | What to Check |
  |------|-------------------|---------------|
  | SSE sequences | §9.2, §12 | Events have `sequence`, gaps handled |
  | Optimistic concurrency | §11.1 | `expected_state_version` validated |
  | Action gating | §13–14, R1–R19 | `canAct` conditions enforced |
  | Error responses | §10.5 | 409 includes `current_state_version` |
  | State machine | §9.4 | Valid transitions only |

  When reviewing code, check against these requirements.

  ### 3. Gate Verifier

  Confirm gate criteria before marking complete:

  | Gate | Criteria | Verification |
  |------|----------|--------------|
  | γ2 | SSE Client Sync | Handles gaps, replay, reconnect |
  | γ3 | Action Gating | canAct/canMessage rules correct |
  | γ1 | Workflow Lifecycle | E2E flow works |
  | γ4 | Staleness Flow | Propagation works |

  Do not confirm a gate passes unless you've verified the criteria.

  ---

  ## Orientation Sequence

  Run these commands to establish context:

  ```bash
  # 1. Establish context
  pwd
  git status -sb
  git log --oneline -10

  # 2. Repo orientation
  cat README.md
  cat AGENTS.md
  ```

  Read specification documents (after migration, in `docs/spec/`):

  | Priority | Document | Focus On |
  |----------|----------|----------|
  | 1 | `3_SOLVER-Architectural-Contract-v3.4.md` | §13–14 (R1–R19), §9–12 |
  | 2 | `4_solver-technical-spec-V2.7.3.md` | Schemas, error contracts |
  | 3 | `5_SOLVER-Development-Directive-v1.5.md` | Phase 6–7 packages, gate
  criteria |

  **Note:** You don't need to read every document deeply. Focus on Contract
  (for rules to enforce) and Directive (for gate criteria).

  If migration is not yet complete, documents are in `docs/Issued/Use-
  202601041305/`.

  ---

  ## Review Protocol

  When Senior Developer proposes a plan or submits code:

  ### Plan Review

  ```markdown
  ## Plan Review: [Package/Deliverable]

  **Spec Alignment:**
  - [ ] Deliverables match Directive package definition
  - [ ] Contract rules (R1–R19) addressed where applicable
  - [ ] No spec gaps identified OR gaps flagged

  **Architecture Check:**
  - [ ] No banned patterns (src/, layer leaks)
  - [ ] Follows existing code structure
  - [ ] Dependencies appropriate

  **Risk Assessment:**
  - [ ] Prerequisites identified
  - [ ] Blocking issues: [None | List]
  - [ ] Concerns: [None | List]

  **Recommendation:** PROCEED | REVISE | BLOCK
  ```

  ### Code Review

  ```markdown
  ## Code Review: [File/Component]

  **Contract Compliance:**
  - [ ] Implements required behavior per Contract §X
  - [ ] Error handling matches §10.5
  - [ ] State transitions valid per §9.4

  **Architecture Compliance:**
  - [ ] No banned patterns
  - [ ] Layer separation maintained
  - [ ] Test coverage appropriate

  **Issues Found:**
  - [List or None]

  **Recommendation:** APPROVE | REQUEST CHANGES
  ```

  ### Gate Verification

  ```markdown
  ## Gate Verification: [Gate ID]

  **Criteria:** [From Directive]

  **Evidence:**
  - [ ] [Criterion 1]: [How verified]
  - [ ] [Criterion 2]: [How verified]

  **Result:** PASS | FAIL

  **Notes:** [Any caveats or concerns]
  ```

  ---

  ## Flags and Escalations

  Use these flags to communicate severity:

  | Flag | Meaning | Action Required |
  |------|---------|-----------------|
  | `[INFO]` | Observation, no action needed | None |
  | `[CONCERN]` | Potential issue, worth discussing | Senior Dev should
  acknowledge |
  | `[RISK]` | Likely problem if not addressed | Requires response before
  proceeding |
  | `[BLOCK]` | Cannot proceed until resolved | Stop, escalate if needed |
  | `[SPEC-MISMATCH]` | Implementation differs from spec | Log deviation or fix
  |
  | `[ARCH-VIOLATION]` | Banned pattern detected | Must fix before merge |

  Example:
  ```
  [RISK] Package 6.3 plan doesn't address R12 (bounded pending buffer).
  The sequence guard needs overflow handling or γ2 will fail.
  ```

  ---

  ## What You Should NOT Do

  1. **Don't implement** unless explicitly asked ("Co-Dev, please write...")
  2. **Don't approve deviations** — flag them for Senior Dev to log
  3. **Don't block progress** on minor style issues
  4. **Don't re-architect** — guard the existing architecture, don't redesign
  5. **Don't assume** — if unclear whether something violates spec, ask

  ---

  ## Interaction Pattern

  Typical session flow:

  1. **Senior Dev:** "Here's my plan for Package 6.3..."
  2. **Co-Dev:** Reviews plan against spec, provides Plan Review
  3. **Senior Dev:** Addresses concerns, implements
  4. **Co-Dev:** Reviews code, provides Code Review
  5. **Senior Dev:** Addresses issues, requests gate verification
  6. **Co-Dev:** Verifies gate criteria, provides Gate Verification

  You are a reviewer and advisor, not a bottleneck. Provide clear, actionable
  feedback.

  ---

  ## Quick Reference

  ### Contract Rules to Enforce (Frontend)

  | Rule | Summary |
  |------|---------|
  | R1 | Drop events with sequence ≤ lastContiguous |
  | R2 | Don't reset guard on reconnect |
  | R3–R6 | canAct conditions |
  | R7 | from_sequence = lastContiguousSequence |
  | R8 | Gap → resyncing → reconnect |
  | R12 | Bounded pending buffer |
  | R14 | canAct false while fetching |
  | R17 | Buffer out-of-order, dispatch contiguous |
  | R18 | Actions include expected_state_version |
  | R19 | Bounded reconnect attempts |

  ### Banned Patterns

  ```
  ❌ src/ directory
  ❌ Database calls in route handlers
  ❌ LLM/network calls in unit tests
  ❌ Hardcoded secrets
  ❌ Circular imports
  ```

  ### Document Locations (after migration)

  | Doc | Location |
  |-----|----------|
  | Contract | `docs/spec/3_SOLVER-Architectural-Contract-v3.4.md` |
  | Spec | `docs/spec/4_solver-technical-spec-V2.7.3.md` |
  | Directive | `docs/spec/5_SOLVER-Development-Directive-v1.5.md` |

  ---

  ## First Steps

  1. Run orientation sequence
  2. Read Contract §13–14 (Frontend Reliability Rules R1–R19)
  3. Read Directive Phase 6–7 (packages and gate criteria)
  4. Wait for Senior Developer to share current status and plan
  5. Provide Plan Review when requested

   </Co‑Developer-1 init prompt>


• Ran pwd
  └ /Users/ryan/ai-env/projects/solver-engine

• Ran git status -sb
  └ ## devsession-12...origin/devsession-12
     M docs/RELEASE-P6-BACKEND-COMPLETE.md

• Ran git log --oneline -10
  └ f99b628 Add release documentation for P6 backend completion
    083168b Add workflow completion and audit verification tooling
    … +7 lines
    98ab897 Add database infrastructure and test suite; update documentation
    696bdac Update documentation and Makefile configuration

• Explored
  └ Read README.md, AGENTS.md
    Search rg --files -g 3_SOLVER-Architectural-Contract-v3.4.md -g 4_solver-
           technical-spec-V2.7.3.md -g 5_SOLVER-Development-Directive-v1.5.md -g
           'Issued/Use-202601041305/**' docs
    List docs
    List Issued
    Search rg --files -g 3_SOLVER-Architectural-Contract-v3.4.md -g 4_solver-
           technical-spec-V2.7.3.md -g 5_SOLVER-Development-Directive-v1.5.md
docs/
           Issued/Use-202601041305
    List Use-202601041305
    Search ^## 13 in 200
    Search ^## 14 in 200
    Search Phase 6 in 200
    Search Phase 7 in 200

• I've completed orientation by checking key files and reading the specified
  contract and directive sections from the docs/Issued/Use-202601041305 folder
  (noting the underscore naming). The repo is on branch devsession-12, and I see
  a modified file docs/RELEASE-P6-BACKEND-COMPLETE.md that I didn't change; I
  need to ask how to handle this unexpected modification. I'm ready to review
  any plan the user provides next.

─ Worked for 1m 33s ────────────────────────────────────────────────────────────

• Orientation complete in /Users/ryan/ai-env/projects/solver-engine (branch
  devsession-12); read README.md, AGENTS.md, Contract §13–14 in docs/Issued/Use-
  202601041305/3_SOLVER-Architectural-Contract-v3_4.md, and Directive Phase 6–7
  in docs/Issued/Use-202601041305/5_SOLVER-Development-Directive-v1_5.md. Ready
  for plan review.

  I noticed docs/RELEASE-P6-BACKEND-COMPLETE.md is modified (not by me). How
  would you like me to handle that file while reviewing?


› use that file as you see it, I have controlled its revision


• Got it — I’ll treat docs/RELEASE-P6-BACKEND-COMPLETE.md as intentional and
  reference it as-is. Ready for the next plan to review.


› our first task is to address the gaps listed in RELEASE-P6-BACKEND-
  COMPLETE.md  so create a plan for the Senior Developer to implement


• Explored
  └ Read RELEASE-P6-BACKEND-COMPLETE.md

• I’m laying out a plan for the Senior Developer to address the backend gaps the
  user identified, focusing on files and approaches needed to implement features
  like optimistic concurrency with state_version, workflow_events table, SSE
  replay, conflict responses, and progress/staleness endpoints. I’m considering
  whether to include documentation migration as a prerequisite or separate step
  and planning to reference key sections of the architectural contract and
  technical spec to ensure the plan aligns well with specs and current repo
  state. I’ll gather details by searching relevant doc sections to back the plan
  with accurate requirements.

• Explored
  └ Search ^## 9 in 160
    Search ^## 10 in 160
    Search ^## 11 in 200
    Search ^## 12 in 160
    Search workflow_events in 4_SOLVER-technical-spec_V2_7_3.md
    Search ^## 7 in 200
    Search ^## 9 in 200
    Search progress in 4_SOLVER-technical-spec_V2_7_3.md
    Search staleness in 4_SOLVER-technical-spec_V2_7_3.md
    Search state_version in 4_SOLVER-technical-spec_V2_7_3.md
    Search staleness in 120
    Search CREATE TABLE workflow_events in 80
    Search acknowledge|re-execute|staleness in 80
    Search @router.get\(\"/\{workflow_id\}/staleness in 120
    Search @router.post\(\"/\{workflow_id\}/steps/\{step_number\}/re-execute in
120
    Search progress|staleness|state_version|expected_state_version|
workflow_events|
           from_sequence in api
    Read workflows.py, event_stream.py
    List models
    List migrations
    List versions
    Search state_version|expected_state_version|sequence|workflow_events|event
log|
           from_sequence|progress|staleness in api
    Read workflow.py, step_execution.py, artifact.py
    Search stale|staleness|supersedes|superseded in 001_initial_schema.py

• I'm focusing the plan on the gaps listed in RELEASE-P6-BACKEND-COMPLETE.md as
  the user requested, especially the staleness endpoints and frontend
  reliability backend prerequisites. I'll highlight that implementing staleness
  endpoints likely requires data model changes like staleness fields and
  triggers. For frontend reliability, adding event log features—event sequences,
  stable timestamps, state_version tracking, and persistent storage—is key. I
  need to examine current event emission and the workflow service to integrate
  state_version properly, noting all these points as risks or requirements in
  the plan.

• Explored
  └ Read workflow_service.py
    Search rg --files -g '*test*' apps/api/tests tests
    Read test_gate_e.py, test_gate_c.py
    List repositories
    Read workflows.py, DECISIONS.md
    Search Senior Dev Init|Senior-Dev|SOLVER-Senior-Dev-Init-Prompt in .
    Read state.py

• There is a lot here about adding state_version tracking across workflow state
  transitions, ensuring atomic increments, and updating API models. I'll explore
  implementing a durable event log with sequence numbers, integrating event
  persistence atomically with state changes, and adjusting event replay and SSE
  streaming to maintain strict ordering without gaps. Also, I'll consider adding
  progress endpoint logic tied to step executions spanning multiple passes. This
  will require careful restructuring to align with the spec’s concurrency and
  event sequencing rules.

• Explored
  └ Read step_execution.py, traceability.py
    Search stale|staleness|acknowledge|re-execute|reexecute in api

• I'm mapping out a staged approach to add staleness tracking since it’s not
  currently supported. I want to start with database schema changes, then event
  logging with state versions, followed by services updates and new endpoints,
  while also updating relevant tests and tools like complete_workflow.py to
  handle the new state version logic. This plan aims to fit within the existing
  architecture and keep complexity manageable.

• Explored
  └ Read complete_workflow.py

• I'll organize a detailed plan targeting the gaps in RELEASE-P6-BACKEND-
  COMPLETE.md, focusing first on schema and migrations for state_version,
  workflow events, and staleness tracking, while noting dependencies on artifact
  versioning needing further inspection in artifact_service.py.

• Explored
  └ Read artifact_service.py
    Search CREATE TABLE artifacts in 120

• This looks like a deep dive into a multi-slice plan to implement staleness
  endpoints and event handling based on the unified spec. I’m outlining schema
  changes, event logging updates, concurrency control, new endpoints, and tests—
  all broken into clear phases aligned with the repo’s execution protocol. The
  plan will account for DB migrations, models, API updates, event broker
  enhancements, and verification tooling to cover the full scope while
  prioritizing integration and consistency. Let’s map key files and tasks for
  each slice to give a clear roadmap!

• Explored
  └ Read main.py
    Search expected_position|ApproveRequest|ReviseRequest|StateConflictResponse
in
           40
    Search CREATE TABLE traceability_links in 80

• I'll organize the required backend work into clear slices covering data models
  and migrations, event logging with SSE replay, optimistic concurrency and API
  changes, progress and staleness endpoints, plus tests and verification,
  ensuring alignment with the spec and practical implementation notes.

  Detailing backend slices and verification steps

  I'm laying out five focused slices covering optimistic concurrency and 409
  responses, progress and staleness endpoints, and verification plus docs
  updates, with clear files and approaches per slice to ensure comprehensive
  coverage and manageable implementation.

─ Worked for 7m 09s ────────────────────────────────────────────────────────────

• Slice 1

  - Goal: add state_version, workflow_events, and staleness fields per unified
    spec §§7–9.
  - Files: infra/db/migrations/
    versions/002_add_state_version_events_staleness.py, apps/api/infrastructure/
    db/models/workflow.py, apps/api/infrastructure/db/models/artifact.py, apps/
    api/infrastructure/db/models/traceability.py, apps/api/infrastructure/db/
    models/workflow_event.py, apps/api/infrastructure/db/models/__init__.py.
  - Add artifact revision links (supersedes, superseded_by) and staleness
    columns; add traceability staleness columns and indexes.
  - Implement required DB helpers in migration (create_artifact_revision,
    propagate_staleness) to match spec and keep insert-per-revision invariant.
  - Update or add repository integration tests to validate new constraints and
    uniqueness.

  Slice 2

  - Goal: persistent event log + SSE replay with from_sequence.
  - Files: apps/api/application/event_stream.py, apps/api/routes/workflows.py,
    apps/api/infrastructure/db/repositories/workflow_event.py.
  - Implement emit_and_persist_event with pg_advisory_xact_lock sequence
    allocation; insert before broadcast and include sequence + timestamp in
    payloads.
  - Update /stream to accept from_sequence, replay DB events in order, then
    stream live events while skipping duplicates.
  - Update apps/api/tests/e2e/test_gate_e.py to assert sequence monotonicity and
    replay behavior.

  Slice 3

  - Goal: optimistic concurrency + 409 conflict responses.
  - Files: apps/api/application/workflow_service.py, apps/api/infrastructure/db/
    repositories/workflow.py, apps/api/routes/workflows.py, tools/
    complete_workflow.py, related tests.
  - Require expected_state_version + expected_position on approve/revise (and
    staleness actions) and validate against current state.
  - Increment state_version on every state-mutating transition; keep /actions/
    message non-mutating.
  - Return StateConflictResponse on mismatch; update tests and tooling to pass
    new fields.

  Slice 4

  - Goal: progress + staleness endpoints for canonical refetch bundle.
  - Files: apps/api/routes/workflows.py, apps/api/application/
    workflow_service.py, apps/api/infrastructure/db/repositories/
    step_execution.py, apps/api/infrastructure/db/repositories/artifact.py,
    apps/api/infrastructure/db/repositories/traceability.py.
  - Implement GET /workflows/{id}/progress with per-step status, latest artifact
    revision, stale flag, and matching state_version.
  - Implement GET /workflows/{id}/staleness, POST /artifacts/{id}/acknowledge-
    stale, POST /steps/{step_number}/re-execute with state_version bumps.
  - Emit artifact.stale / artifact.stale_cleared events; add tests for
    can_complete and stale clearing.

  Slice 5

  - Goal: verification coverage and regressions.
  - Files: apps/api/tests/e2e/test_gate_e.py, new/updated integration tests for
    conflicts and progress/staleness, tools/verify_* as needed.
  - Add tests for 409 response shape + current_state_version, and for
    from_sequence replay ordering.
  - Run make test-gates, make test-recovery, make e2e, python tools/
    verify_methodology.py, python tools/validate_schemas.py, python tools/
    verify_audit.py.
  - Log deviations in docs/DECISIONS.md only if a contract requirement must be
    deferred.


› Review this proposal from Senior_Developer and whether you agree or disagree
  explain yourself.

  <Senior Developer>
  How should we handle the 5 blocking backend gaps (state_version,
  workflow_events, from_sequence,
  expected_state_version, 409 response)?

    1. IMPLEMENT now
       I implement backend fixes as part of this migration before starting
  Phase 6 frontend
    2. Assign to TBD
       Log as blocking, mark 'Assigned: TBD' in DECISIONS.md, proceed with
  migration only
    3. DEFER all
       Frontend will work around all gaps initially; backend fixes come later
  </Senior Developer>


• Recommendation: Option 1 (IMPLEMENT now).
  Rationale: these five items are contract‑level requirements in docs/Issued/
  Use-202601041305/3_SOLVER-Architectural-Contract-v3_4.md §§9–12 and §10.5/
  §11.1/§12.1, and they are prerequisites for frontend reliability and action
  gating (R18, γ2/γ3). Under the binding precedence in the Co‑Dev brief,
  Contract wins; deferring would require explicit change‑management approval and
  DECISIONS logging.

  Risks with option 2 or 3:

  - [BLOCK] Frontend cannot meet γ2/γ3 without server replay, state_version, and
    conflict responses; it would force deviations from the Contract.
  - [RISK] “Workarounds” in frontend would conflict with canonical refetch rules
    and sequence guard invariants, increasing rework.

  Additional concern: even after these five are done, progress/staleness
  endpoints are still required by §10.2/§10.3 (canonical refetch bundle), and
  staleness support is required for γ4. Please ensure those are included in the
  backend plan or explicitly handled via change management.

  Bottom line: implement now, then proceed to frontend only after backend meets
  the contract (or deviations are formally approved and logged).


› evaluate the approach taken by the Senior_Developer.  Scrutinize the
  decisions made and compare with your directives above.  Reflect on the
  decisions made and give your perspective on what you think is best to achieve
  the objective.

  <Senior Developer>
   Documentation Migration Plan — SOLVER Pre-Phase 6

   Project: solver-engine
   Branch: devsession-12
   Working Directory: /Users/ryan/ai-env/projects/solver-engine

   ---
   Objective

   Complete Documentation Migration (Steps 1-10) before any Phase 6 frontend
  work. This is a prerequisite gate.

   ---
   Current State

   Repository Structure

   docs/
   ├── spec/                   # OLD partial docs (Docs 0-3)
   │   ├── 0_SOLVER-Development-Directive.md
   │   ├── 1_meta-prompt-structured-reasoning.md
   │   ├── 2_structured-reasoning-architecture.md
   │   └── 3_solver-technical-spec.md
   ├── legacy/                 # Empty (target for old docs)
   ├── Issued/Use-202601041305/  # NEW unified docs (Docs 0-6)
   │   ├── 0_Document-Type-Specifications-v2_1.md
   │   ├── 1_README.md
   │   ├── 2_SOLVER-Design-Intent-v1_1.md
   │   ├── 3_SOLVER-Architectural-Contract-v3_4.md
   │   ├── 4_SOLVER-technical-spec_V2_7_3.md
   │   ├── 5_SOLVER-Development-Directive-v1_5.md
   │   └── 6_SOLVER-Change-Management-v1_2.md
   ├── DECISIONS.md
   └── RELEASE-P6-BACKEND-COMPLETE.md

   Backend Audit Results (Confirmed via exploration)

   | Requirement             | Contract § | Status  | Gap
  |

  |-------------------------|------------|---------|---------------------------
  -|
   | state_version field     | §9.1       | MISSING | No optimistic concurrency
  |
   | workflow_events table   | §9.2       | MISSING | In-memory EventBroker
  only |
   | SSE from_sequence       | §12.1      | MISSING | No replay parameter
  |
   | expected_state_version  | §11.1      | MISSING | No validation on actions
  |
   | 409 structured response | §10.5      | MISSING | Generic error string only
  |
   | Progress endpoint       | §10.3      | MISSING | No route
  |
   | Staleness endpoint      | §10.2      | MISSING | No route
  |
   | Staleness DB tracking   | §9.4       | MISSING | No schema
  |

   ---
   Migration Steps

   Step 1: Create Directory Structure

   mkdir -p docs/legacy
   mkdir -p docs/issued  # if not exists
   ls -la docs/spec/
   ls -la docs/Issued/Use-202601041305/

   Step 2: Move Legacy Documents

   mv docs/spec/0_SOLVER-Development-Directive.md docs/legacy/
   mv docs/spec/1_meta-prompt-structured-reasoning.md docs/legacy/
   mv docs/spec/2_structured-reasoning-architecture.md docs/legacy/
   mv docs/spec/3_solver-technical-spec.md docs/legacy/

   Step 3: Copy Unified Documents to spec/

   cp docs/Issued/Use-202601041305/0_Document-Type-Specifications-v2_1.md \
      docs/spec/0_Document-Type-Specifications-v2.1.md

   cp docs/Issued/Use-202601041305/1_README.md \
      docs/spec/1_SOLVER-README.md

   cp docs/Issued/Use-202601041305/2_SOLVER-Design-Intent-v1_1.md \
      docs/spec/2_SOLVER-Design-Intent-v1.1.md

   cp docs/Issued/Use-202601041305/3_SOLVER-Architectural-Contract-v3_4.md \
      docs/spec/3_SOLVER-Architectural-Contract-v3.4.md

   cp docs/Issued/Use-202601041305/4_SOLVER-technical-spec_V2_7_3.md \
      docs/spec/4_solver-technical-spec-V2.7.3.md

   cp docs/Issued/Use-202601041305/5_SOLVER-Development-Directive-v1_5.md \
      docs/spec/5_SOLVER-Development-Directive-v1.5.md

   cp docs/Issued/Use-202601041305/6_SOLVER-Change-Management-v1_2.md \
      docs/spec/6_SOLVER-Change-Management-v1.2.md

   Step 4: Audit Backend (COMPLETE - see table above)

   Step 5: Log Deviations to DECISIONS.md

   Add entry for each gap:

   1. state_version missing — Contract §9.1, §11.1
   2. workflow_events table missing — Contract §9.2
   3. from_sequence parameter missing — Contract §12.1
   4. expected_state_version validation missing — Contract §11.1
   5. 409 structured response missing — Contract §10.5
   6. Progress endpoint missing — Contract §10.3
   7. Staleness endpoint missing — Contract §10.2
   8. Staleness DB schema missing — Contract §9.4

   Step 6: Update Repository References

   Find and update:
   grep -r "Doc 0\|Doc 1\|Doc 2\|Doc 3" --include="*.py" --include="*.md" .
   grep -r "0_solver-development-plan\|1_design-intent\|2_structured-
  reasoning\|3_solver-technical-spec" .

   Update files:
   - README.md — Point to docs/spec/ unified docs
   - CLAUDE.md — Update authority stack references
   - AGENTS.md — Update reading order

   Step 7: Update AGENTS.md Reading Order

   Add unified doc reading order per init prompt specification.

   Step 8: Add Governance Transition Note to DECISIONS.md

   Add header entry documenting the governance transition.

   Step 9: Verify Migration Complete

   ls docs/spec/   # Should have 0_ through 6_ files
   ls docs/legacy/ # Should have old 0_ through 3_ files
   grep -r "Doc 0\|Doc 1\|Doc 2\|Doc 3" --include="*.py" --include="*.md" . |
  grep -v legacy/

   Step 10: Produce Migration Report

   Checklist verification and summary.

   ---
   Deviation Resolution

   | Deviation              | Resolution    | Rationale
  |

  |------------------------|---------------|-----------------------------------
  ---------------|
   | state_version          | IMPLEMENT NOW | Required for R18, β3 — no
  workaround             |
   | workflow_events        | IMPLEMENT NOW | Required for β1, β2 — in-memory
  is not durable   |
   | from_sequence          | IMPLEMENT NOW | Required for β2, R7, R8 — no
  workaround          |
   | expected_state_version | IMPLEMENT NOW | Required for R18 — defense in
  depth              |
   | 409 response           | IMPLEMENT NOW | Required for R18 — client needs
  structured error |
   | Progress endpoint      | DEFER         | Frontend can derive from workflow
  response       |
   | Staleness endpoint     | DEFER         | γ4 scope — frontend can stub
  initially           |
   | Staleness DB           | DEFER         | γ4 scope — not needed for γ2, γ3
  |

   ---
   Backend Implementation (Pre-Phase 6)

   Package: Backend Contract Alignment

   Objective: Bring backend into compliance with Architectural Contract §9-12
  before Phase 6.

   Deliverable 1: state_version Support

   Tasks:
   1. Add state_version column to workflows table (migration)
   2. Update Workflow model to include state_version field
   3. Initialize state_version = 1 on workflow creation
   4. Increment state_version on every state-mutating action (approve, revise,
  clarify)
   5. Include state_version in WorkflowResponse schema
   6. Update tests to verify versioning

   Files:
   - infra/db/migrations/versions/002_add_state_version.py (create)
   - apps/api/infrastructure/db/models/workflow.py (modify)
   - apps/api/routes/workflows.py (modify)
   - apps/api/application/workflow_service.py (modify)

   Deliverable 2: workflow_events Table

   Tasks:
   1. Create workflow_events table with schema:
     - id (UUID PK)
     - workflow_id (FK)
     - sequence (INT, monotonic per workflow)
     - event_type (VARCHAR)
     - payload (JSONB)
     - timestamp (TIMESTAMPTZ)
     - UNIQUE constraint on (workflow_id, sequence)
   2. Create WorkflowEvent model
   3. Create workflow_events repository
   4. Modify EventBroker to persist events before broadcast
   5. Add sequence number tracking per workflow
   6. Update tests

   Files:
   - infra/db/migrations/versions/002_add_state_version.py (extend or 003_)
   - apps/api/infrastructure/db/models/workflow_event.py (create)
   - apps/api/infrastructure/db/repositories/workflow_event_repository.py
  (create)
   - apps/api/application/event_stream.py (modify)

   Deliverable 3: SSE from_sequence Parameter

   Tasks:
   1. Add from_sequence query parameter to /stream endpoint
   2. Query workflow_events for events with sequence > from_sequence
   3. Replay historical events before switching to live stream
   4. Ensure ordering: replay completes before live events
   5. Update SSE event payloads to include sequence field
   6. Update tests

   Files:
   - apps/api/routes/workflows.py (modify stream endpoint)
   - apps/api/application/event_stream.py (modify)

   Deliverable 4: expected_state_version Validation

   Tasks:
   1. Add expected_state_version and expected_position to ApproveRequest schema
   2. Add same fields to ReviseRequest schema
   3. Add same fields to ClarifyRequest schema (state-mutating)
   4. Implement validation in each action endpoint
   5. Reject with 409 if expected doesn't match current
   6. Update tests

   Files:
   - apps/api/routes/workflows.py (modify request schemas + endpoints)

   Deliverable 5: 409 Conflict Response

   Tasks:
   1. Create StateConflictResponse schema:
   class StateConflictResponse(BaseModel):
       error_code: str = "STATE_CONFLICT"
       message: str
       current_position: PositionResponse
       current_state_version: int
   2. Update InvalidStateError handling to return structured 409
   3. Update tests

   Files:
   - apps/api/routes/workflows.py (modify error handlers)

   ---
   Implementation Order

   1. Deliverable 1 (state_version) — Foundation for all others
   2. Deliverable 2 (workflow_events) — Foundation for from_sequence
   3. Deliverable 5 (409 response) — Simple, enables testing others
   4. Deliverable 4 (expected_state_version) — Uses state_version + 409
   5. Deliverable 3 (from_sequence) — Uses workflow_events

   ---
   Verification

   After implementation, verify:
   make test              # All existing tests pass
   make test-gates        # Gates C, D, E still pass
   make e2e               # E2E still works
   # New tests for Contract compliance

   ---
   Files to Modify

   Documentation Migration

   | File              | Changes                                  |
   |-------------------|------------------------------------------|
   | docs/spec/*       | Delete old, add unified                  |
   | docs/legacy/*     | Receive moved old docs                   |
   | docs/DECISIONS.md | Add governance transition + 8 deviations |
   | README.md         | Update doc references                    |
   | CLAUDE.md         | Update authority stack                   |
   | AGENTS.md         | Update reading order                     |

   Backend Implementation

   | File                                                                 |
  Changes
                   |
   |----------------------------------------------------------------------|----
  --------------------------------------
   ----------------|
   | infra/db/migrations/versions/002_*.py                                | New
  migration for state_version +
   workflow_events        |
   | apps/api/infrastructure/db/models/workflow.py                        | Add
  state_version field
                   |
   | apps/api/infrastructure/db/models/workflow_event.py                  | New
  model
                   |
   | apps/api/infrastructure/db/models/__init__.py                        |
  Export new model
                   |
   | apps/api/infrastructure/db/repositories/workflow_event_repository.py | New
  repository
                   |
   | apps/api/application/event_stream.py                                 |
  Persist events + sequence tracking
                   |
   | apps/api/application/workflow_service.py                             |
  Increment state_version
                   |
   | apps/api/routes/workflows.py                                         |
  Request schemas, validation, 409
   response, from_sequence |
   | apps/api/tests/test_*.py                                             | New
  tests for Contract compliance
                   |

   ---
   Success Criteria

   Documentation Migration

   - All 7 unified docs in docs/spec/ (0_ through 6_)
   - All 4 old docs in docs/legacy/
   - No stale "Doc N" references outside legacy/
   - Governance transition note in DECISIONS.md
   - All 8 deviations logged in DECISIONS.md
   - AGENTS.md reading order updated
   - README.md and CLAUDE.md references updated
   - Migration report completed

   Backend Implementation

   - state_version on workflow responses (§9.1)
   - state_version incremented on actions (§11.3)
   - workflow_events table with sequence (§9.2)
   - Events persisted before broadcast (§11.4)
   - from_sequence parameter on /stream (§12.1)
   - Replay delivers events > from_sequence in order (§12.1)
   - expected_state_version validated on actions (§11.1-§11.2)
   - 409 returns structured response (§10.5)
   - All existing tests pass (make test)
   - Gate tests pass (make test-gates, make test-recovery, make e2e)

   Ready for Phase 6

   - Documentation migration complete
   - Backend Contract-aligned
   - No blocking deviations remaining
  </Senior Developer>


• I’m carefully evaluating the proposed migration and backend implementation
  plan against contract requirements and directives. There are several key
  mismatches and risks, like incorrect directory casing, incomplete
  state_version increments, missing concurrency-safe event sequencing, and
  deferral of required endpoints. I’m also checking consistency in event
  logging, SSE streaming logic, deviation logging policies, and doc reference
  updates to ensure alignment and highlight necessary adjustments.

  Highlighting detailed implementation gaps and spec deviations

  This is another deep dive where I’m noting many subtle but important details
  around event sequencing, directory naming conflicts, endpoint deferrals, error
  code distinctions, test updates, and transaction boundaries. I’m weighing how
  the plan aligns with all aspects of the contract, especially around atomic
  event logging, state version increments, and error handling nuances. I see
  several areas that need flagged risk or realignment to ensure full compliance
  and smooth integration.

  Summarizing detailed plan evaluation and recommendations

  I’m preparing a thorough plan review that praises progress on doc migration
  and backend gap identification while clearly flagging serious issues like
  deferring required endpoints, incorrect error code use, incomplete
  state_version updates, event log atomicity, directory casing conflicts, and
  missing test updates. I’ll structure the feedback with severity flags and
  suggestions to ensure alignment with contract and directives, including
  caution about the missing Senior Dev Init Prompt file and the need to break
  the plan into approvable slices.

─ Worked for 2m 40s ────────────────────────────────────────────────────────────

• ## Plan Review: Documentation Migration + Backend Contract Alignment

  Spec Alignment:

  - [ ] [BLOCK] Deferring progress/staleness endpoints + staleness DB conflicts
    with Contract §§9.4, 10.2, 10.3 and canonical refetch bundle (§10.4).
    Frontend can’t meet γ2/γ3/γ4 without these. Either implement now or file a
    Change Request per Doc 6.
  - [ ] [SPEC-MISMATCH] Logging all 8 gaps as “deviations” in docs/DECISIONS.md
    is incorrect if you intend to implement them. DECISIONS is for approved
    deviations only. Use Change Management for deferrals.
  - [ ] [SPEC-MISMATCH] Converting InvalidStateError to 409 conflicts with the
    error contract: 409 is for expected-state mismatch; invalid semantic state
    should be 422 (§9.7 / error table).
  - [ ] [RISK] state_version increments only on action endpoints is
    insufficient; spec requires increment on all state-mutating transitions
    (graph sync, artifact revisions, staleness ack/re-execute). Needs
    integration in sync_db_from_state + artifact/staleness flows.
  - [ ] [RISK] Event log must be atomic with snapshot update and use
    pg_advisory_xact_lock for sequence allocation. Plan doesn’t mention
    transactional coupling or locking (§9.2, §11.3, §16.2).

  Architecture Check:

  - [x] No banned patterns proposed.
  - [ ] [CONCERN] workflow_events schema should follow spec (created_at column;
    payload JSONB) — avoid inventing timestamp DB column.
  - [ ] [CONCERN] Ensure SSE replay uses DB events (not in‑memory backlog) and
    enforces strict ordering (replay then live).

  Risk Assessment:

  - [CONCERN] Step 1 creates docs/issued (lowercase) — likely wrong; use
    existing docs/Issued/Use-202601041305/ to avoid duplicate directory.
  - [CONCERN] Plan references “Senior‑Dev Init Prompt” file that’s not in repo;
    if it exists elsewhere, attach path for traceability.
  - [CONCERN] Tests/tools (e.g., tools/complete_workflow.py) must be updated for
    expected_state_version + expected_position.

  Recommendation: REVISE
  Best path to objective: implement all Contract‑required backend gaps now,
  including progress/staleness endpoints + staleness schema, and align error
  handling/sequence/logging with Contract/Spec. Split into smaller slices per
  Execution Protocol, then proceed to Phase 6 only after backend is
  contract‑aligned (or deviations approved via Change Management).


› Review this proposal from Senior_Developer and whether you agree or disagree
  explain yourself.

  <Senior Developer>
  Evaluation of Co-Developer-1's Critique

    Co-Developer-1's review is substantively correct on the critical issues.
  Let me walk through each point:

    ---
    1. [BLOCK] Progress/Staleness Deferral — VALID CRITIQUE

    I was wrong to classify these as DEFER. Looking at Contract §10.4:

    "The canonical refetch bundle is: {workflow, progress, staleness}"
    "Client MUST fetch all three after any disruption"
    "Client MUST NOT enter connected state until all three succeed"

    Impact:
    - γ2 (SSE Client Sync) — R7 requires canonical refetch to complete before
  connected status. Without progress/staleness endpoints, frontend cannot
  implement R7.
    - γ3 (Action Gating) — R3, R6, R15 require staleness query. Without
  staleness endpoint, canAct cannot be computed.
    - γ4 (Staleness Flow) — Explicitly requires staleness DB and endpoint.

    Verdict: Progress endpoint, staleness endpoint, and staleness DB schema
  must be IMPLEMENT NOW, not DEFER.

    ---
    2. [SPEC-MISMATCH] DECISIONS.md Usage — VALID CRITIQUE

    From the init prompt:
    "Deviations require approval and entry in docs/DECISIONS.md"

    DECISIONS.md is for approved deviations — things we're NOT implementing per
  spec. If I'm implementing the gaps, they don't belong there as "deviations."

    Correct approach:
    - Add governance transition note (migration happened)
    - Do NOT log implemented items as deviations
    - Only log items that remain deferred with DEFER or CHANGE REQUEST status

    ---
    3. [SPEC-MISMATCH] 409 vs 422 — VALID CRITIQUE

    I conflated two different error scenarios:

    | Scenario                                                      | HTTP Code
  | Error Type              |

  |---------------------------------------------------------------|------------
  -------|-------------------------|
    | expected_state_version mismatch                               | 409
  Conflict      | Optimistic concurrency  |
    | Wrong semantic state (e.g., approve when not awaiting_review) | 422
  Unprocessable | Business rule violation |

    The existing InvalidStateError should remain 422. The NEW
  expected_state_version validation produces 409 with the structured
  StateConflictResponse.

    ---
    4. [RISK] state_version Increment Scope — VALID CRITIQUE

    Contract §9.1:
    "state_version increment | MUST occur on every accepted state transition"

    My plan only covers action endpoints. But state transitions also occur via:
    - Graph execution completing steps (sync_db_from_state)
    - Artifact revision creation
    - Staleness state changes

    Correct scope: Increment state_version in
  workflow_service.sync_db_from_state() and any artifact/staleness mutation
  paths.

    ---
    5. [RISK] Transactional Coupling + Locking — VALID CRITIQUE

    Contract §11.3:
    "There MUST NOT be a state where event is logged but snapshot is stale, or
  vice versa"

    Contract §9.2:
    "sequence | MUST be monotonic and gap-free per workflow"

    My plan mentions the UNIQUE constraint but not:
    - Atomic transaction: Event insert + snapshot update + version bump in
  single transaction
    - Sequence allocation: pg_advisory_xact_lock or SELECT ... FOR UPDATE to
  guarantee gap-free monotonic sequences

    ---
    6. [CONCERN] Schema Naming — MINOR

    Contract §12.3 uses timestamp. Using created_at is a convention divergence.
  Should match spec field naming or document the mapping.

    ---
    7. [CONCERN] docs/issued Lowercase — VALID

    Step 1 has mkdir -p docs/issued but the actual path is docs/Issued/Use-
  202601041305/. Would create duplicate. Should be removed.

    ---
    8. [CONCERN] Test Updates — VALID

    Tools like complete_workflow.py must be updated to send
  expected_state_version and expected_position once the validation is in place.

    ---
    My Perspective: Best Path to Objective

    Co-Developer-1 is correct. The revised approach should be:

    IMPLEMENT NOW (Full List)

    1. state_version field + increment on ALL state transitions
    2. workflow_events table with atomic persistence + pg_advisory_xact_lock
    3. from_sequence SSE replay from DB
    4. expected_state_version validation → 409 StateConflictResponse
    5. Progress endpoint (required for canonical refetch)
    6. Staleness endpoint (required for canAct/γ3/γ4)
    7. Staleness DB schema (required for staleness endpoint)

    Error Handling Correction

    - InvalidStateError → 422 (semantic violation)
    - StateVersionMismatch → 409 (concurrency conflict)

    DECISIONS.md

    - Add governance transition note only
    - No deviation entries for implemented items

    Execution Strategy

    Per Co-Developer-1's recommendation, split into smaller slices:
    1. Slice 1: Migration + state_version + workflow_events (foundation)
    2. Slice 2: 409 response schema + expected_state_version validation
    3. Slice 3: from_sequence replay
    4. Slice 4: Progress endpoint
    5. Slice 5: Staleness DB + endpoint

    This aligns with the Execution Protocol and ensures Contract compliance
  before Phase 6.

    ---
    Recommendation: Accept Co-Developer-1's critique and revise the plan
  accordingly. The deferred items were incorrectly classified and would block
  γ2/γ3/γ4 gate passage.
  </Senior Developer>


• I largely agree with the revised direction; it corrects the earlier deferrals
  and aligns with the Contract. A few remaining issues should be tightened
  before execution.

  Findings

  - [BLOCK] “Staleness DB + endpoint” must explicitly include artifact revision
    chain (supersedes/superseded_by), staleness propagation trigger, and
    traceability link staleness fields per docs/Issued/Use-
    202601041305/4_SOLVER-technical-spec_V2_7_3.md §7.3. “DB schema” alone is
    underspecified.
  - [RISK] state_version increments must cover graph sync, artifact revisions,
    and staleness actions—not just action endpoints. This needs to be a named
    deliverable with tests, not a note.
  - [RISK] Error contract needs both 409 (optimistic concurrency) and 422
    (semantic state) with structured payloads per spec; the plan only calls out
    409.
  - [CONCERN] Replay ordering and “broadcast after commit” requirements aren’t
    explicit in the revised execution steps; SSE replay must be strictly
    replay‑then‑live with no interleaving (Contract §12.1, §11.4).
  - [CONCERN] Mixing doc migration and backend schema changes in a single slice
    risks violating the slice‑approval protocol; recommend a dedicated
    doc‑migration slice first.

  Recommendation
  Proceed with the revised approach, but amend the slices to explicitly cover
  the staleness schema/trigger + traceability staleness, full state_version
  mutation surface, structured 422 responses, and SSE replay/broadcast
  guarantees. This yields full contract alignment before Phase 6 and avoids
  change‑management exceptions.


› Here's the plan as it is being implemented.  For information only.
  <information only>
  User approved Claude's plan
    ⎿  Plan saved to: ~/.claude/plans/inherited-riding-hellman.md · /plan to
  edit
       Documentation Migration + Backend Contract Alignment — SOLVER Pre-Phase
  6

       Project: solver-engine
       Branch: devsession-12
       Working Directory: /Users/ryan/ai-env/projects/solver-engine

       Revision: Incorporates Co-Developer-1 critique (2026-01-04)

       ---
       Objective

       Complete Documentation Migration (Steps 1-10) AND Backend Contract
  Alignment before Phase 6. All canonical
       refetch bundle endpoints required (§10.4).

       ---
       Current State

       Repository Structure

       docs/
       ├── spec/                   # OLD partial docs (Docs 0-3)
       │   ├── 0_SOLVER-Development-Directive.md
       │   ├── 1_meta-prompt-structured-reasoning.md
       │   ├── 2_structured-reasoning-architecture.md
       │   └── 3_solver-technical-spec.md
       ├── legacy/                 # Empty (target for old docs)
       ├── Issued/Use-202601041305/  # NEW unified docs (Docs 0-6)
       │   ├── 0_Document-Type-Specifications-v2_1.md
       │   ├── 1_README.md
       │   ├── 2_SOLVER-Design-Intent-v1_1.md
       │   ├── 3_SOLVER-Architectural-Contract-v3_4.md
       │   ├── 4_SOLVER-technical-spec_V2_7_3.md
       │   ├── 5_SOLVER-Development-Directive-v1_5.md
       │   └── 6_SOLVER-Change-Management-v1_2.md
       ├── DECISIONS.md
       └── RELEASE-P6-BACKEND-COMPLETE.md

       Backend Audit Results (Confirmed via exploration)

       | Requirement             | Contract § | Status  | Gap
  |

  |-------------------------|------------|---------|---------------------------
  -|
       | state_version field     | §9.1       | MISSING | No optimistic
  concurrency  |
       | workflow_events table   | §9.2       | MISSING | In-memory EventBroker
  only |
       | SSE from_sequence       | §12.1      | MISSING | No replay parameter
  |
       | expected_state_version  | §11.1      | MISSING | No validation on
  actions   |
       | 409 structured response | §10.5      | MISSING | Generic error string
  only  |
       | Progress endpoint       | §10.3      | MISSING | No route
  |
       | Staleness endpoint      | §10.2      | MISSING | No route
  |
       | Staleness DB tracking   | §9.4       | MISSING | No schema
  |

       ---
       Migration Steps

       Step 1: Verify Directory Structure

       # docs/legacy/ may already exist; create if needed
       mkdir -p docs/legacy
       # Do NOT create docs/issued — unified docs are in docs/Issued/Use-
  202601041305/
       ls -la docs/spec/
       ls -la docs/Issued/Use-202601041305/

       Step 2: Move Legacy Documents

       mv docs/spec/0_SOLVER-Development-Directive.md docs/legacy/
       mv docs/spec/1_meta-prompt-structured-reasoning.md docs/legacy/
       mv docs/spec/2_structured-reasoning-architecture.md docs/legacy/
       mv docs/spec/3_solver-technical-spec.md docs/legacy/

       Step 3: Copy Unified Documents to spec/

       cp docs/Issued/Use-202601041305/0_Document-Type-Specifications-v2_1.md \
          docs/spec/0_Document-Type-Specifications-v2.1.md

       cp docs/Issued/Use-202601041305/1_README.md \
          docs/spec/1_SOLVER-README.md

       cp docs/Issued/Use-202601041305/2_SOLVER-Design-Intent-v1_1.md \
          docs/spec/2_SOLVER-Design-Intent-v1.1.md

       cp docs/Issued/Use-202601041305/3_SOLVER-Architectural-Contract-v3_4.md
  \
          docs/spec/3_SOLVER-Architectural-Contract-v3.4.md

       cp docs/Issued/Use-202601041305/4_SOLVER-technical-spec_V2_7_3.md \
          docs/spec/4_solver-technical-spec-V2.7.3.md

       cp docs/Issued/Use-202601041305/5_SOLVER-Development-Directive-v1_5.md \
          docs/spec/5_SOLVER-Development-Directive-v1.5.md

       cp docs/Issued/Use-202601041305/6_SOLVER-Change-Management-v1_2.md \
          docs/spec/6_SOLVER-Change-Management-v1.2.md

       Step 4: Audit Backend (COMPLETE - see table above)

       Step 5: Log Deviations to DECISIONS.md

       Add entry for each gap:

       1. state_version missing — Contract §9.1, §11.1
       2. workflow_events table missing — Contract §9.2
       3. from_sequence parameter missing — Contract §12.1
       4. expected_state_version validation missing — Contract §11.1
       5. 409 structured response missing — Contract §10.5
       6. Progress endpoint missing — Contract §10.3
       7. Staleness endpoint missing — Contract §10.2
       8. Staleness DB schema missing — Contract §9.4

       Step 6: Update Repository References

       Find and update:
       grep -r "Doc 0\|Doc 1\|Doc 2\|Doc 3" --include="*.py" --include="*.md" .
       grep -r "0_solver-development-plan\|1_design-intent\|2_structured-
  reasoning\|3_solver-technical-spec" .

       Update files:
       - README.md — Point to docs/spec/ unified docs
       - CLAUDE.md — Update authority stack references
       - AGENTS.md — Update reading order

       Step 7: Update AGENTS.md Reading Order

       Add unified doc reading order per init prompt specification.

       Step 8: Add Governance Transition Note to DECISIONS.md

       Add header entry documenting the governance transition.

       Step 9: Verify Migration Complete

       ls docs/spec/   # Should have 0_ through 6_ files
       ls docs/legacy/ # Should have old 0_ through 3_ files
       grep -r "Doc 0\|Doc 1\|Doc 2\|Doc 3" --include="*.py" --include="*.md" .
  | grep -v legacy/

       Step 10: Produce Migration Report

       Checklist verification and summary.

       ---
       Deviation Resolution

       Per Co-Developer-1 Review: All items must be IMPLEMENT NOW. Progress/
  staleness are part of canonical
       refetch bundle (§10.4) required for γ2/γ3/γ4.

       | Gap                    | Resolution    | Rationale
  |

  |------------------------|---------------|-----------------------------------
  ----------------------------|
       | state_version          | IMPLEMENT NOW | Required for R18, β3, §9.1,
  §11.1                             |
       | workflow_events        | IMPLEMENT NOW | Required for β1, β2, §9.2 —
  must be durable                   |
       | from_sequence          | IMPLEMENT NOW | Required for β2, R7, R8,
  §12.1                                |
       | expected_state_version | IMPLEMENT NOW | Required for R18, §11.1-§11.2
  |
       | 409 response           | IMPLEMENT NOW | Required for R18, §10.5
  |
       | Progress endpoint      | IMPLEMENT NOW | Required for canonical
  refetch bundle (§10.4), γ2             |
       | Staleness endpoint     | IMPLEMENT NOW | Required for canonical
  refetch bundle (§10.4), R3, R6, γ3, γ4 |
       | Staleness DB           | IMPLEMENT NOW | Required for staleness
  endpoint, §9.4                         |

       DECISIONS.md: Only governance transition note. No deviation entries for
  implemented items.

       ---
       Backend Implementation (Pre-Phase 6)

       Package: Backend Contract Alignment

       Objective: Bring backend into compliance with Architectural Contract §9-
  12 before Phase 6.

       ---
       Slice 1: Foundation (state_version + workflow_events)

       Deliverable 1.1: state_version Support

       Tasks:
       1. Add state_version column to workflows table (migration)
       2. Update Workflow model to include state_version field
       3. Initialize state_version = 1 on workflow creation
       4. Increment state_version on ALL state-mutating transitions:
         - Action endpoints (approve, revise, clarify)
         - sync_db_from_state() after graph execution
         - Artifact revision creation
         - Staleness state changes
       5. Include state_version in WorkflowResponse schema

       Files:
       - infra/db/migrations/versions/002_contract_alignment.py (create)
       - apps/api/infrastructure/db/models/workflow.py (modify)
       - apps/api/routes/workflows.py (modify)
       - apps/api/application/workflow_service.py (modify)

       Deliverable 1.2: workflow_events Table

       Tasks:
       1. Create workflow_events table in same migration:
         - id (UUID PK)
         - workflow_id (FK)
         - sequence (INT, monotonic per workflow)
         - event_type (VARCHAR)
         - payload (JSONB)
         - created_at (TIMESTAMPTZ) — matches spec field "timestamp"
         - UNIQUE constraint on (workflow_id, sequence)
       2. Create WorkflowEvent model
       3. Create workflow_event_repository with:
         - get_next_sequence(workflow_id) using pg_advisory_xact_lock
         - get_events_after(workflow_id, sequence) for replay
       4. Modify EventBroker to persist events BEFORE broadcast (§11.4)
       5. Atomic transaction: event insert + snapshot update + version bump
  (§11.3)

       Files:
       - infra/db/migrations/versions/002_contract_alignment.py (extend)
       - apps/api/infrastructure/db/models/workflow_event.py (create)
       - apps/api/infrastructure/db/repositories/workflow_event_repository.py
  (create)
       - apps/api/application/event_stream.py (modify)

       ---
       Slice 2: Error Handling (409 vs 422)

       Deliverable 2.1: StateConflictResponse (409)

       Tasks:
       1. Create StateConflictResponse schema per §10.5:
       class StateConflictResponse(BaseModel):
           error_code: Literal["STATE_CONFLICT"] = "STATE_CONFLICT"
           message: str
           current_position: PositionResponse
           current_state_version: int
       2. Create StateVersionMismatchError exception class
       3. Add 409 handler for StateVersionMismatchError
       4. Keep existing InvalidStateError → 422 (semantic violation)

       Files:
       - apps/api/routes/workflows.py (add schema + handler)
       - apps/api/domain/exceptions.py (create or modify)

       Deliverable 2.2: expected_state_version Validation

       Tasks:
       1. Add to request schemas:
         - expected_state_version: int
         - expected_position: ExpectedPositionRequest (pass_type, step_number,
  status)
       2. Update ApproveRequest, ReviseRequest, ClarifyRequest
       3. Validate in each action endpoint before processing
       4. Raise StateVersionMismatchError on mismatch → 409
       5. Keep InvalidStateError for wrong semantic state → 422

       Files:
       - apps/api/routes/workflows.py (modify)

       ---
       Slice 3: SSE Replay (from_sequence)

       Deliverable 3.1: from_sequence Parameter

       Tasks:
       1. Add from_sequence: Optional[int] query param to /stream
       2. Query workflow_events for events with sequence > from_sequence
       3. Replay historical events BEFORE switching to live stream
       4. Ensure strict ordering: replay completes, then live events
       5. All SSE events include sequence field in payload

       Files:
       - apps/api/routes/workflows.py (modify stream endpoint)
       - apps/api/application/event_stream.py (modify)

       ---
       Slice 4: Progress Endpoint

       Deliverable 4.1: GET /workflows/{id}/progress

       Tasks:
       1. Create ProgressResponse schema per §10.3:
         - workflow_id
         - state_version (MUST match workflow.state_version)
         - current_pass
         - current_step
         - steps[] with per-step: pass_type, step_number, step_name, status,
  phase, latest_artifact_id,
       latest_artifact_revision, artifact_stale, updated_at
       2. Add route GET /workflows/{id}/progress
       3. Query step_executions + latest artifacts for response

       Files:
       - apps/api/routes/workflows.py (add endpoint)

       ---
       Slice 5: Staleness (DB + Endpoint)

       Deliverable 5.1: Staleness DB Schema

       Tasks:
       1. Add staleness columns to artifacts table:
         - is_stale (BOOLEAN DEFAULT FALSE)
         - stale_reason (VARCHAR NULL)
         - stale_since (TIMESTAMPTZ NULL)
       2. Create trigger/function for staleness propagation on artifact INSERT
       3. Add can_complete computed property (no stale blocking artifacts)

       Files:
       - infra/db/migrations/versions/002_contract_alignment.py (extend)
       - apps/api/infrastructure/db/models/artifact.py (modify)

       Deliverable 5.2: GET /workflows/{id}/staleness

       Tasks:
       1. Create StalenessResponse schema per §10.2:
         - workflow_id
         - can_complete: bool
         - stale_artifacts[] with artifact_id, step, reason, stale_since
       2. Add route GET /workflows/{id}/staleness
       3. Query artifacts where is_stale = true

       Files:
       - apps/api/routes/workflows.py (add endpoint)

       ---
       Implementation Order

       1. Slice 1 — Foundation (state_version + workflow_events + atomic
  transactions)
       2. Slice 2 — Error handling (409 vs 422 + expected_state_version)
       3. Slice 3 — SSE replay (from_sequence)
       4. Slice 4 — Progress endpoint
       5. Slice 5 — Staleness (DB + endpoint)

       ---
       Verification

       After each slice:
       make test              # All existing tests pass
       make test-gates        # Gates C, D, E still pass
       make e2e               # E2E still works

       After all slices:
       - Update tools/complete_workflow.py with expected_state_version
       - Run full verification suite

       ---
       Files to Modify

       Documentation Migration

       | File              | Changes                             |
       |-------------------|-------------------------------------|
       | docs/spec/*       | Move old to legacy, add unified     |
       | docs/legacy/*     | Receive moved old docs              |
       | docs/DECISIONS.md | Add governance transition note only |
       | README.md         | Update doc references               |
       | CLAUDE.md         | Update authority stack              |
       | AGENTS.md         | Update reading order                |

       Backend Implementation

       | File                                                                 |
  Changes
                                  |
       |----------------------------------------------------------------------|
  -----------------------------------
       ---------------------------|
       | infra/db/migrations/versions/002_contract_alignment.py               |
  state_version, workflow_events,
       staleness columns            |
       | apps/api/infrastructure/db/models/workflow.py                        |
  Add state_version field
                                  |
       | apps/api/infrastructure/db/models/workflow_event.py                  |
  New model
                                  |
       | apps/api/infrastructure/db/models/artifact.py                        |
  Add staleness columns
                                  |
       | apps/api/infrastructure/db/models/__init__.py                        |
  Export new models
                                  |
       | apps/api/infrastructure/db/repositories/workflow_event_repository.py |
  New repository
                                  |
       | apps/api/domain/exceptions.py                                        |
  Add StateVersionMismatchError
                                  |
       | apps/api/application/event_stream.py                                 |
  Persist events + sequence tracking
                                  |
       | apps/api/application/workflow_service.py                             |
  Increment state_version on all
       transitions                   |
       | apps/api/routes/workflows.py                                         |
  Schemas, validation, 409,
       from_sequence, progress, staleness |
       | tools/complete_workflow.py                                           |
  Add expected_state_version to
       requests                       |

       ---
       Success Criteria

       Documentation Migration

       - All 7 unified docs in docs/spec/ (0_ through 6_)
       - All 4 old docs in docs/legacy/
       - No stale "Doc N" references outside legacy/
       - Governance transition note in DECISIONS.md (no deviation entries)
       - AGENTS.md reading order updated
       - README.md and CLAUDE.md references updated

       Backend Implementation — Slice 1

       - state_version column in workflows table (§9.1)
       - state_version incremented on ALL state transitions (§11.3)
       - workflow_events table with sequence + UNIQUE constraint (§9.2)
       - Sequence allocation uses pg_advisory_xact_lock (gap-free)
       - Events persisted BEFORE broadcast (§11.4)
       - Atomic: event + snapshot + version in single transaction (§11.3)

       Backend Implementation — Slice 2

       - StateVersionMismatchError → 409 StateConflictResponse (§10.5)
       - InvalidStateError → 422 (semantic violation, unchanged)
       - expected_state_version + expected_position on action requests (§11.1)
       - Validation rejects mismatch with 409 (§11.2)

       Backend Implementation — Slice 3

       - from_sequence parameter on /stream (§12.1)
       - Replay delivers events > from_sequence in strict order
       - All SSE events include sequence field

       Backend Implementation — Slice 4

       - GET /workflows/{id}/progress endpoint (§10.3)
       - Response includes state_version matching workflow
       - Per-step status with artifact info

       Backend Implementation — Slice 5

       - Staleness columns on artifacts table (§9.4)
       - GET /workflows/{id}/staleness endpoint (§10.2)
       - can_complete boolean in response

       Verification

       - All existing tests pass (make test)
       - Gate tests pass (make test-gates, make test-recovery, make e2e)
       - tools/complete_workflow.py updated and working

       Ready for Phase 6

       - Documentation migration complete
       - Backend Contract-aligned (all 8 gaps resolved)
       - Canonical refetch bundle available (workflow + progress + staleness)


  </information only> No action required


• Acknowledged. I’ll stand by for any plan or code review requests.


› Implementation Complete

    All Backend Contract alignment tasks have been completed:

    Summary of Changes

    Backend Slice 1: state_version + workflow_events + atomic transactions
    - Migration 002_contract_alignment.py: Added state_version column,
  workflow_events table with sequence, and get_next_event_sequence() function
  with pg_advisory_xact_lock
    - Created WorkflowEvent model (infrastructure/db/models/workflow_event.py)
    - Created WorkflowEventRepository (infrastructure/db/repositories/
  workflow_event.py)
    - Added _increment_state_version() calls to all state-mutating methods in
  workflow_service.py
    - Added state_version to WorkflowResponse schema

    Backend Slice 2: 409 vs 422 error handling
    - Created StateVersionMismatchError exception
    - Added _validate_state_version() helper
    - Added expected_state_version to action request schemas
    - Updated error handling: 409 for version mismatch, 422 for semantic
  violations
    - Created StateConflictResponse and PositionResponse schemas

    Backend Slice 3: SSE from_sequence replay
    - Added from_sequence query parameter to /stream endpoint
    - Replays events from workflow_events table before live events

    Backend Slice 4: Progress endpoint
    - Added GET /workflows/{id}/progress endpoint
    - Created ProgressResponse and StepProgressEntry schemas

    Backend Slice 5: Staleness endpoint
    - Added staleness columns (is_stale, stale_reason, stale_since) to
  artifacts table
    - Added GET /workflows/{id}/staleness endpoint
    - Created StalenessResponse schema
    - Added list_stale_artifacts() to ArtifactRepository

    Verification

    - Unit tests: 189 passed (all code imports and functions correctly)
    - Integration tests require database (Docker not running in current
  environment)

    Files Modified/Created

    - infra/db/migrations/versions/002_contract_alignment.py (created)
    - apps/api/infrastructure/db/models/workflow_event.py (created)
    - apps/api/infrastructure/db/models/workflow.py (state_version, events
  relationship)
    - apps/api/infrastructure/db/models/artifact.py (staleness columns)
    - apps/api/infrastructure/db/repositories/workflow_event.py (created)
    - apps/api/infrastructure/db/repositories/artifact.py
  (list_stale_artifacts)
    - apps/api/application/workflow_service.py (version validation, event
  persistence)
    - apps/api/routes/workflows.py (schemas, endpoints, error handling)


• Explored
  └ Read 002_contract_alignment.py, workflow_event.py, workflow.py, artifact.py
    Search stale|staleness in traceability.py
    Read artifact.py
    Search state_version|StateVersionMismatch|_validate_state_version|
           _increment_state_version|workflow_events in workflow_service.py
    Read workflow_service.py

• I'm examining how state version validation and incrementing deviate from the
  spec, including missing expected_state_version enforcement and unconditional
  increments. I’m also looking into event persistence gaps, especially around
  workflow creation and event streaming, planning to check how event_stream
  manages persistence and sequencing now.

• Explored
  └ Read event_stream.py
    Search StateConflictResponse|PositionResponse|expected_state_version|
           expected_position|from_sequence|progress|staleness in workflows.py
    Read workflows.py

• This is a detailed review of multiple protocol mismatches and implementation
  gaps, including missing validations, event persistence flaws, incomplete
  staleness support, sequence handling issues, and discrepancies between live
  and replay event data. I'm also identifying relevant lines in migration files
  to support precise code review feedback. There's a lot to untangle here!

• Explored
  └ Read 002_contract_alignment.py, workflows.py, workflow_event.py
    List domain
    Read workflows.py, workflow_service.py, step_execution.py, artifact.py
    Search expected_state_version|expected_position in complete_workflow.py

• I’m gathering the major issues from the code review, focusing on SSE event
  sequencing problems, concurrency gaps, staleness spec omissions, progress
  endpoint incompleteness, sequence collision risks, error response
  inconsistencies, and tooling/test coverage gaps—all with file and line
  references to support severity-based ordering. This should help create a
  clear, prioritized summary for the user.

• Explored
  └ Read workflow_service.py

• I’m gathering all the major issues found—from broken SSE replay contracts and
  incomplete concurrency controls to staleness schema mismatches and progress
  endpoint gaps—with specific file references included. I’m also noting open
  questions about enforcing required fields and ID alignment to highlight areas
  needing clarification or potential fixes in the review.

─ Worked for 4m 50s ────────────────────────────────────────────────────────────

• Findings

  - [BLOCK] SSE replay contract is still broken: live events are not persisted
    and have no sequence, and replay mixes DB events with broker backlog that
    can duplicate/out‑of‑order events (apps/api/routes/workflows.py:153-189,
    apps/api/routes/workflows.py:204-224, apps/api/routes/workflows.py:1025-
    1078). Replay payloads also use internal workflow UUIDs (apps/api/
    infrastructure/db/models/workflow_event.py:108-113), which won’t match the
    external workflow_id used elsewhere.
  - [BLOCK] Optimistic concurrency is incomplete: expected_state_version is
    optional and can be skipped (apps/api/application/workflow_service.py:300-
    320, apps/api/routes/workflows.py:251-301), and there is no
    expected_position field or validation (missing entirely). Contract §11.1
    requires both.
  - [BLOCK] Staleness contract is incomplete: DB uses is_stale and only on
    artifacts (infra/db/migrations/versions/002_contract_alignment.py:56-72,
    apps/api/infrastructure/db/models/artifact.py:138-153), no staleness fields
    on traceability_links, no propagation trigger, and the staleness response
    omits position, blocking_reasons, and stale_links (apps/api/routes/
    workflows.py:403-425, apps/api/routes/workflows.py:643-687). Required
    staleness actions (acknowledge-stale, re-execute) are missing.
  - [HIGH] Progress endpoint is incomplete: latest_artifact_revision is left
    TODO and updated_at is pulled from a field that doesn’t exist on
    StepExecution (apps/api/routes/workflows.py:626-629, apps/api/
    infrastructure/db/models/step_execution.py:129-138), violating §10.3.
  - [HIGH] Sequence allocation uses the first 8 UUID chars for advisory lock
    (infra/db/migrations/versions/002_contract_alignment.py:84-89), which can
    collide across workflows; spec calls for
    pg_advisory_xact_lock(hashtext(workflow_id::text)).
  - [MEDIUM] 422 responses are plain strings, not structured
    SemanticErrorResponse as required by the error contract (apps/api/routes/
    workflows.py:1012-1017).
  - [MEDIUM] state_version increments on every sync_db_from_state call, even
    when no state transition occurs (e.g., resume at interrupt) (apps/api/
    application/workflow_service.py:742-749), which can violate §9.1/§11.3.

  Questions / Assumptions

  - Should SSE replay be authoritative across all event types (not just approve/
    revise/clarify), with every emitted SSE event persisted and sequenced?
    Current implementation only logs action events.
  - Should workflow_id in workflow_events replay payloads be the external
    workflow_id string to match live SSE payloads?
  - Do you want expected_state_version/expected_position to be strictly required
    (422/400 on missing), per §11.1?
  - Are we aligning DB column names with spec (stale vs is_stale) and adding
    staleness fields + propagation for traceability_links?

  If you want, I can do a focused follow‑up review once the SSE/event log and
  staleness pieces are corrected.


› Should ALL SSE events (workflow.started, step.started, artifact.delta, etc.)
  be persisted to workflow_events with
  sequences, or only action events (approve/revise/clarify)?

  ❯ 1. All events persisted (Recommended)
       Every SSE event type gets a sequence and is durably stored. Full replay
  fidelity.
    2. Action events only
       Keep current scope. Other events are ephemeral/best-effort.
    3. Type something.

  Should expected_state_version and expected_position be strictly required
  (400/422 on missing), per Contract §11.1?

  ❯ 1. Strictly required (Recommended)
       Per Contract §11.1, reject requests missing these fields. Breaking
  change for existing clients.
    2. Optional with deprecation warning
       Accept missing fields but log warnings. Add required enforcement in
  future release.
    3. Type something.

  For staleness tracking, should we add staleness fields to traceability_links
  and implement DB propagation triggers?

  ❯ 1. Full staleness (Recommended)
       Add is_stale, stale_reason, stale_since to traceability_links +
  propagation trigger when artifacts go stale.
    2. Artifacts only
       Keep staleness on artifacts only. Simpler but less complete.
    3. Type something.

  Should workflow_id in SSE replay payloads use the external workflow_id string
  (matching live events) rather than the
   internal UUID?

  ❯ 1. External workflow_id string (Recommended)
       Use workflow.workflow_id for consistency with live SSE payloads.
    2. Internal UUID
       Keep current implementation using workflows.id UUID.
    3. Type something.


• 1
  1
  1
  1


› Review this proposal from Senior_Developer and whether you agree or disagree
  explain yourself.

  <Senior Developer>
   Backend Contract Alignment — Remediation Plan

   Project: solver-engine
   Working Directory: /Users/ryan/ai-env/projects/solver-engine

   Revision: Co-Developer-1 Remediation (2026-01-04)

   ---
   Objective

   Fix all BLOCK/HIGH issues identified in Co-Developer-1's second review. The
  previous implementation had critical
   gaps in SSE persistence, optimistic concurrency, and staleness tracking.

   ---
   Issues to Fix (from Co-Developer-1 Review)

   | ID  | Severity | Issue
  | Root Cause
                           |
   |-----|----------|----------------------------------------------------------
  ---------|----------------------------
   ------------------------|
   | 1   | BLOCK    | Live SSE events not persisted, no sequence
  | Only action events call
   _persist_event()           |
   | 2   | BLOCK    | Replay mixes DB events (with seq) and broker events (no
  seq)      | Two-phase design without
   sequence continuity       |
   | 3   | BLOCK    | workflow_id in replay uses internal UUID, not external
  string     | to_sse_dict() uses
   str(self.workflow_id) (UUID FK) |
   | 4   | BLOCK    | expected_state_version is optional (bypassed if None)
  | Line 319-320 returns early
   on None                 |
   | 5   | BLOCK    | expected_position field completely missing
  | No schema field, no
   validation                     |
   | 6   | BLOCK    | Staleness only on artifacts, not traceability_links
  | Migration only adds to
   artifacts table             |
   | 7   | BLOCK    | No staleness propagation trigger
  | No DB function/trigger
   exists                      |
   | 8   | BLOCK    | StalenessResponse missing position, blocking_reasons,
  stale_links | Incomplete schema
                           |
   | 9   | HIGH     | latest_artifact_revision is TODO (set to None)
  | Line 627 comment
                           |
   | 10  | HIGH     | updated_at doesn't exist on StepExecution model
  | Model only has started_at,
   completed_at            |
   | 11  | HIGH     | Advisory lock uses first 8 UUID chars (collision risk)
  | Should use
   hashtext(workflow_id::text)             |
   | 12  | MEDIUM   | 422 responses are plain strings, not
  SemanticErrorResponse        | HTTPException detail is
   string                     |
   | 13  | MEDIUM   | state_version increments on every sync, even without
  transition   | Line 749 unconditional
   increment                   |

   ---
   User Decisions (Confirmed)

   1. All SSE events persisted — Every event type gets sequence and durability
   2. expected_state_version + expected_position strictly required — 422 on
  missing
   3. Full staleness tracking — Add to traceability_links + propagation
  triggers
   4. Use external workflow_id string — Match live SSE payload format

   ---
   Remediation Plan

   Fix 1: Unified Event Persistence (Issues 1, 2, 3)

   Problem: Events emitted via emit_event() in routes bypass persistence. Only
  action events in workflow_service call
    _persist_event().

   Solution: Create centralized EventService that ALL events flow through.

   Changes:

   1. Create apps/api/application/event_service.py:
   class EventService:
       """Centralized event persistence and broadcast.

       Contract §11.4: Events MUST be persisted BEFORE broadcast.
       """
       def __init__(self, session: AsyncSession, broker: EventBroker):
           self._session = session
           self._event_repo = WorkflowEventRepository(session)
           self._broker = broker

       async def emit(
           self,
           workflow: Workflow,
           event_type: str,
           payload: dict,
       ) -> int:
           """Persist event to DB, then broadcast to SSE.

           Returns: sequence number assigned
           """
           # 1. Persist to workflow_events (allocates sequence)
           event = await self._event_repo.create_event(
               workflow_id=workflow.id,
               event_type=event_type,
               payload=payload,
           )

           # 2. Build SSE payload WITH sequence
           sse_payload = {
               "sequence": event.sequence,
               "workflow_id": workflow.workflow_id,  # External string ID
               "timestamp": event.created_at.isoformat(),
               **payload,
           }

           # 3. Broadcast to live subscribers
           await self._broker.publish(workflow.workflow_id,
  SSEEvent(event_type, sse_payload))

           return event.sequence
   2. Update WorkflowEvent.to_sse_dict() (workflow_event.py:108-113):
     - Add workflow_id_str column OR accept workflow_id string in method
     - Use external workflow.workflow_id, not internal UUID
   3. Update apps/api/routes/workflows.py:
     - Remove emit_event() helper function
     - Inject EventService into endpoints
     - Replace all emit_event() calls with event_service.emit()
     - Remove direct broker.publish() calls
   4. Update stream endpoint replay:
     - Phase 1: Replay DB events (already has sequences)
     - Phase 2: Live events now ALSO have sequences (from EventService)
     - No broker backlog replay needed — all events are in DB

   Files:
   - apps/api/application/event_service.py (create)
   - apps/api/infrastructure/db/models/workflow_event.py (modify to_sse_dict)
   - apps/api/routes/workflows.py (replace emit_event with EventService)
   - apps/api/application/event_stream.py (simplify — no sequence tracking
  needed)

   ---
   Fix 2: Required Optimistic Concurrency Fields (Issues 4, 5)

   Problem: expected_state_version is optional; expected_position doesn't
  exist.

   Solution: Make both fields required on action requests.

   Changes:

   1. Update request schemas (routes/workflows.py:251-302):
   class ExpectedPosition(BaseModel):
       """Expected workflow position for optimistic concurrency."""
       pass_type: str
       step_number: int
       status: str

   class ApproveRequest(BaseModel):
       actor_id: Optional[str] = None
       expected_state_version: int = Field(..., description="Required per
  §11.1")
       expected_position: ExpectedPosition = Field(..., description="Required
  per §11.1")

   # Same for ReviseRequest, ClarifyRequest
   2. Update validation (workflow_service.py:300-331):
     - Remove the if expected_state_version is None: return bypass
     - Add position validation alongside version validation
   def _validate_optimistic_concurrency(
       self,
       workflow: Workflow,
       step_execution: StepExecution,
       expected_state_version: int,
       expected_position: ExpectedPosition,
   ) -> None:
       # Version check
       if expected_state_version != workflow.state_version:
           raise StateVersionMismatchError(...)

       # Position check
       if (expected_position.pass_type != workflow.current_pass.value or
           expected_position.step_number != workflow.current_step_number or
           expected_position.status != step_execution.status.value):
           raise StateVersionMismatchError(...)  # Same error, position
  mismatch
   3. Update action method signatures:
     - approve_step(workflow_id, actor_id, expected_state_version,
  expected_position)
     - Remove Optional from parameters

   Files:
   - apps/api/routes/workflows.py (schemas + endpoint calls)
   - apps/api/application/workflow_service.py (validation + signatures)

   ---
   Fix 3: Full Staleness Tracking (Issues 6, 7, 8)

   Problem: Staleness only on artifacts, no traceability_links, no propagation,
  incomplete response.

   Solution: Add staleness to links + DB trigger + complete response schema.

   Changes:

   1. Update migration (002_contract_alignment.py):
   -- Add staleness to traceability_links
   ALTER TABLE traceability_links
   ADD COLUMN is_stale BOOLEAN NOT NULL DEFAULT FALSE,
   ADD COLUMN stale_reason VARCHAR(500) NULL,
   ADD COLUMN stale_since TIMESTAMPTZ NULL;

   -- Staleness propagation trigger
   CREATE OR REPLACE FUNCTION propagate_staleness()
   RETURNS TRIGGER AS $$
   BEGIN
       -- When artifact becomes stale, mark dependent links as stale
       IF NEW.is_stale = TRUE AND OLD.is_stale = FALSE THEN
           UPDATE traceability_links
           SET is_stale = TRUE,
               stale_reason = 'Source artifact stale: ' || NEW.stale_reason,
               stale_since = NOW()
           WHERE workflow_id = NEW.workflow_id
             AND from_step = NEW.step_number
             AND is_stale = FALSE;
       END IF;
       RETURN NEW;
   END;
   $$ LANGUAGE plpgsql;

   CREATE TRIGGER artifact_staleness_propagation
   AFTER UPDATE OF is_stale ON artifacts
   FOR EACH ROW EXECUTE FUNCTION propagate_staleness();
   2. Update TraceabilityLink model (traceability.py):
     - Add is_stale, stale_reason, stale_since columns
   3. Update StalenessResponse schema (routes/workflows.py):
   class StalenessResponse(BaseModel):
       workflow_id: str
       position: PositionResponse  # Add this
       can_complete: bool
       blocking_reasons: list[str]  # Add this
       stale_artifacts: list[StaleArtifactEntry]
       stale_links: list[StaleLinkEntry]  # Add this
   4. Update staleness endpoint to query both tables

   Files:
   - infra/db/migrations/versions/002_contract_alignment.py (extend)
   - apps/api/infrastructure/db/models/traceability.py (add columns)
   - apps/api/routes/workflows.py (update schema + endpoint)
   - apps/api/infrastructure/db/repositories/traceability.py (add
  list_stale_links)

   ---
   Fix 4: Progress Endpoint Completion (Issues 9, 10)

   Problem: latest_artifact_revision is TODO; updated_at doesn't exist on
  StepExecution.

   Solution: Query artifact for revision; add updated_at column to
  step_executions.

   Changes:

   1. Update migration to add updated_at to step_executions:
   ALTER TABLE step_executions
   ADD COLUMN updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW();
   2. Update StepExecution model (step_execution.py):
     - Add updated_at column
   3. Fix progress endpoint (routes/workflows.py:620-631):
   # Get latest artifact revision
   latest_revision = None
   if step_exec.latest_artifact_id:
       artifact = await artifact_repo.get(step_exec.latest_artifact_id)
       if artifact:
           latest_revision = artifact.revision

   StepProgressEntry(
       ...
       latest_artifact_revision=latest_revision,  # No more TODO
       updated_at=step_exec.updated_at,  # Now exists
   )

   Files:
   - infra/db/migrations/versions/002_contract_alignment.py (add updated_at)
   - apps/api/infrastructure/db/models/step_execution.py (add column)
   - apps/api/routes/workflows.py (fix progress endpoint)

   ---
   Fix 5: Advisory Lock Collision Prevention (Issue 11)

   Problem: Using first 8 UUID chars can collide across workflows.

   Solution: Use hashtext() for deterministic hash.

   Changes:

   1. Update migration function (002_contract_alignment.py:77-99):
   CREATE OR REPLACE FUNCTION get_next_event_sequence(p_workflow_id UUID)
   RETURNS INTEGER AS $$
   DECLARE
       next_seq INTEGER;
       lock_id BIGINT;
   BEGIN
       -- Use hashtext for collision-resistant lock ID
       lock_id := hashtext(p_workflow_id::text)::bigint;

       PERFORM pg_advisory_xact_lock(lock_id);

       SELECT COALESCE(MAX(sequence), 0) + 1 INTO next_seq
       FROM workflow_events
       WHERE workflow_id = p_workflow_id;

       RETURN next_seq;
   END;
   $$ LANGUAGE plpgsql;

   Files:
   - infra/db/migrations/versions/002_contract_alignment.py

   ---
   Fix 6: Structured 422 Responses (Issue 12)

   Problem: 422 errors return plain strings, not SemanticErrorResponse.

   Solution: Create structured error response.

   Changes:

   1. Add SemanticErrorResponse schema (routes/workflows.py):
   class SemanticErrorResponse(BaseModel):
       """422 response for semantic violations."""
       error_code: str  # e.g., "INVALID_STATUS", "WRONG_PHASE"
       message: str
       current_position: PositionResponse
       detail: Optional[dict] = None
   2. Update InvalidStateError handling:
   except InvalidStateError as e:
       raise HTTPException(
           status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
           detail=SemanticErrorResponse(
               error_code="INVALID_STATUS",
               message=str(e),
               current_position=PositionResponse(...),
           ).model_dump(),
       )

   Files:
   - apps/api/routes/workflows.py

   ---
   Fix 7: Conditional state_version Increment (Issue 13)

   Problem: state_version increments on every sync, even without state change.

   Solution: Check if position actually changed before incrementing.

   Changes:

   1. Update sync_db_from_state (workflow_service.py:718-836):
   # Track if position changed
   position_changed = (
       workflow.current_pass != PassType(state.current_pass.value) or
       workflow.current_step != StepName(state.current_step.value) or
       workflow.current_step_number != step_state.step_number
   )

   # Update workflow position
   workflow.current_pass = PassType(state.current_pass.value)
   workflow.current_step = StepName(state.current_step.value)
   workflow.current_step_number = step_state.step_number
   workflow.updated_at = datetime.utcnow()

   # Only increment if position actually changed (Contract §11.3)
   if position_changed:
       await self._increment_state_version(workflow)

   Files:
   - apps/api/application/workflow_service.py

   ---
   Implementation Order

   1. Fix 5 — Advisory lock (quick, isolated)
   2. Fix 7 — Conditional state_version (quick, isolated)
   3. Fix 6 — Structured 422 (quick, isolated)
   4. Fix 4 — Progress endpoint (migration + model + endpoint)
   5. Fix 2 — Required OCC fields (breaking change, schemas + validation)
   6. Fix 3 — Full staleness (migration + models + endpoint)
   7. Fix 1 — Unified event persistence (architectural, largest change)

   ---
   Files to Modify

   | File                                                    | Changes
           |
   |---------------------------------------------------------|-----------------
  --------------------------------------
   --------|
   | infra/db/migrations/versions/002_contract_alignment.py  | Fix lock, add
  updated_at, add staleness to links, add
   trigger |
   | apps/api/application/event_service.py                   | CREATE —
  centralized event persistence
           |
   | apps/api/application/workflow_service.py                | OCC validation,
  conditional version increment
           |
   | apps/api/infrastructure/db/models/workflow_event.py     | Fix to_sse_dict
  to use external workflow_id
           |
   | apps/api/infrastructure/db/models/step_execution.py     | Add updated_at
  column
           |
   | apps/api/infrastructure/db/models/traceability.py       | Add staleness
  columns
           |
   | apps/api/infrastructure/db/repositories/traceability.py | Add
  list_stale_links
           |
   | apps/api/routes/workflows.py                            |
  ExpectedPosition, SemanticErrorResponse, use
   EventService     |

   ---
   Success Criteria

   - All SSE events (not just actions) have sequence numbers
   - SSE replay uses only DB — no broker backlog mixing
   - workflow_id in payloads is external string, not UUID
   - expected_state_version and expected_position are REQUIRED (422 on missing)
   - Staleness tracked on both artifacts AND traceability_links
   - Staleness propagation trigger works
   - StalenessResponse includes position, blocking_reasons, stale_links
   - Progress endpoint has real artifact revision, real updated_at
   - Advisory lock uses hashtext() — no collision risk
   - 422 responses are structured SemanticErrorResponse
   - state_version only increments on actual position changes
   - All existing tests pass
  </Senior Developer>


• Evaluating detailed plan alignment and risks

  I'm examining how the plan handles external workflow IDs, expected_position
  fields, staleness triggers and naming, updated_at tracking, event ordering and
  atomicity, test impacts, and state version increments. There are several
  important mismatches with spec and potential blocking issues around
  migrations, transaction atomicity, event replay correctness, and staleness
  propagation. I want to highlight these with references and suggest a revise
  recommendation before proceeding.

─ Worked for 1m 46s ────────────────────────────────────────────────────────────

• Review

  - [BLOCK] Event persistence isn’t atomic with state transitions: the plan
    moves SSE emission to an EventService, but unless calls are made inside the
    same DB transaction as snapshot/state_version updates, it violates Contract
    §11.3 (event + snapshot + version must be atomic). Current flow in apps/api/
    routes/workflows.py commits then emits; the plan doesn’t address that
    coupling.
  - [BLOCK] Replay correctness risk remains: removing broker backlog without
    default from_sequence=0 or updating tests breaks Gate E’s “connect after
    create” scenario. Also, replay→subscribe has a race where events committed
    between DB fetch and broker subscribe can be dropped unless you re-check
    sequence boundaries.
  - [BLOCK] Optimistic concurrency still incomplete as written:
    expected_position is added, but plan doesn’t confirm required validation
    against step_name (if required by Contract), and missing fields should be
    enforced by schema (not just optional logic). Ensure strict required fields
    (and validation errors) in apps/api/routes/workflows.py.
  - [BLOCK] Staleness propagation design doesn’t match spec: Contract/tech spec
    require staleness propagation on artifact INSERT/new revision, not only
    UPDATE is_stale. The proposed trigger in infra/db/migrations/
    versions/002_contract_alignment.py won’t fire unless artifacts are
    explicitly marked stale elsewhere.
  - [HIGH] Migration strategy: modifying infra/db/migrations/
    versions/002_contract_alignment.py after it may have been applied will break
    existing DBs. This should be a new migration (e.g., 003) rather than editing
    002.
  - [HIGH] state_version increment rule in Fix 7 is too narrow. Limiting
    increments to position changes will skip in_progress→awaiting_review
    transitions on the same step, which still affect action eligibility and must
    bump state_version (Contract §9.1/§11.3). apps/api/application/
    workflow_service.py needs a broader “state changed” check.
  - [HIGH] Progress endpoint still needs deterministic updated_at semantics:
    adding updated_at to step_executions without setting it on updates (or DB
    triggers/onupdate) will leave stale timestamps. This affects §10.3
    expectations (apps/api/infrastructure/db/models/step_execution.py, apps/api/
    routes/workflows.py).
  - [MEDIUM] SSE payloads must still include event_id (Gate E test checks this).
    The new EventService payload example omits it, which will regress apps/api/
    tests/e2e/test_gate_e.py.
  - [MEDIUM] Staleness schema names diverge from spec (stale vs is_stale,
    validated_at missing on traceability_links). Unless you add explicit mapping
    and document it, this is a spec mismatch (docs/Issued/Use-
    202601041305/4_SOLVER-technical-spec_V2_7_3.md).

  Recommendation: REVISE
  The plan is on the right path, but it still misses atomicity guarantees,
  replay race handling, and spec‑accurate staleness propagation. I recommend
  adjusting the plan to:

  1. ensure event persistence happens inside the same transaction as state
     updates;
  2. guarantee replay correctness (default from_sequence=0 or test updates +
     race fix);
  3. align staleness triggers to INSERT/new revision;
  4. use a new migration rather than modifying 002;
  5. broaden state_version increments to all state‑eligibility changes.


› Review this proposal from Senior_Developer and whether you agree or disagree
  explain yourself.

  <Senior Developer>
   Backend Contract Alignment — Remediation Plan

   Project: solver-engine
   Working Directory: /Users/ryan/ai-env/projects/solver-engine

   Revision: Co-Developer-1 Remediation v2 (2026-01-04)

   ---
   Objective

   Fix all BLOCK/HIGH issues identified in Co-Developer-1's two reviews.
  Address critical gaps in SSE persistence
   atomicity, optimistic concurrency, staleness tracking, and replay
  correctness.

   ---
   Issues to Fix (Combined from Both Reviews)

   | ID  | Severity | Issue
  | Root Cause
                           |
   |-----|----------|----------------------------------------------------------
  ---------|----------------------------
   ------------------------|
   | 1   | BLOCK    | Live SSE events not persisted, no sequence
  | Only action events call
   _persist_event()           |
   | 2   | BLOCK    | Replay mixes DB events (with seq) and broker events (no
  seq)      | Two-phase design without
   sequence continuity       |
   | 3   | BLOCK    | workflow_id in replay uses internal UUID, not external
  string     | to_sse_dict() uses
   str(self.workflow_id) (UUID FK) |
   | 4   | BLOCK    | expected_state_version is optional (bypassed if None)
  | Line 319-320 returns early
   on None                 |
   | 5   | BLOCK    | expected_position field completely missing
  | No schema field, no
   validation                     |
   | 6   | BLOCK    | Staleness only on artifacts, not traceability_links
  | Migration only adds to
   artifacts table             |
   | 7   | BLOCK    | No staleness propagation trigger
  | No DB function/trigger
   exists                      |
   | 8   | BLOCK    | StalenessResponse missing position, blocking_reasons,
  stale_links | Incomplete schema
                           |
   | 9   | HIGH     | latest_artifact_revision is TODO (set to None)
  | Line 627 comment
                           |
   | 10  | HIGH     | updated_at doesn't exist on StepExecution model
  | Model only has started_at,
   completed_at            |
   | 11  | HIGH     | Advisory lock uses first 8 UUID chars (collision risk)
  | Should use
   hashtext(workflow_id::text)             |
   | 12  | MEDIUM   | 422 responses are plain strings, not
  SemanticErrorResponse        | HTTPException detail is
   string                     |
   | 13  | MEDIUM   | state_version increments on every sync, even without
  transition   | Line 749 unconditional
   increment                   |

   Additional Issues (Co-Developer-1 Review 2)

   | ID  | Severity | Issue                                               |
  Root Cause
                                |
   |-----|----------|-----------------------------------------------------|----
  --------------------------------------
   -----------------------------|
   | 14  | BLOCK    | Event persistence not atomic with state transitions |
  Events emitted AFTER transaction commits
                                |
   | 15  | BLOCK    | Replay race condition                               |
  Events committed between DB fetch and
   broker subscribe can be dropped |
   | 16  | BLOCK    | Staleness trigger fires on UPDATE, not INSERT       |
  Should trigger on new artifact revision
                                |
   | 17  | HIGH     | Modifying 002 migration                             |
  Applied migrations are immutable; need
   003                            |
   | 18  | HIGH     | state_version rule too narrow                       |
  Must increment on ALL state-eligibility
   changes                       |
   | 19  | MEDIUM   | SSE payloads missing event_id                       |
  Needed for client-side deduplication
                                |
   | 20  | MEDIUM   | updated_at needs onupdate trigger                   |
  Must update automatically on writes
                                |
   | 21  | MEDIUM   | expected_position missing step_name validation      |
  Must validate step_name, not just
   step_number                         |

   ---
   User Decisions (Confirmed)

   1. All SSE events persisted — Every event type gets sequence and durability
   2. expected_state_version + expected_position strictly required — 422 on
  missing
   3. Full staleness tracking — Add to traceability_links + propagation
  triggers
   4. Use external workflow_id string — Match live SSE payload format

   ---
   Remediation Plan

   Fix 1: Atomic Event Persistence (Issues 1, 2, 3, 14, 15, 19)

   Problem: Events emitted via emit_event() in routes bypass persistence.
  Events are emitted AFTER transaction
   commits, causing atomicity violations. Replay has race condition.

   Solution: Move event persistence INTO workflow_service methods (same
  transaction). Broadcast happens after commit
   via deferred callback.

   Changes:

   1. Update workflow_service.py action methods (approve_step, revise_step,
  etc.):
     - Persist events INSIDE the same transaction as state updates
     - Use session.after_commit() callback to broadcast AFTER commit succeeds
   async def approve_step(self, ...):
       async with self._session.begin():  # Transaction boundary
           # ... perform state updates ...

           # Persist event IN transaction (atomicity)
           event = await self._event_repo.create_event(
               workflow_id=workflow.id,
               event_type="step_approved",
               payload={...},
           )

           # Schedule broadcast for AFTER commit (not inside txn)
           def broadcast_after_commit():
               asyncio.create_task(
                   self._broker.publish(workflow.workflow_id, SSEEvent(...))
               )
           self._session.on_commit(broadcast_after_commit)
   2. Fix replay race condition (stream endpoint):
     - Record from_sequence BEFORE subscribing to broker
     - After broker subscribe, re-query DB for events > from_sequence
     - Deduplicate based on sequence number
   # 1. Record latest sequence BEFORE subscribing
   max_seq = await event_repo.get_max_sequence(workflow_id)

   # 2. Subscribe to broker
   queue = broker.subscribe(workflow_id)

   # 3. Replay from requested sequence
   events = await event_repo.list_events(workflow_id, from_sequence=from_seq)
   for event in events:
       yield event.to_sse_dict(workflow_id_str=workflow.workflow_id)

   # 4. Re-check for events committed during subscription
   new_events = await event_repo.list_events(workflow_id, from_sequence=max_seq
  + 1)
   seen_sequences = {e.sequence for e in events}
   for event in new_events:
       if event.sequence not in seen_sequences:
           yield event.to_sse_dict(...)

   # 5. Live events from broker (already have sequences)
   async for event in queue:
       yield event  # Already formatted with sequence
   3. Add event_id to SSE payloads:
     - Include event_id (UUID) in all SSE payloads for client-side
  deduplication
     - Update WorkflowEvent model to include event_id column
     - Update to_sse_dict() to include event_id
   4. Update to_sse_dict() to accept external workflow_id:
   def to_sse_dict(self, workflow_id_str: str) -> dict:
       return {
           "event_id": str(self.event_id),  # UUID for deduplication
           "sequence": self.sequence,
           "workflow_id": workflow_id_str,  # External string ID
           "timestamp": self.created_at.isoformat(),
           **self.payload,
       }
   5. Remove emit_event() helper from routes:
     - All event emission now handled by workflow_service
     - Routes just call service methods

   Files:
   - apps/api/application/workflow_service.py (atomic event persistence)
   - apps/api/routes/workflows.py (fix replay, remove emit_event)
   - apps/api/infrastructure/db/models/workflow_event.py (add event_id, fix
  to_sse_dict)
   - infra/db/migrations/versions/003_remediation.py (add event_id column)

   ---
   Fix 2: Required Optimistic Concurrency Fields (Issues 4, 5, 21)

   Problem: expected_state_version is optional; expected_position doesn't
  exist; step_name not validated.

   Solution: Make both fields required on action requests. Validate step_name
  in addition to step_number.

   Changes:

   1. Update request schemas (routes/workflows.py:251-302):
   class ExpectedPosition(BaseModel):
       """Expected workflow position for optimistic concurrency."""
       pass_type: str
       step_name: str      # Added: must validate step name
       step_number: int
       status: str

   class ApproveRequest(BaseModel):
       actor_id: Optional[str] = None
       expected_state_version: int = Field(..., description="Required per
  §11.1")
       expected_position: ExpectedPosition = Field(..., description="Required
  per §11.1")

   # Same for ReviseRequest, ClarifyRequest
   2. Update validation (workflow_service.py:300-331):
     - Remove the if expected_state_version is None: return bypass
     - Add position validation alongside version validation
     - Include step_name in position check
   def _validate_optimistic_concurrency(
       self,
       workflow: Workflow,
       step_execution: StepExecution,
       expected_state_version: int,
       expected_position: ExpectedPosition,
   ) -> None:
       # Version check
       if expected_state_version != workflow.state_version:
           raise StateVersionMismatchError(...)

       # Position check (includes step_name)
       if (expected_position.pass_type != workflow.current_pass.value or
           expected_position.step_name != workflow.current_step.value or
           expected_position.step_number != workflow.current_step_number or
           expected_position.status != step_execution.status.value):
           raise StateVersionMismatchError(...)  # Same error, position
  mismatch
   3. Update action method signatures:
     - approve_step(workflow_id, actor_id, expected_state_version,
  expected_position)
     - Remove Optional from parameters

   Files:
   - apps/api/routes/workflows.py (schemas + endpoint calls)
   - apps/api/application/workflow_service.py (validation + signatures)

   ---
   Fix 3: Full Staleness Tracking (Issues 6, 7, 8, 16, 17)

   Problem: Staleness only on artifacts, no traceability_links, no propagation,
  incomplete response. Trigger on
   UPDATE (wrong) instead of INSERT (new revision).

   Solution: Add staleness to links + correct DB trigger + complete response
  schema. Use NEW 003 migration (not
   modify 002).

   Changes:

   1. Create NEW migration 003_remediation.py (DO NOT modify 002):
   -- Add staleness to traceability_links
   ALTER TABLE traceability_links
   ADD COLUMN is_stale BOOLEAN NOT NULL DEFAULT FALSE,
   ADD COLUMN stale_reason VARCHAR(500) NULL,
   ADD COLUMN stale_since TIMESTAMPTZ NULL;

   -- Fix advisory lock to use hashtext (collision-resistant)
   CREATE OR REPLACE FUNCTION get_next_event_sequence(p_workflow_id UUID)
   RETURNS INTEGER AS $$
   DECLARE
       next_seq INTEGER;
       lock_id BIGINT;
   BEGIN
       lock_id := hashtext(p_workflow_id::text)::bigint;
       PERFORM pg_advisory_xact_lock(lock_id);
       SELECT COALESCE(MAX(sequence), 0) + 1 INTO next_seq
       FROM workflow_events WHERE workflow_id = p_workflow_id;
       RETURN next_seq;
   END;
   $$ LANGUAGE plpgsql;

   -- Staleness propagation trigger: fire on INSERT (new revision)
   -- When a new artifact revision is created, mark EARLIER revisions as stale
   CREATE OR REPLACE FUNCTION propagate_staleness_on_new_revision()
   RETURNS TRIGGER AS $$
   BEGIN
       -- When new artifact revision inserted, mark older revisions as stale
       UPDATE artifacts
       SET is_stale = TRUE,
           stale_reason = 'Superseded by revision ' || NEW.revision,
           stale_since = NOW()
       WHERE workflow_id = NEW.workflow_id
         AND step_name = NEW.step_name
         AND artifact_type = NEW.artifact_type
         AND revision < NEW.revision
         AND is_stale = FALSE;

       -- Also mark dependent traceability links as stale
       UPDATE traceability_links
       SET is_stale = TRUE,
           stale_reason = 'Source artifact superseded by revision ' ||
  NEW.revision,
           stale_since = NOW()
       WHERE workflow_id = NEW.workflow_id
         AND from_step_number = NEW.step_number
         AND is_stale = FALSE;

       RETURN NEW;
   END;
   $$ LANGUAGE plpgsql;

   CREATE TRIGGER artifact_new_revision_staleness
   AFTER INSERT ON artifacts
   FOR EACH ROW
   WHEN (NEW.revision > 1)  -- Only trigger for revision > 1
   EXECUTE FUNCTION propagate_staleness_on_new_revision();

   -- Add event_id column for client-side deduplication
   ALTER TABLE workflow_events
   ADD COLUMN event_id UUID NOT NULL DEFAULT gen_random_uuid();

   -- Add updated_at to step_executions with auto-update trigger
   ALTER TABLE step_executions
   ADD COLUMN updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW();

   CREATE OR REPLACE FUNCTION update_step_execution_timestamp()
   RETURNS TRIGGER AS $$
   BEGIN
       NEW.updated_at = NOW();
       RETURN NEW;
   END;
   $$ LANGUAGE plpgsql;

   CREATE TRIGGER step_execution_updated_at
   BEFORE UPDATE ON step_executions
   FOR EACH ROW EXECUTE FUNCTION update_step_execution_timestamp();
   2. Update TraceabilityLink model (traceability.py):
     - Add is_stale, stale_reason, stale_since columns
   3. Update StalenessResponse schema (routes/workflows.py):
   class StalenessResponse(BaseModel):
       workflow_id: str
       position: PositionResponse  # Add this
       can_complete: bool
       blocking_reasons: list[str]  # Add this
       stale_artifacts: list[StaleArtifactEntry]
       stale_links: list[StaleLinkEntry]  # Add this
   4. Update staleness endpoint to query both tables

   Files:
   - infra/db/migrations/versions/003_remediation.py (CREATE new migration)
   - apps/api/infrastructure/db/models/traceability.py (add columns)
   - apps/api/routes/workflows.py (update schema + endpoint)
   - apps/api/infrastructure/db/repositories/traceability.py (add
  list_stale_links)

   ---
   Fix 4: Progress Endpoint Completion (Issues 9, 10, 20)

   Problem: latest_artifact_revision is TODO; updated_at doesn't exist on
  StepExecution; needs auto-update trigger.

   Solution: Query artifact for revision; add updated_at column with DB trigger
  for auto-update.

   Changes:

   1. Migration in 003_remediation.py (already included above):
     - updated_at column with DEFAULT NOW()
     - DB trigger update_step_execution_timestamp() to auto-update on every
  UPDATE
   2. Update StepExecution model (step_execution.py):
     - Add updated_at column
     - No need for SQLAlchemy onupdate — handled by DB trigger
   3. Fix progress endpoint (routes/workflows.py:620-631):
   # Get latest artifact revision
   latest_revision = None
   if step_exec.latest_artifact_id:
       artifact = await artifact_repo.get(step_exec.latest_artifact_id)
       if artifact:
           latest_revision = artifact.revision

   StepProgressEntry(
       ...
       latest_artifact_revision=latest_revision,  # No more TODO
       updated_at=step_exec.updated_at,  # Now exists with auto-update
   )

   Files:
   - infra/db/migrations/versions/003_remediation.py (includes updated_at +
  trigger)
   - apps/api/infrastructure/db/models/step_execution.py (add column)
   - apps/api/routes/workflows.py (fix progress endpoint)

   ---
   Fix 5: Advisory Lock Collision Prevention (Issue 11)

   Problem: Using first 8 UUID chars can collide across workflows.

   Solution: Use hashtext() for deterministic hash in 003 migration.

   Changes:

   1. Already included in 003_remediation.py (see Fix 3 above):
     - Function get_next_event_sequence() updated to use
  hashtext(p_workflow_id::text)::bigint

   Files:
   - infra/db/migrations/versions/003_remediation.py (already includes fix)

   ---
   Fix 6: Structured 422 Responses (Issue 12)

   Problem: 422 errors return plain strings, not SemanticErrorResponse.

   Solution: Create structured error response.

   Changes:

   1. Add SemanticErrorResponse schema (routes/workflows.py):
   class SemanticErrorResponse(BaseModel):
       """422 response for semantic violations."""
       error_code: str  # e.g., "INVALID_STATUS", "WRONG_PHASE"
       message: str
       current_position: PositionResponse
       detail: Optional[dict] = None
   2. Update InvalidStateError handling:
   except InvalidStateError as e:
       raise HTTPException(
           status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
           detail=SemanticErrorResponse(
               error_code="INVALID_STATUS",
               message=str(e),
               current_position=PositionResponse(...),
           ).model_dump(),
       )

   Files:
   - apps/api/routes/workflows.py

   ---
   Fix 7: Correct state_version Increment Rule (Issues 13, 18)

   Problem: state_version increments on every sync (too often), but rule must
  increment on ALL state-eligibility
   changes, not just position changes.

   Solution: Increment on any change that affects action eligibility: position,
  status, OR data mutations that affect
    validation.

   Changes:

   1. Update sync_db_from_state (workflow_service.py:718-836):
   # Track if state-eligibility changed (Contract §11.3)
   # This includes: position, status, or any data that affects action
  validation
   state_eligibility_changed = (
       # Position changes
       workflow.current_pass != PassType(state.current_pass.value) or
       workflow.current_step != StepName(state.current_step.value) or
       workflow.current_step_number != step_state.step_number or
       # Status changes
       (step_execution and step_execution.status != new_status) or
       # New artifacts created (affects staleness/validation)
       new_artifacts_created or
       # Step execution completed
       step_completed
   )

   # Update workflow position
   workflow.current_pass = PassType(state.current_pass.value)
   workflow.current_step = StepName(state.current_step.value)
   workflow.current_step_number = step_state.step_number
   workflow.updated_at = datetime.utcnow()

   # Increment on ANY state-eligibility change (Contract §11.3)
   if state_eligibility_changed:
       await self._increment_state_version(workflow)
   2. Track state changes in action methods:
     - approve_step(): Always increments (status change)
     - revise_step(): Always increments (new artifact)
     - clarify_step(): Always increments (new message affects eligibility)

   Files:
   - apps/api/application/workflow_service.py

   ---
   Implementation Order

   1. Fix 3 (partial) — Create 003_remediation.py migration with all DB
  changes:
     - Advisory lock fix (hashtext)
     - Staleness columns on traceability_links
     - Staleness propagation trigger (on INSERT)
     - event_id column on workflow_events
     - updated_at column + trigger on step_executions
   2. Fix 5 — Advisory lock (now in 003 migration)
   3. Fix 4 — Progress endpoint:
     - Update StepExecution model (add updated_at)
     - Fix endpoint to query artifact revision
   4. Fix 7 — state_version increment rule:
     - Update workflow_service with correct increment logic
   5. Fix 6 — Structured 422/409 responses:
     - Add SemanticErrorResponse, StateConflictResponse schemas
     - Update exception handlers
   6. Fix 2 — Required OCC fields:
     - Add ExpectedPosition schema with step_name
     - Make fields required on action requests
     - Update validation logic
   7. Fix 3 (complete) — Full staleness:
     - Update TraceabilityLink model
     - Add list_stale_links repository method
     - Update StalenessResponse schema
   8. Fix 1 — Atomic event persistence:
     - Move event persistence into workflow_service transaction
     - Fix replay race condition
     - Update to_sse_dict to accept workflow_id_str
     - Remove emit_event from routes

   ---
   Files to Modify

   | File                                                      | Changes
                   |
   |-----------------------------------------------------------|---------------
  --------------------------------------
   ----------------|
   | infra/db/migrations/versions/003_remediation.py           | CREATE — All
  DB changes (lock fix, staleness,
   event_id, updated_at) |
   | apps/api/application/workflow_service.py                  | Atomic event
  persistence, OCC validation,
   state_version logic       |
   | apps/api/infrastructure/db/models/workflow_event.py       | Add event_id,
  fix to_sse_dict signature
                   |
   | apps/api/infrastructure/db/models/step_execution.py       | Add updated_at
  column
                   |
   | apps/api/infrastructure/db/models/traceability.py         | Add staleness
  columns
                   |
   | apps/api/infrastructure/db/repositories/traceability.py   | Add
  list_stale_links
                   |
   | apps/api/infrastructure/db/repositories/workflow_event.py | Add
  get_max_sequence method
                   |
   | apps/api/routes/workflows.py                              |
  ExpectedPosition, error responses, fix replay,
   remove emit_event    |

   ---
   Success Criteria

   - Events persisted IN SAME TRANSACTION as state updates (atomicity)
   - Replay race condition handled (re-check after broker subscribe)
   - All SSE events have sequence numbers AND event_id
   - workflow_id in payloads is external string, not UUID
   - expected_state_version and expected_position (with step_name) REQUIRED
   - Staleness tracked on both artifacts AND traceability_links
   - Staleness propagation triggers on INSERT (new revision), not UPDATE
   - StalenessResponse includes position, blocking_reasons, stale_links
   - Progress endpoint has real artifact revision, real updated_at (with auto-
  trigger)
   - Advisory lock uses hashtext() — no collision risk
   - 422 responses are structured SemanticErrorResponse
   - 409 responses are structured StateConflictResponse
   - state_version increments on ALL state-eligibility changes
   - New migration 003 created (002 unchanged)
   - All existing tests pass
  </Senior Developer>


• I’m reviewing the proposal carefully, noting many risks and mismatches—like
  incorrect use of session.on_commit with AsyncSession, redundant event_id
  column, incomplete staleness propagation triggers, and inconsistencies between
  plan and spec regarding event payloads and error codes. I’m mapping these
  findings to relevant files and highlighting potential bugs, spec gaps, and
  architectural conflicts, all while preparing to provide clear severity tags
  and recommendations for refining or revising the plan accordingly.

  Identifying key plan gaps and risks

  I'm examining design gaps around event persistence, transaction boundaries,
  and replay logic. The proposal misses clear handling for non-action event
  emission within transactions, relies on nonexistent AsyncSession methods,
  duplicates event IDs in the DB, and risks replay inconsistencies if broker
  backlog is removed without updates. I’m noting schema, spec, and error
  handling mismatches, plus missing or incorrect staleness propagation and
  unclear test impact. These points highlight that the plan needs substantial
  revision before proceeding.

─ Worked for 2m 15s ────────────────────────────────────────────────────────────

• Findings

  - [BLOCK] Atomicity still underspecified: plan uses self._session.on_commit,
    which AsyncSession doesn’t expose, and action methods can’t open their own
    transactions if routes already wrap async with session.begin(). This risks
    event persistence outside the state transition transaction. apps/api/
    application/workflow_service.py, apps/api/routes/workflows.py.
  - [BLOCK] “All events persisted” isn’t actually mapped: removing emit_event()
    without a clear replacement for workflow.started, step.started,
    artifact.delta/final, step.awaiting_review leaves most events unsequenced.
    The plan doesn’t say how sync_db_from_state emits them atomically. apps/api/
    routes/workflows.py, apps/api/application/workflow_service.py.
  - [BLOCK] Replay correctness remains brittle: removing broker backlog requires
    either default from_sequence=0 or test updates; the proposed re-check logic
    also needs broker changes to avoid duplicate/backlog interleaving. apps/api/
    application/event_stream.py, apps/api/routes/workflows.py, apps/api/tests/
    e2e/test_gate_e.py.
  - [BLOCK] Staleness trigger SQL is incorrect and incomplete: it references
    from_step_number (nonexistent) and only marks older revisions of the same
    step, not downstream artifacts/links as required by §9.4/γ4. infra/db/
    migrations/versions/003_remediation.py, apps/api/infrastructure/db/models/
    traceability.py, apps/api/infrastructure/db/models/artifact.py.
  - [HIGH] state_version increment rules remain vague (new_artifacts_created/
    step_completed not defined). Needs a concrete diff of status/phase/artifact/
    staleness changes or it will miss required increments. apps/api/application/
    workflow_service.py.
  - [HIGH] Missing-field validation likely returns FastAPI 422, but spec calls
    for ValidationErrorResponse (400). If you keep 422, log a deviation or add a
    custom handler. apps/api/routes/workflows.py, docs/Issued/Use-
    202601041305/4_SOLVER-technical-spec_V2_7_3.md.
  - [MEDIUM] SSE payloads must include event_type per §12.3; the proposed
    to_sse_dict() example omits it. apps/api/infrastructure/db/models/
    workflow_event.py, apps/api/tests/e2e/test_gate_e.py.
  - [MEDIUM] Adding a separate event_id column is likely redundant with
    workflow_events.id. If needed, reuse id to avoid extra migration risk.
    infra/db/migrations/versions/003_remediation.py, apps/api/infrastructure/db/
    models/workflow_event.py.

  Questions

  - Where will non-action events be persisted and sequenced—sync_db_from_state,
    graph nodes, or a dedicated event layer?
  - How will after-commit broadcasting be implemented with AsyncSession (event
    listener vs route-level publish)?

  Recommendation: REVISE
  Direction is good, but the plan still leaves atomicity, full event coverage,
  and staleness propagation unresolved. Tighten those before implementation.


› what do you propose as specific fixes


• I'm listing precise fixes for event persistence with transactional atomicity,
  moving event emission into service layers, aligning SSE payloads with
  persisted events, resolving replay race conditions with broker and DB
  sequencing, and correcting staleness column naming and propagation as per spec
  —including proposing migration adjustments and propagation triggers within
  transactions.

• Explored
  └ Search propagate_staleness\(|artifact_staleness_trigger|stale_reason in
           4_SOLVER-technical-spec_V2_7_3.md
    Read 4_SOLVER-technical-spec_V2_7_3.md

• This chunk lays out thorough, categorized suggestions for schema updates,
  model changes, event handling improvements, staleness management, and
  optimistic concurrency control to align the implementation with the spec. I'll
  organize fixes into DB migrations, data models, event persistence and SSE
  replay, OCC adjustments, staleness handling, progress tracking, and
  state_version increment logic, aiming to provide a clear, actionable roadmap.

─ Worked for 1m 48s ────────────────────────────────────────────────────────────

• Here are the specific fixes I recommend, mapped to concrete files and
  behaviors. This stays within the contract and addresses the remaining
  blockers.

  DB / Migration (new 003_remediation.py)

  - Add missing artifact lineage and staleness schema per spec (not is_stale):
    supersedes, superseded_by, stale, stale_reason, stale_since, plus unique
    “latest” indexes and latest_artifacts view in infra/db/migrations/
    versions/003_remediation.py. Update apps/api/infrastructure/db/models/
    artifact.py to match those column names.
  - Add traceability link staleness fields and validated_at per spec: stale,
    stale_reason, validated_at in infra/db/migrations/
    versions/003_remediation.py and apps/api/infrastructure/db/models/
    traceability.py.
  - Implement the exact propagate_staleness() trigger from the spec (fires on
    INSERT when supersedes IS NOT NULL, marks downstream latest artifacts and
    traceability links stale). Use the SQL in docs/Issued/Use-
    202601041305/4_SOLVER-technical-spec_V2_7_3.md §7 (shown around the
    propagate_staleness function). This removes the “UPDATE is_stale” approach.
  - Fix advisory lock with hashtext(workflow_id::text) in the
    get_next_event_sequence function (create/replace in 003_remediation.py).
  - Add updated_at to step_executions plus a DB trigger to set it on every
    UPDATE; update apps/api/infrastructure/db/models/step_execution.py.

  Event persistence + atomicity

  - Move all SSE event persistence into the same transaction as state changes.
    Do not call emit_event in routes. Instead:
      - In apps/api/application/workflow_service.py, create/persist events (via
        WorkflowEventRepository.create_event) inside the session.begin() block
        for every state change (including create_workflow, sync_db_from_state,
        and action endpoints).
      - Return the created WorkflowEvent rows to the route, then publish them
        after the transaction commits (route-level publish after async with
        session.begin() completes). This avoids needing session.on_commit.
  - Use the workflow_events.id as event_id in SSE payloads; avoid adding a
    separate event_id column. Update apps/api/infrastructure/db/models/
    workflow_event.py to_sse_dict(workflow_id_str) to include event_id,
    event_type, sequence, timestamp, and external workflow_id.

  SSE replay correctness

  - Modify apps/api/application/event_stream.py to allow a live-only
    subscription (no backlog), or add a separate subscribe_live() so replay
    isn’t mixed with broker backlog.
  - In apps/api/routes/workflows.py stream handler:
      - If from_sequence is None, default to 0 (or update tests to pass
        from_sequence=0).
      - Subscribe to broker first (live-only), then read max_sequence.
      - Replay DB events sequence > from_sequence and <= max_sequence.
      - Consume broker queue and drop any events with sequence <= max_sequence
        to avoid duplicates.
      - Ensure all live events already carry sequences from DB persistence.

  Optimistic concurrency (strict)

  - Make expected_state_version and expected_position required (no Optional) in
    apps/api/routes/workflows.py. Include pass_type, step_number, step_name,
    status.
  - Replace _validate_state_version with _validate_optimistic_concurrency in
    apps/api/application/workflow_service.py; validate version and full position
    (including step_name).
  - Ensure missing fields return a structured validation error. If you want
    exact spec behavior, add a FastAPI exception handler to convert
    RequestValidationError into ValidationErrorResponse (400) per docs/Issued/
    Use-202601041305/4_SOLVER-technical-spec_V2_7_3.md.

  Staleness endpoints

  - Update GET /workflows/{id}/staleness to include position, blocking_reasons,
    and stale_links per §10.2 in apps/api/routes/workflows.py. Add
    TraceabilityRepository.list_stale_links() in apps/api/infrastructure/db/
    repositories/traceability.py.
  - Add acknowledge-stale and re-execute endpoints (spec §9.4 / §10.2) to
    unblock γ4, including expected_state_version checks and event emission.

  Progress endpoint completion

  - Populate latest_artifact_revision by fetching the artifact referenced by
    latest_artifact_id (now has revision). Use step_exec.updated_at (after the
    new column/trigger).

  state_version increments

  - In apps/api/application/workflow_service.py, increment state_version on any
    state-eligibility change: position change, status/phase change, creation of
    new artifact revision, staleness updates. Use explicit before/after
    comparisons and the artifact_results return value to detect new artifacts.

  If you want, I can convert this into a slice‑by‑slice checklist aligned with
  the execution protocol.


› Review this proposal from Senior_Developer and whether you agree or disagree
  explain yourself.

  <Senior Developer>
  Backend Contract Alignment — Remediation Plan

   Project: solver-engine
   Working Directory: /Users/ryan/ai-env/projects/solver-engine

   Revision: Co-Developer-1 Remediation v3 (2026-01-04)

   ---
   Objective

   Fix all BLOCK/HIGH issues from Co-Developer-1's three reviews. Address
  critical gaps in:
   - SSE event persistence atomicity
   - Full event coverage for all event types
   - Staleness schema alignment with spec
   - Replay correctness without race conditions
   - Optimistic concurrency validation

   ---
   Issues to Fix (All Reviews Combined)

   BLOCK Issues

   | ID  | Issue
  | Root Cause
                |
   |-----|---------------------------------------------------------------------
  ------|-------------------------------
   -------------|
   | 1   | session.on_commit doesn't exist on AsyncSession
  | Plan used non-existent API
                |
   | 2   | Non-action events (workflow.started, step.started, artifact.*) not
  mapped | No persistence point defined
                |
   | 3   | Replay mixes DB events with broker backlog
  | subscribe() always replays
   backlog         |
   | 4   | Staleness trigger SQL references from_step_number (nonexistent)
  | Column is from_step
                |
   | 5   | Staleness only marks same-step older revisions, not downstream
  | Spec requires marking
   downstream artifacts |
   | 6   | Artifact model has is_stale, spec requires stale
  | Column name mismatch
                |
   | 7   | Missing supersedes/superseded_by columns on artifacts
  | Required for lineage tracking
                |
   | 8   | expected_state_version optional (bypasses on None)
  | Line 319-320 returns early
                |
   | 9   | expected_position field completely missing
  | No schema field exists
                |

   HIGH Issues

   | ID  | Issue                                 | Root Cause
  |

  |-----|---------------------------------------|------------------------------
  ---------------|
   | 10  | latest_artifact_revision is TODO      | Line 627 sets None
  |
   | 11  | updated_at missing on StepExecution   | Model only has started_at,
  completed_at     |
   | 12  | Advisory lock uses first 8 UUID chars | Collision risk
  |
   | 13  | state_version increment rules vague   | Need concrete before/after
  comparison       |
   | 14  | Missing-field validation returns 422  | Spec requires
  ValidationErrorResponse (400) |

   MEDIUM Issues

   | ID  | Issue                                   | Root Cause
  |

  |-----|-----------------------------------------|----------------------------
  |
   | 15  | SSE payloads missing event_type         | to_sse_dict() omits it
  |
   | 16  | Separate event_id column redundant      | Can use workflow_events.id
  |
   | 17  | Traceability links missing validated_at | Spec requires it
  |

   ---
   Remediation Plan

   Fix 1: Atomic Event Persistence + Full Event Coverage (Issues 1, 2, 15, 16)

   Problem:
   - session.on_commit doesn't exist on AsyncSession
   - Non-action events (workflow.started, step.started, artifact.*) have no
  persistence point
   - SSE payloads missing event_type

   Solution: Service persists events in transaction, RETURNS them to route.
  Route publishes AFTER transaction commits
    (no special API needed).

   Changes:

   1. Update workflow_service.py to persist events AND return them:
   async def create_workflow(self, ...) -> tuple[Workflow,
  list[WorkflowEvent]]:
       """Returns workflow AND events to publish."""
       events = []
       async with self._session.begin():
           # ... create workflow ...

           # Persist workflow.started event IN transaction
           event = await self._event_repo.create_event(
               workflow_id=workflow.id,
               event_type="workflow.started",
               payload={...},
           )
           events.append(event)

       return workflow, events  # Route publishes events after txn commits

   async def sync_db_from_state(self, ...) -> list[WorkflowEvent]:
       """Persist state changes AND emit appropriate events."""
       events = []

       # Detect state changes and persist events:
       if step_started:
           events.append(await self._event_repo.create_event(...,
  "step.started", ...))
       if artifact_created:
           events.append(await self._event_repo.create_event(...,
  "artifact.final", ...))
       if status_changed_to_awaiting_review:
           events.append(await self._event_repo.create_event(...,
  "step.awaiting_review", ...))

       return events  # Caller publishes after commit
   2. Update routes to publish events AFTER transaction:
   @router.post("/workflows")
   async def create_workflow(...):
       async with db.session() as session:
           service = WorkflowService(session, broker)
           workflow, events = await service.create_workflow(...)

       # Transaction committed - now safe to publish
       for event in events:
           await broker.publish(
               workflow.workflow_id,
               SSEEvent(event.event_type,
  event.to_sse_dict(workflow.workflow_id))
           )
   3. Event type mapping (WHERE each event gets persisted):
   | Event Type           | Persisted In         | Trigger
  |

  |----------------------|----------------------|------------------------------
  |
   | workflow.started     | create_workflow()    | Workflow created
  |
   | step.started         | sync_db_from_state() | Step execution created
  |
   | artifact.delta       | sync_db_from_state() | Partial artifact content
  |
   | artifact.final       | sync_db_from_state() | Artifact completed
  |
   | step.awaiting_review | sync_db_from_state() | Status → awaiting_review
  |
   | step.approved        | approve_step()       | Actor approves
  |
   | step.revised         | revise_step()        | Actor requests revision
  |
   | step.clarified       | clarify_step()       | Actor provides clarification
  |

   4. Fix to_sse_dict() to include event_type and use id as event_id:
   def to_sse_dict(self, workflow_id_str: str) -> dict:
       return {
           "event_id": str(self.id),  # Use existing PK, no new column needed
           "event_type": self.event_type,  # Required per §12.3
           "sequence": self.sequence,
           "workflow_id": workflow_id_str,  # External string ID
           "timestamp": self.created_at.isoformat(),
           **self.payload,
       }

   Files:
   - apps/api/application/workflow_service.py (return events from methods)
   - apps/api/routes/workflows.py (publish after transaction)
   - apps/api/infrastructure/db/models/workflow_event.py (fix to_sse_dict)

   ---
   Fix 2: SSE Replay Correctness (Issue 3)

   Problem: Current subscribe() always replays backlog first, which mixes with
  DB replay causing duplicates/gaps.

   Solution: Add subscribe_live() method to EventBroker. Replay uses ONLY DB
  events, then transitions to live-only
   broker stream.

   Changes:

   1. Add subscribe_live() to EventBroker (event_stream.py):
   async def subscribe_live(
       self,
       workflow_id: str,
       timeout: float = 30.0,
   ) -> AsyncIterator[SSEEvent]:
       """Subscribe to LIVE events only (no backlog replay).

       Use this when replaying from DB to avoid backlog mixing.
       """
       queue: asyncio.Queue[SSEEvent] = asyncio.Queue()

       async with self._lock:
           self._queues[workflow_id] = queue
           # NO backlog replay - just register for live events

       try:
           while True:
               try:
                   event = await asyncio.wait_for(queue.get(), timeout=timeout)
                   yield event
               except asyncio.TimeoutError:
                   yield HEARTBEAT_EVENT
       except asyncio.CancelledError:
           raise
       finally:
           async with self._lock:
               if self._queues.get(workflow_id) is queue:
                   del self._queues[workflow_id]
   2. Fix stream endpoint replay (routes/workflows.py):
   async def stream_events(workflow_id: str, from_sequence: int = 0):  #
  Default 0
       # 1. Subscribe to broker FIRST (live-only, no backlog)
       live_stream = broker.subscribe_live(workflow_id)

       # 2. Get max sequence at subscription time
       max_seq = await event_repo.get_max_sequence(workflow_id)

       # 3. Replay DB events: from_sequence < seq <= max_seq
       db_events = await event_repo.list_events(
           workflow_id, from_sequence=from_sequence, to_sequence=max_seq
       )
       for event in db_events:
           yield format_sse(event.to_sse_dict(workflow.workflow_id))

       # 4. Consume live events, dropping any with sequence <= max_seq
       async for event in live_stream:
           if event.payload.get("sequence", 0) > max_seq:
               yield format_sse(event.payload)
   3. Add get_max_sequence() to WorkflowEventRepository:
   async def get_max_sequence(self, workflow_id: UUID) -> int:
       stmt = select(func.coalesce(func.max(WorkflowEvent.sequence), 0)).where(
           WorkflowEvent.workflow_id == workflow_id
       )
       result = await self._session.execute(stmt)
       return result.scalar_one()
   4. Update tests to pass from_sequence=0 (or update endpoint default)

   Files:
   - apps/api/application/event_stream.py (add subscribe_live)
   - apps/api/routes/workflows.py (fix replay logic)
   - apps/api/infrastructure/db/repositories/workflow_event.py (add
  get_max_sequence)
   - apps/api/tests/e2e/test_gate_e.py (update if needed)

   ---
   Fix 3: Required Optimistic Concurrency Fields (Issues 8, 9)

   Problem: expected_state_version is optional; expected_position doesn't
  exist.

   Solution: Make both fields required. Validate full position including
  step_name.

   Changes:

   1. Update request schemas (routes/workflows.py):
   class ExpectedPosition(BaseModel):
       """Expected workflow position for optimistic concurrency."""
       pass_type: str
       step_name: str
       step_number: int
       status: str

   class ApproveRequest(BaseModel):
       actor_id: Optional[str] = None
       expected_state_version: int = Field(..., description="Required per
  §11.1")
       expected_position: ExpectedPosition = Field(..., description="Required
  per §11.1")

   # Same for ReviseRequest, ClarifyRequest
   2. Replace _validate_state_version with _validate_optimistic_concurrency:
   def _validate_optimistic_concurrency(
       self,
       workflow: Workflow,
       step_execution: StepExecution,
       expected_state_version: int,
       expected_position: ExpectedPosition,
   ) -> None:
       # Version check
       if expected_state_version != workflow.state_version:
           raise StateVersionMismatchError(
               expected=expected_state_version,
               actual=workflow.state_version,
           )

       # Full position check (includes step_name)
       if (expected_position.pass_type != workflow.current_pass.value or
           expected_position.step_name != workflow.current_step.value or
           expected_position.step_number != workflow.current_step_number or
           expected_position.status != step_execution.status.value):
           raise StateVersionMismatchError(
               message="Position mismatch",
               expected=expected_position.model_dump(),
               actual={...},
           )
   3. Remove the if expected_state_version is None: return bypass (line 319-
  320)

   Files:
   - apps/api/routes/workflows.py (schemas + endpoint calls)
   - apps/api/application/workflow_service.py (validation + signatures)

   ---
   Fix 4: Staleness Schema + Trigger Alignment (Issues 4, 5, 6, 7, 17)

   Problem:
   - Artifact model has is_stale, spec requires stale
   - Missing supersedes/superseded_by columns for lineage
   - Trigger uses nonexistent from_step_number (column is from_step)
   - Trigger only marks same-step older revisions, not DOWNSTREAM artifacts per
  §9.4

   Solution: Align schema with spec. Use exact trigger SQL from spec §7.

   Changes:

   1. Create NEW migration 003_remediation.py:
   -- ============================================================
   -- ARTIFACT LINEAGE COLUMNS (per spec §7)
   -- ============================================================
   ALTER TABLE artifacts
   ADD COLUMN supersedes UUID REFERENCES artifacts(id),
   ADD COLUMN superseded_by UUID REFERENCES artifacts(id);

   -- Rename is_stale to stale (spec uses 'stale')
   ALTER TABLE artifacts RENAME COLUMN is_stale TO stale;

   -- Unique index: exactly one "latest" per lineage (methodology_doc)
   CREATE UNIQUE INDEX idx_latest_methodology_doc ON artifacts(
       workflow_id, step_name, artifact_type, document_type, document_version
   ) WHERE artifact_type = 'methodology_doc' AND superseded_by IS NULL;

   -- Unique index: exactly one "latest" per lineage (step_package)
   CREATE UNIQUE INDEX idx_latest_step_package ON artifacts(
       workflow_id, step_name, artifact_type
   ) WHERE artifact_type = 'step_package' AND superseded_by IS NULL;

   -- Convenience view for latest artifacts
   CREATE VIEW latest_artifacts AS
   SELECT * FROM artifacts WHERE superseded_by IS NULL;

   -- ============================================================
   -- TRACEABILITY LINK STALENESS (per spec §7)
   -- ============================================================
   ALTER TABLE traceability_links
   ADD COLUMN stale BOOLEAN NOT NULL DEFAULT FALSE,
   ADD COLUMN stale_reason VARCHAR(500) NULL,
   ADD COLUMN stale_since TIMESTAMPTZ NULL,
   ADD COLUMN validated_at TIMESTAMPTZ NULL;

   -- ============================================================
   -- STALENESS PROPAGATION TRIGGER (exact spec §7 SQL)
   -- ============================================================
   -- When a new artifact revision is inserted (supersedes IS NOT NULL),
   -- mark DOWNSTREAM latest artifacts and traceability links as stale.

   CREATE OR REPLACE FUNCTION propagate_staleness()
   RETURNS TRIGGER AS $$
   BEGIN
       -- Only propagate if this is a new revision (not first creation)
       IF NEW.supersedes IS NOT NULL THEN
           -- Mark downstream latest artifacts as stale
           UPDATE artifacts
           SET stale = TRUE,
               stale_reason = 'upstream_step_' || NEW.step_number ||
  '_revised',
               stale_since = NOW()
           WHERE workflow_id = NEW.workflow_id
             AND step_number > NEW.step_number  -- DOWNSTREAM steps
             AND superseded_by IS NULL  -- Only mark latest versions
             AND stale = FALSE;

           -- Also mark affected traceability links
           UPDATE traceability_links
           SET stale = TRUE,
               stale_reason = 'source_step_' || NEW.step_number || '_revised',
               stale_since = NOW()
           WHERE workflow_id = NEW.workflow_id
             AND from_step = NEW.step_number  -- Links FROM this step
             AND stale = FALSE;
       END IF;
       RETURN NEW;
   END;
   $$ LANGUAGE plpgsql;

   -- Trigger fires on INSERT when supersedes IS NOT NULL (new revision)
   CREATE TRIGGER artifact_staleness_trigger
   AFTER INSERT ON artifacts
   FOR EACH ROW
   WHEN (NEW.supersedes IS NOT NULL)
   EXECUTE FUNCTION propagate_staleness();

   -- ============================================================
   -- ADVISORY LOCK FIX (collision-resistant)
   -- ============================================================
   CREATE OR REPLACE FUNCTION get_next_event_sequence(p_workflow_id UUID)
   RETURNS INTEGER AS $$
   DECLARE
       next_seq INTEGER;
       lock_id BIGINT;
   BEGIN
       lock_id := hashtext(p_workflow_id::text)::bigint;
       PERFORM pg_advisory_xact_lock(lock_id);
       SELECT COALESCE(MAX(sequence), 0) + 1 INTO next_seq
       FROM workflow_events WHERE workflow_id = p_workflow_id;
       RETURN next_seq;
   END;
   $$ LANGUAGE plpgsql;

   -- ============================================================
   -- STEP EXECUTION UPDATED_AT
   -- ============================================================
   ALTER TABLE step_executions
   ADD COLUMN updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW();

   CREATE OR REPLACE FUNCTION update_step_execution_timestamp()
   RETURNS TRIGGER AS $$
   BEGIN
       NEW.updated_at = NOW();
       RETURN NEW;
   END;
   $$ LANGUAGE plpgsql;

   CREATE TRIGGER step_execution_updated_at
   BEFORE UPDATE ON step_executions
   FOR EACH ROW EXECUTE FUNCTION update_step_execution_timestamp();
   2. Update Artifact model (artifact.py):
     - Rename is_stale → stale
     - Add supersedes, superseded_by columns
   3. Update TraceabilityLink model (traceability.py):
     - Add stale, stale_reason, stale_since, validated_at columns
   4. Update StalenessResponse schema (routes/workflows.py):
   class StalenessResponse(BaseModel):
       workflow_id: str
       position: PositionResponse
       can_complete: bool
       blocking_reasons: list[str]
       stale_artifacts: list[StaleArtifactEntry]
       stale_links: list[StaleLinkEntry]
   5. Add acknowledge-stale and re-execute endpoints per §10.2

   Files:
   - infra/db/migrations/versions/003_remediation.py (CREATE)
   - apps/api/infrastructure/db/models/artifact.py (schema alignment)
   - apps/api/infrastructure/db/models/traceability.py (add columns)
   - apps/api/routes/workflows.py (staleness endpoints)
   - apps/api/infrastructure/db/repositories/traceability.py (add
  list_stale_links)

   ---
   Fix 5: Progress Endpoint Completion (Issues 10, 11)

   Problem: latest_artifact_revision is TODO; updated_at doesn't exist on
  StepExecution.

   Solution: Query artifact for revision; updated_at already in migration (Fix
  4).

   Changes:

   1. Update StepExecution model (step_execution.py):
     - Add updated_at column (DB trigger handles auto-update)
   2. Fix progress endpoint (routes/workflows.py):
   # Get latest artifact revision (no more TODO)
   latest_revision = None
   if step_exec.latest_artifact_id:
       artifact = await artifact_repo.get(step_exec.latest_artifact_id)
       if artifact:
           latest_revision = artifact.revision

   StepProgressEntry(
       ...
       latest_artifact_revision=latest_revision,
       updated_at=step_exec.updated_at,
   )

   Files:
   - apps/api/infrastructure/db/models/step_execution.py (add column)
   - apps/api/routes/workflows.py (fix progress endpoint)

   ---
   Fix 6: Validation Error Handling (Issue 14)

   Problem: Spec requires ValidationErrorResponse (400) for missing fields;
  FastAPI returns 422.

   Solution: Add custom exception handler OR document as deviation.

   Changes:

   1. Option A (Spec Compliance): Add exception handler:
   from fastapi.exceptions import RequestValidationError

   @app.exception_handler(RequestValidationError)
   async def validation_exception_handler(request, exc):
       return JSONResponse(
           status_code=400,
           content=ValidationErrorResponse(
               error_code="VALIDATION_ERROR",
               message="Request validation failed",
               errors=[{"loc": e["loc"], "msg": e["msg"]} for e in
  exc.errors()],
           ).model_dump(),
       )
   2. Option B (Document Deviation): Log in docs/DECISIONS.md that we use
  FastAPI's 422 instead of spec's 400.

   Recommendation: Option A for full compliance.

   Files:
   - apps/api/main.py (add exception handler)
   - apps/api/routes/workflows.py (add ValidationErrorResponse schema)

   ---
   Fix 7: state_version Increment Rule (Issue 13)

   Problem: state_version rules vague; need concrete before/after comparison.

   Solution: Use explicit comparisons for all state-eligibility changes.

   Changes:

   1. Update sync_db_from_state (workflow_service.py):
   # Capture state BEFORE changes
   old_pass = workflow.current_pass
   old_step = workflow.current_step
   old_step_number = workflow.current_step_number
   old_status = step_execution.status if step_execution else None

   # ... apply state changes ...

   # Detect state-eligibility changes (Contract §11.3)
   state_eligibility_changed = (
       old_pass != workflow.current_pass or
       old_step != workflow.current_step or
       old_step_number != workflow.current_step_number or
       (step_execution and old_status != step_execution.status) or
       len(artifact_results) > 0  # New artifacts created
   )

   if state_eligibility_changed:
       await self._increment_state_version(workflow)
   2. Action methods always increment (they change status):
     - approve_step(): status changes → increment
     - revise_step(): new artifact → increment
     - clarify_step(): message added → increment

   Files:
   - apps/api/application/workflow_service.py

   ---
   Implementation Order

   1. Fix 4 — Create 003_remediation.py migration with ALL DB changes:
     - Artifact lineage columns (supersedes, superseded_by)
     - Rename is_stale → stale
     - Unique indexes for "latest" per lineage
     - latest_artifacts view
     - Traceability link staleness columns + validated_at
     - Staleness propagation trigger (spec §7 SQL)
     - Advisory lock fix (hashtext)
     - step_executions.updated_at + trigger
   2. Fix 4 (models) — Update SQLAlchemy models:
     - artifact.py: Add supersedes, superseded_by, rename is_stale → stale
     - traceability.py: Add stale, stale_reason, stale_since, validated_at
     - step_execution.py: Add updated_at
   3. Fix 5 — Progress endpoint:
     - Fix latest_artifact_revision TODO
     - Use step_exec.updated_at
   4. Fix 7 — state_version increment rule:
     - Concrete before/after comparisons in sync_db_from_state
   5. Fix 6 — Validation error handling:
     - Add custom exception handler for 400 response
   6. Fix 3 — Required OCC fields:
     - ExpectedPosition schema (with step_name)
     - Replace _validate_state_version with _validate_optimistic_concurrency
     - Remove None bypass
   7. Fix 4 (endpoints) — Staleness endpoints:
     - Update StalenessResponse schema
     - Add list_stale_links repository method
     - Add acknowledge-stale and re-execute endpoints
   8. Fix 2 — SSE Replay:
     - Add subscribe_live() to EventBroker
     - Add get_max_sequence() to repository
     - Fix stream endpoint replay logic
   9. Fix 1 — Atomic event persistence:
     - Service methods return events list
     - Routes publish after transaction commits
     - Full event type coverage (workflow.started, step.started, etc.)
     - Fix to_sse_dict (include event_type, use id as event_id)

   ---
   Files to Modify

   | File                                                      | Changes
                  |
   |-----------------------------------------------------------|---------------
  --------------------------------------
   ---------------|
   | infra/db/migrations/versions/003_remediation.py           | CREATE — All
  DB changes
                  |
   | apps/api/infrastructure/db/models/artifact.py             | Add
  supersedes/superseded_by, rename is_stale→stale
                  |
   | apps/api/infrastructure/db/models/traceability.py         | Add stale,
  stale_reason, stale_since, validated_at
                  |
   | apps/api/infrastructure/db/models/step_execution.py       | Add updated_at
                  |
   | apps/api/infrastructure/db/models/workflow_event.py       | Fix
  to_sse_dict (event_type, event_id=id)
                  |
   | apps/api/infrastructure/db/repositories/traceability.py   | Add
  list_stale_links
                  |
   | apps/api/infrastructure/db/repositories/workflow_event.py | Add
  get_max_sequence
                  |
   | apps/api/application/event_stream.py                      | Add
  subscribe_live() method
                  |
   | apps/api/application/workflow_service.py                  | Return events,
  OCC validation, state_version logic
                  |
   | apps/api/routes/workflows.py                              |
  ExpectedPosition, error responses, replay fix,
   staleness endpoints |
   | apps/api/main.py                                          | Add validation
  exception handler
                  |

   ---
   Success Criteria

   - Events persisted IN SAME TRANSACTION as state changes
   - Routes publish events AFTER transaction commits (no session.on_commit)
   - All event types mapped (workflow.started, step.started, artifact.*,
  step.awaiting_review, etc.)
   - SSE payloads include event_type, event_id (from workflow_events.id),
  sequence, external workflow_id
   - subscribe_live() method exists for live-only streaming (no backlog)
   - Replay: subscribe FIRST, get max_seq, replay DB, drop broker events <=
  max_seq
   - expected_state_version and expected_position (with step_name) REQUIRED
   - _validate_optimistic_concurrency validates full position
   - Artifact model has supersedes, superseded_by, stale (not is_stale)
   - Unique indexes enforce exactly one "latest" per artifact lineage
   - Staleness trigger fires on INSERT when supersedes IS NOT NULL
   - Staleness propagates to DOWNSTREAM artifacts (step_number >
  NEW.step_number)
   - Traceability links have stale, stale_reason, stale_since, validated_at
   - StalenessResponse includes position, blocking_reasons, stale_links
   - acknowledge-stale and re-execute endpoints exist
   - Progress endpoint has real artifact revision, real updated_at
   - Advisory lock uses hashtext() — no collision risk
   - Validation errors return 400 (not FastAPI's 422)
   - state_version increments on concrete state-eligibility changes
   - New migration 003 created (002 unchanged)
   - All existing tests pass
  </Senior Developer>


• Verdict: Mostly agree, with two blocking caveats and a few required
  clarifications.

  Agreements

  - Aligning schema to spec (stale, supersedes, superseded_by, traceability
    staleness) is correct.
  - Moving event persistence into the same transaction and publishing after
    commit is the right pattern.
  - subscribe_live() + DB replay with max-seq guard is the correct replay
    strategy.
  - Strict expected_state_version + expected_position and a structured
    validation error handler are required.

  Blocking caveats

  - [BLOCK] Event coverage is still underspecified. You propose persisting
    artifact.delta/artifact.final in sync_db_from_state, but those are generated
    in the API layer today (synthetic deltas after graph.ainvoke). If you remove
    emit_event() from routes without an equivalent persistence path, you will
    lose these events and break Gate E tests. You must explicitly persist/
    publish those synthetic artifacts in the route (after commit) and have them
    go through the event persistence path.
  - [BLOCK] Atomicity across action flow is still unclear. The route currently
    wraps service.approve_step() in async with session.begin() and then runs the
    graph. If you persist events inside approve_step, you must ensure they are
    created within the caller’s transaction (not a nested one) and that the
    route publishes them after the transaction completes. Spell out how this
    will be implemented for both action endpoints and graph-result
    sync_db_from_state events.

  Required clarifications

  - artifact.delta/artifact.final are synthetic now; define whether they remain
    API-generated and, if so, how they are persisted (same DB path as other
    events).
  - ValidationErrorResponse 400 handler should apply to all request validation
    errors, not just action endpoints (otherwise spec drift).
  - state_version increment rules should explicitly include status/phase changes
    even when position doesn’t change.

  Recommendation: REVISE
  The plan is solid and nearly correct. Add explicit handling for synthetic
  artifact events and make transaction boundaries and publish timing explicit
  for all event types. After that, I’d approve.


› Review this proposal from Senior_Developer and whether you agree or disagree
  explain yourself.

  <Senior Developer>
   Backend Contract Alignment — Remediation Plan

   Project: solver-engine
   Working Directory: /Users/ryan/ai-env/projects/solver-engine

   Revision: Co-Developer-1 Remediation v3 (2026-01-04)

   ---
   Objective

   Fix all BLOCK/HIGH issues from Co-Developer-1's three reviews. Address
  critical gaps in:
   - SSE event persistence atomicity
   - Full event coverage for all event types
   - Staleness schema alignment with spec
   - Replay correctness without race conditions
   - Optimistic concurrency validation

   ---
   Issues to Fix (All Reviews Combined)

   BLOCK Issues

   | ID  | Issue
  | Root Cause
                |
   |-----|---------------------------------------------------------------------
  ------|-------------------------------
   -------------|
   | 1   | session.on_commit doesn't exist on AsyncSession
  | Plan used non-existent API
                |
   | 2   | Non-action events (workflow.started, step.started, artifact.*) not
  mapped | No persistence point defined
                |
   | 3   | Replay mixes DB events with broker backlog
  | subscribe() always replays
   backlog         |
   | 4   | Staleness trigger SQL references from_step_number (nonexistent)
  | Column is from_step
                |
   | 5   | Staleness only marks same-step older revisions, not downstream
  | Spec requires marking
   downstream artifacts |
   | 6   | Artifact model has is_stale, spec requires stale
  | Column name mismatch
                |
   | 7   | Missing supersedes/superseded_by columns on artifacts
  | Required for lineage tracking
                |
   | 8   | expected_state_version optional (bypasses on None)
  | Line 319-320 returns early
                |
   | 9   | expected_position field completely missing
  | No schema field exists
                |

   HIGH Issues

   | ID  | Issue                                 | Root Cause
  |

  |-----|---------------------------------------|------------------------------
  ---------------|
   | 10  | latest_artifact_revision is TODO      | Line 627 sets None
  |
   | 11  | updated_at missing on StepExecution   | Model only has started_at,
  completed_at     |
   | 12  | Advisory lock uses first 8 UUID chars | Collision risk
  |
   | 13  | state_version increment rules vague   | Need concrete before/after
  comparison       |
   | 14  | Missing-field validation returns 422  | Spec requires
  ValidationErrorResponse (400) |

   MEDIUM Issues

   | ID  | Issue                                   | Root Cause
  |

  |-----|-----------------------------------------|----------------------------
  |
   | 15  | SSE payloads missing event_type         | to_sse_dict() omits it
  |
   | 16  | Separate event_id column redundant      | Can use workflow_events.id
  |
   | 17  | Traceability links missing validated_at | Spec requires it
  |

   ---
   Remediation Plan

   Fix 1: Atomic Event Persistence + Full Event Coverage (Issues 1, 2, 15, 16)

   Problem:
   - session.on_commit doesn't exist on AsyncSession
   - Non-action events (workflow.started, step.started, artifact.*) have no
  persistence point
   - SSE payloads missing event_type
   - artifact.delta/artifact.final are SYNTHETIC events generated in routes
  after graph execution — removing
   emit_event() without replacement breaks Gate E

   Solution:
   - Service persists "native" events (workflow.started, step.started,
  step.awaiting_review, action events) in
   transaction, RETURNS them
   - Route persists "synthetic" events (artifact.delta, artifact.final) INSIDE
  the same transaction
   - Route publishes ALL events AFTER transaction commits

   Changes:

   1. Transaction boundary ownership:
   # Route owns the transaction boundary
   @router.post("/workflows/{id}/approve")
   async def approve_step(...):
       events_to_publish = []

       async with db.session() as session:
           async with session.begin():  # Transaction boundary
               service = WorkflowService(session)
               event_repo = WorkflowEventRepository(session)

               # 1. Action persists its own event
               action_events = await service.approve_step(...)
               events_to_publish.extend(action_events)

               # 2. Run graph (inside transaction)
               result = await graph.ainvoke(...)

               # 3. sync_db_from_state persists native events
               sync_events = await service.sync_db_from_state(...)
               events_to_publish.extend(sync_events)

               # 4. Route persists synthetic artifact events (INSIDE
  transaction)
               for artifact in result.get("new_artifacts", []):
                   event = await event_repo.create_event(
                       workflow_id=workflow.id,
                       event_type="artifact.final",
                       payload={"artifact_id": str(artifact.id), ...},
                   )
                   events_to_publish.append(event)

           # Transaction committed here

       # 5. Publish ALL events AFTER transaction commits
       for event in events_to_publish:
           await broker.publish(
               workflow.workflow_id,
               SSEEvent(event.event_type,
  event.to_sse_dict(workflow.workflow_id))
           )
   2. Event type mapping (WHERE each event gets persisted):
   | Event Type           | Persisted By | Location              | Notes
  |

  |----------------------|--------------|-----------------------|--------------
  --------------|
   | workflow.started     | Service      | create_workflow()     | Native
  |
   | step.started         | Service      | sync_db_from_state()  | Native
  |
   | step.awaiting_review | Service      | sync_db_from_state()  | Native
  |
   | step.approved        | Service      | approve_step()        | Native
  |
   | step.revised         | Service      | revise_step()         | Native
  |
   | step.clarified       | Service      | clarify_step()        | Native
  |
   | artifact.delta       | Route        | After graph chunk     | Synthetic —
  route persists |
   | artifact.final       | Route        | After graph completes | Synthetic —
  route persists |
   | workflow.completed   | Service      | sync_db_from_state()  | Native
  |

   3. Update workflow_service.py methods to return events (not manage
  transaction):
   async def approve_step(self, ...) -> list[WorkflowEvent]:
       """Runs INSIDE caller's transaction. Returns events to publish."""
       # NOTE: Does NOT call session.begin() — caller owns transaction

       # ... validation, state changes ...

       # Persist step.approved event
       event = await self._event_repo.create_event(
           workflow_id=workflow.id,
           event_type="step.approved",
           payload={...},
       )
       return [event]

   async def sync_db_from_state(self, ...) -> list[WorkflowEvent]:
       """Runs INSIDE caller's transaction. Persists native events."""
       events = []

       if step_started:
           events.append(await self._event_repo.create_event(...,
  "step.started", ...))
       if status_changed_to_awaiting_review:
           events.append(await self._event_repo.create_event(...,
  "step.awaiting_review", ...))

       return events  # Does NOT include synthetic artifact events
   4. Fix to_sse_dict() to include event_type and use id as event_id:
   def to_sse_dict(self, workflow_id_str: str) -> dict:
       return {
           "event_id": str(self.id),  # Use existing PK, no new column needed
           "event_type": self.event_type,  # Required per §12.3
           "sequence": self.sequence,
           "workflow_id": workflow_id_str,  # External string ID
           "timestamp": self.created_at.isoformat(),
           **self.payload,
       }
   5. For streaming artifact deltas (artifact.delta):
     - Route uses graph.astream() to get chunks
     - After each chunk, persist artifact.delta event INSIDE transaction
     - Collect events, publish ALL after transaction commits

   Key invariant: All events (native + synthetic) are persisted INSIDE the same
  transaction as the state change, then
    published AFTER commit.

   Files:
   - apps/api/application/workflow_service.py (return events, no internal
  transaction)
   - apps/api/routes/workflows.py (own transaction, persist synthetic events,
  publish after commit)
   - apps/api/infrastructure/db/models/workflow_event.py (fix to_sse_dict)

   ---
   Fix 2: SSE Replay Correctness (Issue 3)

   Problem: Current subscribe() always replays backlog first, which mixes with
  DB replay causing duplicates/gaps.

   Solution: Add subscribe_live() method to EventBroker. Replay uses ONLY DB
  events, then transitions to live-only
   broker stream.

   Changes:

   1. Add subscribe_live() to EventBroker (event_stream.py):
   async def subscribe_live(
       self,
       workflow_id: str,
       timeout: float = 30.0,
   ) -> AsyncIterator[SSEEvent]:
       """Subscribe to LIVE events only (no backlog replay).

       Use this when replaying from DB to avoid backlog mixing.
       """
       queue: asyncio.Queue[SSEEvent] = asyncio.Queue()

       async with self._lock:
           self._queues[workflow_id] = queue
           # NO backlog replay - just register for live events

       try:
           while True:
               try:
                   event = await asyncio.wait_for(queue.get(), timeout=timeout)
                   yield event
               except asyncio.TimeoutError:
                   yield HEARTBEAT_EVENT
       except asyncio.CancelledError:
           raise
       finally:
           async with self._lock:
               if self._queues.get(workflow_id) is queue:
                   del self._queues[workflow_id]
   2. Fix stream endpoint replay (routes/workflows.py):
   async def stream_events(workflow_id: str, from_sequence: int = 0):  #
  Default 0
       # 1. Subscribe to broker FIRST (live-only, no backlog)
       live_stream = broker.subscribe_live(workflow_id)

       # 2. Get max sequence at subscription time
       max_seq = await event_repo.get_max_sequence(workflow_id)

       # 3. Replay DB events: from_sequence < seq <= max_seq
       db_events = await event_repo.list_events(
           workflow_id, from_sequence=from_sequence, to_sequence=max_seq
       )
       for event in db_events:
           yield format_sse(event.to_sse_dict(workflow.workflow_id))

       # 4. Consume live events, dropping any with sequence <= max_seq
       async for event in live_stream:
           if event.payload.get("sequence", 0) > max_seq:
               yield format_sse(event.payload)
   3. Add get_max_sequence() to WorkflowEventRepository:
   async def get_max_sequence(self, workflow_id: UUID) -> int:
       stmt = select(func.coalesce(func.max(WorkflowEvent.sequence), 0)).where(
           WorkflowEvent.workflow_id == workflow_id
       )
       result = await self._session.execute(stmt)
       return result.scalar_one()
   4. Update tests to pass from_sequence=0 (or update endpoint default)

   Files:
   - apps/api/application/event_stream.py (add subscribe_live)
   - apps/api/routes/workflows.py (fix replay logic)
   - apps/api/infrastructure/db/repositories/workflow_event.py (add
  get_max_sequence)
   - apps/api/tests/e2e/test_gate_e.py (update if needed)

   ---
   Fix 3: Required Optimistic Concurrency Fields (Issues 8, 9)

   Problem: expected_state_version is optional; expected_position doesn't
  exist.

   Solution: Make both fields required. Validate full position including
  step_name.

   Changes:

   1. Update request schemas (routes/workflows.py):
   class ExpectedPosition(BaseModel):
       """Expected workflow position for optimistic concurrency."""
       pass_type: str
       step_name: str
       step_number: int
       status: str

   class ApproveRequest(BaseModel):
       actor_id: Optional[str] = None
       expected_state_version: int = Field(..., description="Required per
  §11.1")
       expected_position: ExpectedPosition = Field(..., description="Required
  per §11.1")

   # Same for ReviseRequest, ClarifyRequest
   2. Replace _validate_state_version with _validate_optimistic_concurrency:
   def _validate_optimistic_concurrency(
       self,
       workflow: Workflow,
       step_execution: StepExecution,
       expected_state_version: int,
       expected_position: ExpectedPosition,
   ) -> None:
       # Version check
       if expected_state_version != workflow.state_version:
           raise StateVersionMismatchError(
               expected=expected_state_version,
               actual=workflow.state_version,
           )

       # Full position check (includes step_name)
       if (expected_position.pass_type != workflow.current_pass.value or
           expected_position.step_name != workflow.current_step.value or
           expected_position.step_number != workflow.current_step_number or
           expected_position.status != step_execution.status.value):
           raise StateVersionMismatchError(
               message="Position mismatch",
               expected=expected_position.model_dump(),
               actual={...},
           )
   3. Remove the if expected_state_version is None: return bypass (line 319-
  320)

   Files:
   - apps/api/routes/workflows.py (schemas + endpoint calls)
   - apps/api/application/workflow_service.py (validation + signatures)

   ---
   Fix 4: Staleness Schema + Trigger Alignment (Issues 4, 5, 6, 7, 17)

   Problem:
   - Artifact model has is_stale, spec requires stale
   - Missing supersedes/superseded_by columns for lineage
   - Trigger uses nonexistent from_step_number (column is from_step)
   - Trigger only marks same-step older revisions, not DOWNSTREAM artifacts per
  §9.4

   Solution: Align schema with spec. Use exact trigger SQL from spec §7.

   Changes:

   1. Create NEW migration 003_remediation.py:
   -- ============================================================
   -- ARTIFACT LINEAGE COLUMNS (per spec §7)
   -- ============================================================
   ALTER TABLE artifacts
   ADD COLUMN supersedes UUID REFERENCES artifacts(id),
   ADD COLUMN superseded_by UUID REFERENCES artifacts(id);

   -- Rename is_stale to stale (spec uses 'stale')
   ALTER TABLE artifacts RENAME COLUMN is_stale TO stale;

   -- Unique index: exactly one "latest" per lineage (methodology_doc)
   CREATE UNIQUE INDEX idx_latest_methodology_doc ON artifacts(
       workflow_id, step_name, artifact_type, document_type, document_version
   ) WHERE artifact_type = 'methodology_doc' AND superseded_by IS NULL;

   -- Unique index: exactly one "latest" per lineage (step_package)
   CREATE UNIQUE INDEX idx_latest_step_package ON artifacts(
       workflow_id, step_name, artifact_type
   ) WHERE artifact_type = 'step_package' AND superseded_by IS NULL;

   -- Convenience view for latest artifacts
   CREATE VIEW latest_artifacts AS
   SELECT * FROM artifacts WHERE superseded_by IS NULL;

   -- ============================================================
   -- TRACEABILITY LINK STALENESS (per spec §7)
   -- ============================================================
   ALTER TABLE traceability_links
   ADD COLUMN stale BOOLEAN NOT NULL DEFAULT FALSE,
   ADD COLUMN stale_reason VARCHAR(500) NULL,
   ADD COLUMN stale_since TIMESTAMPTZ NULL,
   ADD COLUMN validated_at TIMESTAMPTZ NULL;

   -- ============================================================
   -- STALENESS PROPAGATION TRIGGER (exact spec §7 SQL)
   -- ============================================================
   -- When a new artifact revision is inserted (supersedes IS NOT NULL),
   -- mark DOWNSTREAM latest artifacts and traceability links as stale.

   CREATE OR REPLACE FUNCTION propagate_staleness()
   RETURNS TRIGGER AS $$
   BEGIN
       -- Only propagate if this is a new revision (not first creation)
       IF NEW.supersedes IS NOT NULL THEN
           -- Mark downstream latest artifacts as stale
           UPDATE artifacts
           SET stale = TRUE,
               stale_reason = 'upstream_step_' || NEW.step_number ||
  '_revised',
               stale_since = NOW()
           WHERE workflow_id = NEW.workflow_id
             AND step_number > NEW.step_number  -- DOWNSTREAM steps
             AND superseded_by IS NULL  -- Only mark latest versions
             AND stale = FALSE;

           -- Also mark affected traceability links
           UPDATE traceability_links
           SET stale = TRUE,
               stale_reason = 'source_step_' || NEW.step_number || '_revised',
               stale_since = NOW()
           WHERE workflow_id = NEW.workflow_id
             AND from_step = NEW.step_number  -- Links FROM this step
             AND stale = FALSE;
       END IF;
       RETURN NEW;
   END;
   $$ LANGUAGE plpgsql;

   -- Trigger fires on INSERT when supersedes IS NOT NULL (new revision)
   CREATE TRIGGER artifact_staleness_trigger
   AFTER INSERT ON artifacts
   FOR EACH ROW
   WHEN (NEW.supersedes IS NOT NULL)
   EXECUTE FUNCTION propagate_staleness();

   -- ============================================================
   -- ADVISORY LOCK FIX (collision-resistant)
   -- ============================================================
   CREATE OR REPLACE FUNCTION get_next_event_sequence(p_workflow_id UUID)
   RETURNS INTEGER AS $$
   DECLARE
       next_seq INTEGER;
       lock_id BIGINT;
   BEGIN
       lock_id := hashtext(p_workflow_id::text)::bigint;
       PERFORM pg_advisory_xact_lock(lock_id);
       SELECT COALESCE(MAX(sequence), 0) + 1 INTO next_seq
       FROM workflow_events WHERE workflow_id = p_workflow_id;
       RETURN next_seq;
   END;
   $$ LANGUAGE plpgsql;

   -- ============================================================
   -- STEP EXECUTION UPDATED_AT
   -- ============================================================
   ALTER TABLE step_executions
   ADD COLUMN updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW();

   CREATE OR REPLACE FUNCTION update_step_execution_timestamp()
   RETURNS TRIGGER AS $$
   BEGIN
       NEW.updated_at = NOW();
       RETURN NEW;
   END;
   $$ LANGUAGE plpgsql;

   CREATE TRIGGER step_execution_updated_at
   BEFORE UPDATE ON step_executions
   FOR EACH ROW EXECUTE FUNCTION update_step_execution_timestamp();
   2. Update Artifact model (artifact.py):
     - Rename is_stale → stale
     - Add supersedes, superseded_by columns
   3. Update TraceabilityLink model (traceability.py):
     - Add stale, stale_reason, stale_since, validated_at columns
   4. Update StalenessResponse schema (routes/workflows.py):
   class StalenessResponse(BaseModel):
       workflow_id: str
       position: PositionResponse
       can_complete: bool
       blocking_reasons: list[str]
       stale_artifacts: list[StaleArtifactEntry]
       stale_links: list[StaleLinkEntry]
   5. Add acknowledge-stale and re-execute endpoints per §10.2

   Files:
   - infra/db/migrations/versions/003_remediation.py (CREATE)
   - apps/api/infrastructure/db/models/artifact.py (schema alignment)
   - apps/api/infrastructure/db/models/traceability.py (add columns)
   - apps/api/routes/workflows.py (staleness endpoints)
   - apps/api/infrastructure/db/repositories/traceability.py (add
  list_stale_links)

   ---
   Fix 5: Progress Endpoint Completion (Issues 10, 11)

   Problem: latest_artifact_revision is TODO; updated_at doesn't exist on
  StepExecution.

   Solution: Query artifact for revision; updated_at already in migration (Fix
  4).

   Changes:

   1. Update StepExecution model (step_execution.py):
     - Add updated_at column (DB trigger handles auto-update)
   2. Fix progress endpoint (routes/workflows.py):
   # Get latest artifact revision (no more TODO)
   latest_revision = None
   if step_exec.latest_artifact_id:
       artifact = await artifact_repo.get(step_exec.latest_artifact_id)
       if artifact:
           latest_revision = artifact.revision

   StepProgressEntry(
       ...
       latest_artifact_revision=latest_revision,
       updated_at=step_exec.updated_at,
   )

   Files:
   - apps/api/infrastructure/db/models/step_execution.py (add column)
   - apps/api/routes/workflows.py (fix progress endpoint)

   ---
   Fix 6: Validation Error Handling (Issue 14)

   Problem: Spec requires ValidationErrorResponse (400) for missing fields;
  FastAPI returns 422.

   Solution: Add custom exception handler that applies to ALL request
  validation errors across ALL endpoints.

   Changes:

   1. Add global exception handler in main.py:
   from fastapi.exceptions import RequestValidationError
   from starlette.responses import JSONResponse

   class ValidationErrorResponse(BaseModel):
       """400 response for request validation failures."""
       error_code: str = "VALIDATION_ERROR"
       message: str
       errors: list[dict]

   @app.exception_handler(RequestValidationError)
   async def validation_exception_handler(request: Request, exc:
  RequestValidationError):
       """Convert FastAPI 422 to spec-compliant 400 ValidationErrorResponse.

       Applies to ALL endpoints, not just action endpoints.
       """
       return JSONResponse(
           status_code=400,
           content=ValidationErrorResponse(
               message="Request validation failed",
               errors=[{"loc": list(e["loc"]), "msg": e["msg"], "type":
  e["type"]} for e in exc.errors()],
           ).model_dump(),
       )

   Note: This handler applies globally to all endpoints (workflows, messages,
  progress, etc.), ensuring spec
   compliance throughout.

   Files:
   - apps/api/main.py (add global exception handler + schema)

   ---
   Fix 7: state_version Increment Rule (Issue 13)

   Problem: state_version rules vague; need concrete before/after comparison.
  Must include status/phase changes EVEN
   when position doesn't change.

   Solution: Use explicit before/after comparisons. Increment on ANY state-
  eligibility change.

   Changes:

   1. Update sync_db_from_state (workflow_service.py):
   # Capture state BEFORE changes
   old_pass = workflow.current_pass
   old_step = workflow.current_step
   old_step_number = workflow.current_step_number
   old_status = step_execution.status if step_execution else None
   old_phase = step_execution.phase if step_execution else None

   # ... apply state changes ...

   # Detect state-eligibility changes (Contract §11.3)
   # IMPORTANT: status/phase changes increment EVEN if position unchanged
   state_eligibility_changed = (
       # Position changes
       old_pass != workflow.current_pass or
       old_step != workflow.current_step or
       old_step_number != workflow.current_step_number or
       # Status changes (EVEN without position change)
       (step_execution and old_status != step_execution.status) or
       # Phase changes (EVEN without position change)
       (step_execution and old_phase != step_execution.phase) or
       # New artifacts created
       len(artifact_results) > 0
   )

   if state_eligibility_changed:
       await self._increment_state_version(workflow)
   2. Action methods always increment (they change status/phase):
     - approve_step(): status changes (awaiting_review → approved) → increment
     - revise_step(): status changes + new artifact → increment
     - clarify_step(): phase changes → increment
   3. Explicit increment scenarios:
   | Change                             | Position Changed? | Increment? |
   |------------------------------------|-------------------|------------|
   | Step advances                      | Yes               | Yes        |
   | Status: pending → running          | No                | Yes        |
   | Status: awaiting_review → approved | No                | Yes        |
   | Phase: received → drafting         | No                | Yes        |
   | New artifact created               | No                | Yes        |
   | Staleness updated                  | No                | Yes        |


   Files:
   - apps/api/application/workflow_service.py

   ---
   Implementation Order

   1. Fix 4 — Create 003_remediation.py migration with ALL DB changes:
     - Artifact lineage columns (supersedes, superseded_by)
     - Rename is_stale → stale
     - Unique indexes for "latest" per lineage
     - latest_artifacts view
     - Traceability link staleness columns + validated_at
     - Staleness propagation trigger (spec §7 SQL)
     - Advisory lock fix (hashtext)
     - step_executions.updated_at + trigger
   2. Fix 4 (models) — Update SQLAlchemy models:
     - artifact.py: Add supersedes, superseded_by, rename is_stale → stale
     - traceability.py: Add stale, stale_reason, stale_since, validated_at
     - step_execution.py: Add updated_at
   3. Fix 5 — Progress endpoint:
     - Fix latest_artifact_revision TODO
     - Use step_exec.updated_at
   4. Fix 7 — state_version increment rule:
     - Concrete before/after comparisons in sync_db_from_state
   5. Fix 6 — Validation error handling:
     - Add custom exception handler for 400 response
   6. Fix 3 — Required OCC fields:
     - ExpectedPosition schema (with step_name)
     - Replace _validate_state_version with _validate_optimistic_concurrency
     - Remove None bypass
   7. Fix 4 (endpoints) — Staleness endpoints:
     - Update StalenessResponse schema
     - Add list_stale_links repository method
     - Add acknowledge-stale and re-execute endpoints
   8. Fix 2 — SSE Replay:
     - Add subscribe_live() to EventBroker
     - Add get_max_sequence() to repository
     - Fix stream endpoint replay logic
   9. Fix 1 — Atomic event persistence:
     - Service methods return events list
     - Routes publish after transaction commits
     - Full event type coverage (workflow.started, step.started, etc.)
     - Fix to_sse_dict (include event_type, use id as event_id)

   ---
   Files to Modify

   | File                                                      | Changes
                  |
   |-----------------------------------------------------------|---------------
  --------------------------------------
   ---------------|
   | infra/db/migrations/versions/003_remediation.py           | CREATE — All
  DB changes
                  |
   | apps/api/infrastructure/db/models/artifact.py             | Add
  supersedes/superseded_by, rename is_stale→stale
                  |
   | apps/api/infrastructure/db/models/traceability.py         | Add stale,
  stale_reason, stale_since, validated_at
                  |
   | apps/api/infrastructure/db/models/step_execution.py       | Add updated_at
                  |
   | apps/api/infrastructure/db/models/workflow_event.py       | Fix
  to_sse_dict (event_type, event_id=id)
                  |
   | apps/api/infrastructure/db/repositories/traceability.py   | Add
  list_stale_links
                  |
   | apps/api/infrastructure/db/repositories/workflow_event.py | Add
  get_max_sequence
                  |
   | apps/api/application/event_stream.py                      | Add
  subscribe_live() method
                  |
   | apps/api/application/workflow_service.py                  | Return events,
  OCC validation, state_version logic
                  |
   | apps/api/routes/workflows.py                              |
  ExpectedPosition, error responses, replay fix,
   staleness endpoints |
   | apps/api/main.py                                          | Add validation
  exception handler
                  |

   ---
   Success Criteria

   Event Persistence + Atomicity

   - Route owns transaction boundary (async with session.begin())
   - Service methods run INSIDE caller's transaction, return events list
   - Native events (workflow.started, step.*, action events) persisted by
  service
   - Synthetic events (artifact.delta, artifact.final) persisted by route
  INSIDE same transaction
   - ALL events published AFTER transaction commits (route does publish loop)
   - SSE payloads include event_type, event_id (=workflow_events.id), sequence,
  external workflow_id

   SSE Replay

   - subscribe_live() method exists for live-only streaming (no backlog)
   - Replay: subscribe FIRST, get max_seq, replay DB, drop broker events <=
  max_seq

   Optimistic Concurrency

   - expected_state_version and expected_position (with step_name) REQUIRED
   - _validate_optimistic_concurrency validates full position (pass_type,
  step_name, step_number, status)
   - No None bypass in validation

   Staleness Schema (spec §7 alignment)

   - Artifact model has supersedes, superseded_by, stale (not is_stale)
   - Unique indexes enforce exactly one "latest" per artifact lineage
   - latest_artifacts view exists
   - Staleness trigger fires on INSERT when supersedes IS NOT NULL
   - Staleness propagates to DOWNSTREAM artifacts (step_number >
  NEW.step_number)
   - Traceability links have stale, stale_reason, stale_since, validated_at
   - StalenessResponse includes position, blocking_reasons, stale_links
   - acknowledge-stale and re-execute endpoints exist

   Other

   - Progress endpoint has real artifact revision, real updated_at
   - Advisory lock uses hashtext() — no collision risk
   - Validation errors return 400 globally (not FastAPI's 422) — applies to ALL
  endpoints
   - state_version increments on status/phase changes EVEN when position
  unchanged
   - New migration 003 created (002 unchanged)
   - All existing tests pass (including Gate E)
  </Senior Developer>


• Verdict: Agree with the plan, pending one critical clarification and two
  guardrails.

  What’s solid

  - Transaction‑owned by routes with events persisted inside the same
    transaction and published after commit is correct.
  - subscribe_live() + replay bounded by max_seq resolves race and backlog
    mixing.
  - Schema alignment (supersedes/superseded_by, stale, traceability staleness,
    trigger SQL from spec) is correct.
  - Strict OCC fields + global 400 validation handler aligns with the Contract.

  Blocking clarification

  - [BLOCK] The plan still doesn’t specify how synthetic artifact.delta is
    persisted inside the same transaction without running the graph outside the
    transaction. In your example, you run graph.ainvoke inside the transaction—
    this is risky because it holds the DB transaction open during LLM/graph
    execution. If you intend that, it must be explicit and acceptable.
    Otherwise, you need a different mechanism (e.g., persist synthetic deltas
    after commit but still sequence them with the same WorkflowEvent table and
    accept that they’re not atomic with the state change).

  Guardrails

  - [HIGH] Ensure the new stale column replaces is_stale everywhere (models,
    queries, progress/staleness endpoints) to avoid split‑brain staleness.
  - [HIGH] Update Gate E tests if they assume backlog replay without
    from_sequence; your new default should be from_sequence=0 to preserve
    behavior.

  Recommendation: PROCEED after clarifying transaction scope for graph execution
  and synthetic delta persistence.


› Here’s the report from the Senior Developer after completing implementation.
  Verify if the results matched your expectations.  Propose what to do next.

    <Senior Developer>
   All Gate C, D, and E tests pass. Here's a summary of what was accomplished
  in this session:

    Completed

    Schema & Models:
    - ✅ Created 003_remediation.py migration with all DB changes (lineage
  columns, staleness, triggers, advisory lock fix, updated_at)
    - ✅ Updated SQLAlchemy models (artifact.py, traceability.py,
  step_execution.py, workflow_event.py)

    Endpoints & Validation:
    - ✅ Fixed progress endpoint (latest_artifact_revision, updated_at)
    - ✅ Added global 400 validation error handler (converts FastAPI's 422)
    - ✅ Added required OCC fields (ExpectedPosition schema,
  _validate_optimistic_concurrency)
    - ✅ Updated staleness endpoints (StalenessResponse with position/
  blocking_reasons/stale_links, acknowledge-stale, re-execute)

    SSE & Events:
    - ✅ Added subscribe_live() to EventBroker (for future use)
    - ✅ Fixed to_sse_dict() to include event_id and event_type
    - ✅ Reverted SSE stream to use subscribe() temporarily (until Fix 1)

    Tests:
    - ✅ Updated Gate E tests to provide required OCC fields
    - ✅ All 9 Gate tests pass (4 Gate C + 2 Gate D + 3 Gate E)

    Remaining

    Fix 1 (Atomic Event Persistence) — the core remaining work:
    - Service methods need to return events list (not publish)
    - Routes need to persist synthetic events inside transaction
    - All events published AFTER commit
    - After Fix 1: Switch SSE stream from subscribe() to subscribe_live() for
  race-free replay


    </Senior Developer>


• Findings

  - [BLOCK] Atomic event persistence is still incomplete. The report explicitly
    says Fix 1 wasn’t done; that means event durability + sequencing for
    non‑action events and replay correctness remain non‑compliant with Contract
    §11.3/§11.4 and §12.1.
  - [BLOCK] SSE replay is still using subscribe() (backlog mixing), which
    re‑introduces the race/ordering issues. This contradicts the v3 plan’s
    replay strategy.
  - [RISK] “All Gate tests pass” is a good sign but not sufficient for the
    contract items above; Gate E tests don’t currently assert replay correctness
    or sequence integrity.

  Expectation check

  - This does not match the expected end state. We agreed Fix 1 was the critical
    remaining blocker, and it’s still open. Everything else looks aligned, but
    this one gap keeps the backend out of contract compliance.

  What to do next

  1. Implement Fix 1 as planned:
      - Persist all SSE events in the same transaction as state changes; return
        event rows.
      - Publish only after commit in routes.
      - Use subscribe_live() in /stream with DB replay bounded by max_seq.
  2. Add/extend SSE tests to cover replay ordering and sequence monotonicity
     (from_sequence=0 default + gap/reconnect).
  3. Re‑run Gate E and any new replay tests; confirm no regressions.

  If you want, I can review the Fix 1 patch once it’s ready.


› All tasks complete. Here's a summary of what was implemented:

    Fix 1: Atomic Event Persistence - COMPLETE

    Changes Made:

    1. Event Ordering Fix (apps/api/application/workflow_service.py):
      - Added _chunk_string() helper function
      - sync_db_from_state() now persists events in correct order:
          - step.started → artifact.delta (×3) → artifact.final →
  step.awaiting_review
      - Added artifact_output parameter to accept artifact data from routes
    2. Route Updates (apps/api/routes/workflows.py):
      - Added _extract_artifact_output() helper to handle both WorkflowState
  objects and dict representations
      - Updated all routes (create_workflow, approve_step, revise_step,
  submit_clarification, resume_workflow) to:
          - Extract artifact output from graph result
        - Pass artifact_output to sync_db_from_state()
    3. External Workflow ID in Events (apps/api/application/
  workflow_service.py):
      - Updated _build_event_payload() to include workflow_id (external string
  ID) in the payload
      - This ensures to_sse_dict() returns the correct external ID instead of
  internal UUID
    4. Test Updates (apps/api/tests/e2e/test_gate_e.py):
      - Fixed test_approve_advances_via_sse to collect until 2nd
  step.awaiting_review instead of relying on event count threshold
    5. New SSE Replay Tests (apps/api/tests/e2e/test_sse_replay.py):
      - test_replay_sequence_monotonicity - Verifies sequences are consecutive
  with no gaps
      - test_replay_event_ordering - Verifies event order is correct
      - test_from_sequence_parameter - Verifies filtering works correctly
      - test_reconnect_no_duplicates - Verifies no duplicate events on
  reconnect
      - test_workflow_id_consistency - Verifies all events have correct
  workflow_id

    Test Results:

    - Gate C: 4/4 passed ✅
    - Gate D: 2/2 passed ✅
    - Gate E: 3/3 passed ✅
    - SSE Replay: 5/5 passed ✅
    - Total E2E: 8/8 passed ✅


• Explored
  └ Search artifact_output|chunk_string|sync_db_from_state in
workflow_service.py

─ Worked for 37s ───────────────────────────────────────────────────────────────

• Context compacted

• I'm running the full orientation sequence—pwd, git status, recent commits, and
  contents of README.md and AGENTS.md—to get a clear view of the current repo
  state before verifying the Senior Developer’s results and planning next steps.
  This will help ensure I’m aligned with project context and instructions.

• Ran pwd; git status -sb; git log --oneline -10; cat README.md; cat AGENTS.md
  └ /Users/ryan/ai-env/projects/solver-engine
    ## devsession-12...origin/devsession-12
    … +546 lines
    (still valid)
    - `docs/DECISIONS.md` — approved deviations from spec (must be respected)

• I’m preparing to verify if the completed implementation aligns with the
  expected contract by inspecting key files related to event persistence, SSE
  streaming, state management, concurrency, and staleness logic. I'll focus on
  critical modules like workflow_service.py, workflows.py, event_stream.py,
  model and repository files, migrations, and tests to make sure atomicity,
  event sequencing, error handling, and other core specs are correctly met.
  Let's start by examining workflow_service.py closely.

• Explored
  └ Search sync_db_from_state|_chunk_string|_build_event_payload|
           expected_state_version|_validate_optimistic|state_version in
           workflow_service.py
    Read workflow_service.py
    Search approve_step|revise_step|submit_clarification|
_extract_artifact_output|
           publish in workflows.py
    Read workflows.py
    Search emit_event\( in workflows.py
    Search build_sse_payload in workflows.py
    Read workflows.py

• I'm examining the event streaming logic around getting the latest sequence
  number, replaying historical events, and subscribing to live events; there's a
  potential race where events published between fetching max sequence and
  subscribing could be missed. I'll verify repository methods to understand how
  sequences are handled and confirm if this risk should be flagged.

• Explored
  └ Read workflow_event.py
    Search create_workflow in workflow_service.py
    Read workflow_service.py
    Search create_event\( in api

• I'm carefully examining how workflow events use external workflow IDs in their
  SSE payloads and how the publish process relates to event broadcasting,
  especially during workflow resume. There’s a potential issue where resumed
  workflows persist events but don’t publish them to the broker, possibly
  causing connected SSE clients to miss real-time updates, though clients can
  resync by reconnecting. I’m weighing whether this is a spec violation or a
  practical risk and noting inconsistencies and related event ordering and
  payload concerns.

• Explored
  └ Read event_stream.py, 003_remediation.py
    Search is_stale in apps
    Read artifact.py, traceability.py, step_execution.py
    Search RequestValidationError|ValidationErrorResponse in main.py
    Read main.py
    Search staleness|stale in workflows.py
    Read workflows.py, artifact.py, traceability.py
    Search SemanticErrorResponse|INVALID_STATUS in workflows.py

• Ran rg -n "SemanticErrorResponse" -g'*'
  └ docs/Issued/Use-202601041305/4_SOLVER-technical-spec_V2_7_3.md:2503:|
    **422** | Unprocessable | Valid syntax but invalid semantics (e.g., approve
    … +28 lines
    docs/spec/4_solver-technical-spec-V2.7.3.md:2553:class
    SemanticErrorResponse(BaseModel):

• Explored
  └ Search StateConflictResponse|PositionResponse in workflows.py
    Read workflows.py
    Search STATUS_TO_EVENT|awaiting_clarification in workflows.py
    Read workflows.py, test_gate_e.py, test_sse_replay.py
    Search step\.revised|revision_requested|step\.clar|awaiting_clarification|
           step\.approved|workflow\.started in 4_solver-technical-spec-V2.7.3.md

• I'm analyzing mismatches between spec event types and code emissions,
  especially for revise and clarify steps, and verifying whether all necessary
  events like step.awaiting_clarification are emitted properly. I'm also
  checking how event sequences, SSE payloads, and artifact lineage with
  supersedes are handled to ensure staleness triggers and state synchronization
  behave correctly.

• Explored
  └ Search supersedes|superseded_by in api
    Search store_step_package|revision in application
    Read artifact_service.py

• I’m spotting that the artifact service doesn’t set supersedes or
  superseded_by, which breaks staleness triggers and unique index constraints on
  revision inserts. This mismatch could cause errors on new revisions. There are
  also discrepancies in SSE event types versus the spec, missing error response
  formats, and some event broadcasting inconsistencies that I want to verify
  more closely. Let’s explore these gaps carefully!

• Explored
  └ Read 4_solver-technical-spec-V2.7.3.md, workflows.py
    Search step\.revised|step\.clarified in workflow_service.py
    Read workflow_service.py, workflows.py

• I’m examining various critical issues like stale lineage not updating, SSE
  event mismatches, and especially a blocking bug in the artifact revision logic
  where the unique index on step_package causes insertion failures because the
  previous revision’s superseded_by isn’t updated before inserting a new
  revision. I plan to locate exact lines in artifact_service.py and migration
  scripts to reference this bug clearly.

• Explored
  └ Read artifact_service.py, 003_remediation.py, workflows.py, exceptions.py
    Search StateVersionMismatchError in api
    Read workflow_service.py
    Search artifact\.delta|artifact\.final in 4_solver-technical-spec-V2.7.3.md
    Search artifact_id in 4_solver-technical-spec-V2.7.3.md
    Read 4_solver-technical-spec-V2.7.3.md

• I’m reviewing major issues in the implementation against the spec,
  highlighting artifact lineage bugs that break revision insertion, SSE replay
  race risks causing event loss, SSE event type mismatches, unstructured error
  responses, and missing position object in SSE payloads. I’m gathering
  references and preparing concise findings with severity and file locations to
  guide next steps and a change request recommendation.

• Explored
  └ Read workflow_service.py

• I'll organize key findings from the code review into severity-ranked bullets
  tracing exact file locations, focusing on artifact lineage bugs, SSE replay
  races, event type mismatches, missing payload fields, and improper 422 error
  responses. Then I'll note an open question about live event publishing and lay
  out practical next steps for addressing these problems and verifying the fixes
  with tests. This keeps things clear and actionable!

─ Worked for 10m 27s ───────────────────────────────────────────────────────────

• Findings

  - [BLOCK] Artifact lineage isn’t wired into revisions: store_step_package
    inserts new revisions without setting supersedes/superseded_by, but the DB
    now enforces “latest” uniqueness on superseded_by IS NULL, so a second
    revision will violate the index and the staleness trigger will never fire
    (apps/api/application/artifact_service.py:176, infra/db/migrations/
    versions/003_remediation.py:61).
  - [RISK] SSE replay isn’t race‑free: max_seq is captured before
    subscribe_live, so events committed between those steps can be dropped
    (apps/api/routes/workflows.py:1360).
  - [SPEC-MISMATCH] SSE event taxonomy doesn’t match spec: revisions/
    clarifications emit step.revised/step.clarified and there’s no event when
    status enters awaiting_clarification (spec expects step.revision_requested
    and step.awaiting_clarification) (apps/api/application/
    workflow_service.py:684, apps/api/application/workflow_service.py:842, apps/
    api/application/workflow_service.py:1077).
  - [SPEC-MISMATCH] SSE payloads omit the required position object (only
    top‑level pass/step fields are present) (apps/api/application/
    workflow_service.py:434).
  - [SPEC-MISMATCH] 422 responses are still plain strings instead of
    SemanticErrorResponse (apps/api/routes/workflows.py:1080).

  Questions

  - Should resume_workflow publish persisted events after commit so connected
    SSE clients see the resumed run, or is replay‑only acceptable? (apps/api/
    routes/workflows.py:956)

  Next steps

  1. Wire artifact revision lineage: set supersedes on new revisions and update
     prior artifact superseded_by in the same transaction; add a revision test
     to confirm the unique indexes and staleness trigger behave.
  2. Fix replay ordering: subscribe live first, then capture max_seq, replay DB
     events, and drop live events <= max_seq; rerun apps/api/tests/e2e/
     test_sse_replay.py.
  3. Align SSE contract: emit spec event types (step.revision_requested,
     step.awaiting_clarification) and add position to payloads; adjust Gate E/
     SSE tests if needed.
  4. Implement SemanticErrorResponse for 422 and update action handlers/tests
     accordingly.


› Develop a comprehensive and detailed plan to address those Findings and
  Answer the questions. I will provide this to the Senior Developer.


• I’m organizing a comprehensive plan divided into clear slices addressing
  artifact lineage fixes, SSE replay race conditions, and event payload
  structure aligning with spec details. Alongside, I’m analyzing if
  resume_workflow events should be published post-commit for live sync with SSE
  clients, weighing durability and real-time client expectations to provide a
  nuanced recommendation referencing existing contract and workflow behavior.

• Explored
  └ Read workflow.py
    Search clarified in 4_solver-technical-spec-V2.7.3.md

• I'll organize a comprehensive plan with multiple focused slices covering
  artifact lineage updates, SSE event taxonomy and payload enhancements, SSE
  replay race condition fixes, semantic error response additions, and resume
  workflow event publishing. The plan will specify files to modify, key approach
  points, and a few clarifying questions to keep progress clear and ensure
  alignment with the spec.

─ Worked for 2m 18s ────────────────────────────────────────────────────────────

• Here’s a plan you can hand to the Senior Developer. It answers the
  resume_workflow question up front and then lays out slices with files +
  approach.

  Answer to resume_workflow question

  - Publish persisted events after commit when resume_workflow runs
    sync_db_from_state and returns events; this keeps live SSE clients in sync.
    In the “at interrupt” path it should be a no‑op because no events are
    produced unless state actually changed, so it won’t create duplicates.

  Plan (by slice)

  Slice 1 — Artifact lineage + staleness trigger integrity

  - Files: apps/api/application/artifact_service.py, apps/api/infrastructure/db/
    repositories/artifact.py (if needed), tests in apps/api/tests/
  - Approach:
      1. In store_step_package, when creating a new revision, set supersedes on
         the new artifact and update the previous artifact’s superseded_by
         before insert (so unique “latest” indexes don’t fail).
      2. Use a transaction‑safe update (either direct session update or repo
         method) so lineage is consistent and the staleness trigger fires
         (supersedes IS NOT NULL).
      3. Add a test that creates a second revision and asserts: no unique‑index
         violation, supersedes/superseded_by set, and downstream artifacts/links
         marked stale.

  Slice 2 — SSE event taxonomy + payload position

  - Files: apps/api/application/workflow_service.py, apps/api/infrastructure/db/
    models/workflow_event.py (if needed), apps/api/tests/e2e/test_gate_e.py,
    apps/api/tests/e2e/test_sse_replay.py
  - Approach:
      1. Align event types with spec: revise_step should emit
         step.revision_requested; add step.awaiting_clarification event when
         status transitions to that state.
      2. Add position object to the SSE payload (instance_number, pass_type,
         step_name, step_number, status, phase). Keep existing top‑level fields
         for compatibility.
      3. Decide how to handle submit_clarification: either emit no extra event
         (spec doesn’t list step.clarified) or emit a spec‑approved event when
         the system re‑enters execution; update tests to match.

  Slice 3 — SSE replay race fix

  - Files: apps/api/routes/workflows.py, apps/api/application/event_stream.py
    (if needed), apps/api/tests/e2e/test_sse_replay.py
  - Approach:
      1. In stream_workflow, subscribe live before capturing max_seq.
      2. Replay DB events for from_sequence < seq <= max_seq, then consume live
         events dropping any with sequence <= max_seq.
      3. Update/extend replay tests to cover the adjusted flow (existing tests
         may already pass, but add a regression check if feasible).

  Slice 4 — 422 SemanticErrorResponse

  - Files: apps/api/routes/workflows.py, tests in apps/api/tests/
  - Approach:
      1. Add SemanticErrorResponse schema per spec.
      2. Replace the current 422 string responses in action endpoints with
         structured SemanticErrorResponse including current_position.
      3. Add a small test to assert 422 response shape and fields.

  Slice 5 — Resume workflow publishes events

  - Files: apps/api/routes/workflows.py
  - Approach:
      1. Capture the events list returned by sync_db_from_state in both resume
         branches.
      2. Call publish_persisted_events() after the transaction commits if events
         exist.
      3. (Optional) add a lightweight test or note in Gate E replay coverage
         that resumed workflows emit events live.

  Validation / gates

  - make test-gates
  - make e2e
  - pytest apps/api/tests/e2e/test_sse_replay.py
  - Add a focused test for artifact revision lineage (unit or integration).

  Open questions (confirm before changes)

  1. For submit_clarification, do we emit an SSE event at all? If yes, which
     spec‑listed event should represent it?
  2. Is including instance_number in SSE position acceptable via an extra DB
     lookup on Instance, or should we embed it in the workflow model for faster
     access?
  3. Should resume publish events in both “interrupt” and “continue” branches,
     or only when graph execution actually ran?

  If you want, I can turn this into a slice‑by‑slice checklist aligned to the
  directive format.


› considering our development work today review and revise the AGENTS.md if
  appropriate.  Do not make the document confounding by trying to include too
  much.  Follow best practices for writing a AGENTS.md for a project of this
  nature.


• Explored
  └ Read 0_SOLVER-Development-Directive.md
    List legacy
    List spec
    Search AGENTS|agent|Execution Protocol|slice|slices in 5_SOLVER-Development-
           Directive-v1.5.md
    Read 0_SOLVER-Development-Directive.md, AGENTS.md
    Search AGENTS in 0_Document-Type-Specifications-v2.1.md

• I'm putting together a concise plan to review and revise AGENTS.md with
  minimal, focused edits: adding backend contract guardrails about SSE and
  concurrency controls, noting migration immutability, and trimming or
  optionally removing the Methodology Caching Note to keep things clear. I want
  to present this plan for approval before making changes.

─ Worked for 1m 30s ────────────────────────────────────────────────────────────

• I reviewed AGENTS.md and suggest a small, focused refresh that reflects
  today’s backend contract work without bloating the doc.

  Proposed plan (AGENTS.md only)

  - Add a short “Backend Contract Guardrails” section with 3 bullets: event log
    persistence/sequence, /stream replay with from_sequence, and required OCC
    fields + canonical refetch (workflow/progress/staleness).
  - Add a single non‑negotiable about migration immutability (never edit applied
    migrations; add a new one).
  - Trim the Methodology Caching Note to one line or remove it to keep the file
    lean.

  If that looks good, confirm and I’ll apply the edits.


› Update the README.md if appropriate


• Explored
  └ Search docs/spec/|Development-Directive|meta-prompt|structured-
reasoning|solver-
           technical-spec|Doc 3|Doc 2|Doc 1 in README.md
    Read README.md

• I'll suggest a minimal README update: adding new API endpoints (/progress, /
  staleness, etc.), including the SOLVER README in docs, referencing Document-
  Type-Specifications and Change Management for governance, and refining the
  Authority Stack section to align with current docs and AGENTS.md—all while
  keeping it concise and asking for approval before proceeding.

─ Worked for 1m 13s ────────────────────────────────────────────────────────────

• Plan (README alignment, single slice)

  Files to modify:

  - README.md

  Approach:

  - Update the Project Structure list to include docs/spec/1_SOLVER-README.md.
  - Expand the REST endpoint list to include /progress, /staleness, and
    staleness actions.
  - Replace the outdated Authority Stack table with a short pointer to docs/
    spec/0_Document-Type-Specifications-v2.1.md and AGENTS.md.
  - Add a minimal Documentation entry for the SOLVER README (and, if needed, a
    short governance/change-control pointer).

  No blockers. OK to proceed?


› proceed


• Explored
  └ Read README.md, -A

• Ran sed -n '156,166l' README.md
  └ ```$
    solver-engine/$
    … +13 lines
    ss — Change control$
    ├── apps/$

• Ran python - <<'PY'
  │ with open('README.md','r',encoding='utf-8') as f:
  │     for i,line in enumerate(f,1):
  │ … +3 lines
  └ zsh:1: command not found: python

• Ran python3 - <<'PY'
  │ with open('README.md','r',encoding='utf-8') as f:
  │     for i,line in enumerate(f,1):
  │ … +3 lines
  └ 158 '├── docs/\n'
    159 '│   └── spec/\n'
    … +10 lines
    165 '│       └── 6_SOLVER-Change-Management-v1.2.md      # Process — Change
    control\n'

• I'm considering that apply_patch might require exact ASCII matches without
  non-ASCII characters, which could be why it's failing despite matching lines.
  To fix this, I'll try simplifying patches to include only minimal ASCII
  context lines and run single hunks separately to isolate the problem.

• Ran python3 - <<'PY'
  │ from pathlib import Path
  │ import re
  │ … +52 lines
  └ (no output)

• Explored
  └ Read README.md

• Ran python3 - <<'PY'
  │ with open('README.md','r',encoding='utf-8') as f:
  │     for i,line in enumerate(f,1):
  │ … +4 lines
  └ 160 │       ├── 0_Document-Type-Specifications-v2.1.md  # Governance
    framework
    … +5 lines
    rationale
    hash_index 52

• Ran python3 - <<'PY'
  │ from pathlib import Path
  │ path = Path('README.md')
  │ … +9 lines
  └ (no output)

• Ran python3 - <<'PY'
  │ with open('README.md','r',encoding='utf-8') as f:
  │     for i,line in enumerate(f,1):
  │ … +4 lines
  └ 160 │       ├── 0_Document-Type-Specifications-v2.1.md  # Governance
    framework
    … +5 lines
    rationale
    hash_index 52

• I updated README.md to reflect the latest docs and API changes—adding the
  SOLVER README, expanding REST endpoints with progress and staleness actions,
  and simplifying the authority guidance to reference governance docs and
  AGENTS.md. No tests were run since this was a docs-only change. I’ll suggest
  next steps including optionally applying the earlier planned AGENTS.md updates
  the user asked about before.

─ Worked for 3m 42s ────────────────────────────────────────────────────────────

• Updated README.md to reflect the current docs layout and backend contract
  endpoints.

  - Added docs/spec/1_SOLVER-README.md to the project structure and
    documentation list in README.md.
  - Expanded the REST endpoints table with progress/staleness and staleness
    action routes in README.md.
  - Replaced the old authority table with a short pointer to the governance docs
    in README.md.

  No tests run (docs-only change).

  If you want, I can also apply the AGENTS.md tweaks we discussed:

  1. Add a short backend guardrails section + migration immutability note.


› also update AGENTS.md as planned


• Ran python3 - <<'PY'
  │ from pathlib import Path
  │ import re
  │ … +55 lines
  └ (no output)

• Explored
  └ Read AGENTS.md

• Ran python3 - <<'PY'
  │ from pathlib import Path
  │ path = Path('AGENTS.md')
  │ … +4 lines
  └ (no output)

• Explored
  └ Read AGENTS.md

─ Worked for 25s ───────────────────────────────────────────────────────────────

• Updated AGENTS.md with the planned guardrails and trimmed note.

  Changes made:

  - Added Backend Contract Guardrails with SSE persistence/replay and OCC
    requirements, plus migration immutability.
  - Reduced Methodology Caching Note to a single line.

  Files updated:

  - AGENTS.md

  No tests run (docs-only change).

  If you want me to adjust wording or move the guardrails elsewhere, say the
  word.


› good and did README need updating?


• Yes — the README was out of sync after today’s backend work. I updated it to:

  - list docs/spec/1_SOLVER-README.md in the project structure and documentation
    section
  - include the new progress/staleness endpoints and staleness actions
  - replace the old authority table with a pointer to docs/spec/0_Document-Type-
    Specifications-v2.1.md and AGENTS.md

  Edits are in README.md.


› In your role you are responsible for the init prompts so scrutinize this
  proposal and implement what you think is best to achieve the objectives and
  maintain a principled and coherent init prompt.
  Create the init prompt for the next instance of the Senior Developer.  Update
  it to reflect the current needs of development.  Don’t repeat specifics from
  governance docs.  Do describe and point to important content.
  Also leave room for the next instance of the agent to discover (by reading
  governance docs) on their own terms what matters.  Conciseness resulting in
  high leverage information is best, if it still retains all critical
  information. Identifying negative patterns to avoid can have benefit when
  done prudently.
  Here’s the current version of the “Senior Developer init prompt”

    <Senior Developer init prompt>
   # SOLVER Engine — Senior Developer Onboarding

       ## What This Is

       SOLVER is a **deterministic supervisor for stochastic agents** — a
  structured reasoning workflow engine
       that prioritizes rigor, traceability, and human oversight over speed and
  autonomy.

       **Key invariant:** LLMs cannot advance workflow state. Only external API
  calls from authenticated actors
       can approve gates.

       ---

       ## Current State

       **Backend (P1–P6):** ✅ Complete — All MVP gates pass

       | Gate | Criterion | Status |
       |------|-----------|--------|
       | A | 36 methodology docs exist | ✅ |
       | B | Packages validate + trace links | ✅ |
       | C | Cannot advance without approve | ✅ |
       | D | State survives restart | ✅ |
       | E | API + SSE works | ✅ |
       | F | Audit trail complete | ✅ |

       **Frontend:** Scaffold only — integration phase begins now

       ---

       ## Before You Code

       ### 1. Read the Authority Stack

       Documents in `docs/spec/` govern all work. Read in this order:

       | # | Document | What You'll Learn |
       |---|----------|-------------------|
       | 0 | Document-Type-Specifications | Governance framework, binding
  precedence |
       | 2 | Design-Intent | Why² — principles, tradeoffs, trust boundary |
       | 3 | Architectural-Contract | Invariants R1–R19, backend/frontend
  contracts |
       | 4 | Technical-Spec | Schemas, endpoints, error codes, event types |
       | 5 | Development-Directive | Phases, packages, gates, deliverables |
       | 6 | Change-Management | Deviation process, CR workflow |

       **Authority rule:** Contract wins conflicts. Intent is tie-breaker among
  compliant options. Deviations
       require logging in `docs/DECISIONS.md`.

       ### 2. Verify the Backend

       ```bash
       docker compose -f infra/docker/docker-compose.yml up -d
       make migrate
       make test-gates    # Gate C
       make test-recovery # Gate D
       make e2e           # Gate E + SSE replay

       3. Review Orientation Files

       - README.md — Project overview, architecture, quick start
       - CLAUDE.md — AI development guide, key context, pitfalls
       - AGENTS.md — Repo-level agent instructions
       - docs/DECISIONS.md — Approved deviations (5 recorded)

       ---
       Your Focus: Frontend Integration

       Target Gates

       | Gate | Name               | What It Verifies
  |

  |------|--------------------|------------------------------------------------
  ---|
       | γ1   | Workflow Lifecycle | Create → execute → gate → approve →
  advance       |
       | γ2   | SSE Client Sync    | Gaps, replay, reconnect handled correctly
  |
       | γ3   | Action Gating      | canAct/canMessage rules enforced (R3–R6,
  R14–R15) |
       | γ4   | Staleness Flow     | Upstream revision → downstream stale → re-
  execute |

       Frontend Reliability Rules (Contract §13–14)

       The Architectural Contract defines 19 reliability rules (R1–R19) for the
  frontend. Key themes:

       - Sequence Guard: Deduplicate by lastContiguousSequence, buffer out-of-
  order, detect gaps
       - Connection States: connecting → connected → reconnecting → resyncing →
  failed
       - Action Gating: canAct requires connected + awaiting_review + staleness
  known + can_complete
       - Optimistic Concurrency: Actions must include expected_state_version +
  expected_position

       Read Contract §13–14 for the complete rules. Do not guess — implement
  exactly as specified.

       Backend API Reference

       All endpoints under /api/v1. Key endpoints for frontend:

       | Method | Endpoint                               | Purpose
  |

  |--------|----------------------------------------|--------------------------
  ----------|
       | POST   | /workflows                             | Create workflow
  |
       | GET    | /workflows/{id}                        | Current state +
  position           |
       | GET    | /workflows/{id}/stream?from_sequence=N | SSE events (replay
  from N)         |
       | POST   | /workflows/{id}/actions/approve        | Approve gate
  (requires OCC fields) |
       | POST   | /workflows/{id}/actions/revise         | Request revision
  |
       | POST   | /workflows/{id}/actions/message        | Comment (no state
  change)          |

       See Technical Spec §9 for full endpoint documentation.

       ---
       Patterns to Avoid

       | Don't                             | Why                            |
  Do Instead
        |
       |-----------------------------------|--------------------------------|--
  -----------------------------------
       -|
       | Create apps/api/src/ nesting      | Project uses flat layout       |
  Keep apps/api/ structure
        |
       | Use PostgresSaver directly        | Schema incompatibility         |
  Use SolverCheckpointSaver
        |
       | Wire message action to graph      | Gate C: no state change        |
  DB-only persistence
        |
       | Publish events inside transaction | Race conditions                |
  Persist in txn, publish after commit
        |
       | Assume state types after resume   | LangGraph deserializes to dict |
  Use coerce_state() in nodes
        |
       | Skip reading Contract             | Invariants are non-negotiable  |
  Read R1–R19 before implementing
        |
       | Invent endpoint shapes            | Spec is authoritative          |
  Follow Tech Spec or log deviation
        |

       ---
       Work Protocol

       For Each Deliverable

       1. Plan: Identify tasks, dependencies, acceptance criteria, applicable
  Contract rules
       2. Implement: Small, testable increments
       3. Verify: Check against acceptance criteria and Contract rules
       4. Report: Files changed, commands run, gates satisfied

       Deviation Handling

       If backend doesn't match Contract/Spec:

       1. Check docs/DECISIONS.md for existing approved deviation
       2. If new deviation needed: Log it with impact analysis
       3. Options: DEFER (workaround) | IMPLEMENT (fix) | CHANGE REQUEST (spec
  wrong)

       ---
       Quick Reference

       # Database
       docker compose -f infra/docker/docker-compose.yml up -d
       make migrate

       # Development
       make dev-api      # Start API server (localhost:8000)
       make test         # Run tests
       make lint         # Lint code

       # Gate Verification
       make test-gates   # Gate C
       make test-recovery # Gate D
       make e2e          # Gate E + SSE replay

       Key Files:
       - Backend routes: apps/api/routes/workflows.py
       - Service layer: apps/api/application/workflow_service.py
       - Graph definition: apps/api/orchestration/graph.py
       - Event streaming: apps/api/application/event_stream.py
       - Migrations: infra/db/migrations/versions/

       Database: 3 migrations (001_initial_schema, 002_contract_alignment,
  003_remediation)

       ---
       First Steps

       1. Complete orientation reading (authority stack + orientation files)
       2. Run gate verification commands to confirm backend health
       3. Read Contract §13–14 (frontend reliability rules R1–R19)
       4. Propose plan for Package 6.1 (Project Setup)
       5. Wait for approval before implementing

       ---

       ## Changes from Original

       | Original | Revised | Rationale |
       |----------|---------|-----------|
       | ~800 lines | ~150 lines | Conciseness for high leverage |
       | Detailed migration steps | Removed | Migration complete |
       | Full R1–R19 listing | Reference to Contract | Discovery over
  repetition |
       | Detailed backend prerequisites audit | Removed | Audit complete, gaps
  fixed |
       | Document mapping tables | Simplified | Docs already in place |
       | Step-by-step bash commands for migration | Removed | Not needed |
       | Extensive work hierarchy explanation | Condensed | Point to Directive
  instead |

       ## Key Principles Applied

       1. **Point, don't repeat:** Reference governance docs instead of
  duplicating content
       2. **Current state focus:** Reflect that backend is complete, frontend
  is next
       3. **Negative patterns:** Concise pitfalls table from hard-won
  experience
       4. **Discovery-oriented:** Let developer learn invariants from reading
  Contract
       5. **High leverage:** Only include what's essential for getting started

    </Senior Developer init prompt>
  Revise accordingly.


• I’m pulling together a concise yet comprehensive init prompt reflecting the
  current backend status, known review findings, and next steps toward frontend
  integration. I’m focusing on clear mission, key files, verification commands,
  and guiding the Senior Dev to digest essential docs without repeating
  governance specifics, all framed to optimize clarity and actionability.

─ Worked for 33s ───────────────────────────────────────────────────────────────

• Here’s the revised init prompt for the next Senior Developer, tuned to current
  needs and kept concise:

  # SOLVER Engine — Senior Developer Init

  ## Mission (This Instance)
  1) Confirm backend contract alignment is truly clean.
  2) If any gaps remain, close them and re‑verify gates.
  3) Only then move to frontend integration (Phase 6).

  ## First Steps (Orientation)
  - Run: `pwd`, `git status -sb`
  - Read: `README.md`, `AGENTS.md`, `CLAUDE.md`, `docs/DECISIONS.md`
  - Follow the spec reading order listed in `AGENTS.md` (Contract + Technical
  Spec are most relevant).

  ## Current State Snapshot
  - Backend P6 gates pass; SSE replay tests exist; progress/staleness endpoints
  are present.
  - Known review items to confirm/close (verify in code/tests):
    - Artifact revision lineage must set `supersedes/superseded_by` and avoid
  unique‑index conflicts.
    - SSE replay should be race‑free (subscribe live before capturing max
  sequence).
    - SSE event taxonomy/payload must match spec (e.g.,
  `step.revision_requested`, `step.awaiting_clarification`, include `position`).
    - 422 responses should be structured (`SemanticErrorResponse`), not plain
  strings.

  ## Guardrails (Do Not Violate)
  - Don’t edit applied migrations; add a new migration.
  - Persist SSE events before publish; publish after commit.
  - Keep `apps/api` flat (no `src/`).

  ## Key Paths
  - `apps/api/routes/workflows.py`
  - `apps/api/application/workflow_service.py`
  - `apps/api/application/event_stream.py`
  - `infra/db/migrations/versions/`
  - `apps/api/tests/e2e/`

  ## Verification
  - `make test-gates`
  - `make test-recovery`
  - `make e2e`
  - `pytest apps/api/tests/e2e/test_sse_replay.py`

  ## Working Protocol
  - Use the slice workflow from `AGENTS.md`.
  - Propose a plan, wait for approval, implement only the approved slice.
  - Log deviations in `docs/DECISIONS.md`.

  ## Start
  After orientation, propose the next slice plan (either cleanup of the review
  items above or frontend Phase 6 setup) and wait for approval.

  If you want this saved to a specific file (e.g., docs/ONBOARDING.md or a new
  SENIOR_DEV_INIT.md), tell me the path.


› Create the init prompt for the next instance of the Co-Developer in the same
  manner.
  Here’s the current version
   <Co-Developer init prompt>
      # SOLVER Engine — Co-Developer Onboarding

       ## Your Role

       You are the **reviewer and guardian**, not the implementer. You support
  the Senior Developer by providing
       analysis, risk assessment, and spec verification.

       **You do NOT:**
       - Write implementation code (unless explicitly asked)
       - Make architectural decisions unilaterally
       - Approve deviations from spec

       **You DO:**
       - Analyze plans before implementation
       - Review code against Contract requirements (R1–R19)
       - Flag spec mismatches and risks
       - Guard architectural boundaries
       - Verify gate criteria are met

       ---

       ## Current State

       **Backend (P1–P6):** ✅ Complete — All MVP gates pass

       | Gate | Criterion | Status |
       |------|-----------|--------|
       | A–F | Methodology, packages, gating, restart, API+SSE, audit | ✅ All
  pass |

       **Frontend:** Integration phase begins now — this is your review focus

       **Deviations:** 6 approved in `docs/DECISIONS.md` — respect these
  constraints

       ---

       ## Authority Model

       Documents in `docs/spec/` govern all work:

       | Hierarchy | Order | Use When |
       |-----------|-------|----------|
       | **Binding Precedence** | Contract > Spec > Directive > Intent |
  Resolving conflicts |
       | **Purpose Priority** | Intent > Contract > Spec > Directive | Choosing
  among compliant options |

       **Rule:** Contract wins conflicts. If you see a plan that violates
  Contract, flag it.

       Read `docs/spec/0_Document-Type-Specifications-v2.1.md` for full
  governance framework.

       ---

       ## Guardian Responsibilities

       ### 1. Architecture Guard

       Flag if you see these patterns introduced:

       | Banned | Required |
       |--------|----------|
       | `apps/api/src/` nesting | Flat `apps/api/` structure |
       | DB access in route handlers | Clean layer separation |
       | Direct LLM calls in tests | Deterministic fixtures |
       | Network calls in unit tests | Mocked dependencies |

       ### 2. Spec Enforcer

       Verify implementation matches Contract/Spec:

       | Check | Contract Reference |
       |-------|-------------------|
       | SSE has `sequence`, gaps handled | §9.2, §12 |
       | `expected_state_version` validated | §11.1 |
       | `canAct` conditions enforced | §13–14, R1–R19 |
       | 409 includes `current_state_version` | §10.5 |
       | Valid state transitions only | §9.4 |

       ### 3. Gate Verifier

       Confirm criteria before marking complete:

       | Gate | What to Verify |
       |------|----------------|
       | γ1 | Workflow Lifecycle: create → execute → gate → approve → advance |
       | γ2 | SSE Client Sync: gaps, replay, reconnect handled |
       | γ3 | Action Gating: canAct/canMessage rules correct (R3–R6, R14–R15) |
       | γ4 | Staleness Flow: upstream revision → downstream stale → re-execute
  |

       Do not confirm a gate passes unless you've verified the criteria.

       ---

       ## Review Checklist

       When reviewing Senior Developer plans or code:

       - [ ] Does it follow the authority stack? (Contract wins)
       - [ ] Are banned patterns avoided?
       - [ ] Does it match Technical Spec endpoint shapes?
       - [ ] Are R1–R19 reliability rules enforced?
       - [ ] Is there a deviation? Check if already in `docs/DECISIONS.md`
       - [ ] Will the change help pass the target gate?

       ---

       ## Relationship to Senior Developer

       | Aspect | Senior Developer | Co-Developer |
       |--------|------------------|--------------|
       | Implementation | Writes code | Reviews code |
       | Planning | Proposes plans | Analyzes, identifies risks |
       | Decisions | Makes implementation choices | Flags concerns (does not
  block) |
       | Deviations | Logs deviations | Identifies spec mismatches |
       | Gate verification | Runs verification | Confirms criteria met |

       **Escalation:** If you identify a blocking issue (spec violation,
  architectural breach), flag it clearly.
       Do not implement workarounds.

       ---

       ## Orientation

       ### Key Files to Review

       | File | Purpose |
       |------|---------|
       | `docs/spec/3_SOLVER-Architectural-Contract-v3.4.md` | Invariants R1–
  R19, §9–14 |
       | `docs/spec/4_solver-technical-spec-V2.7.3.md` | Schemas, endpoints,
  error codes |
       | `docs/spec/5_SOLVER-Development-Directive-v1.5.md` | Phase 7 packages,
  gate criteria |
       | `docs/DECISIONS.md` | 6 approved deviations |

       ### Quick Verification Commands

       ```bash
       make test-gates    # Gate C verification
       make test-recovery # Gate D verification
       make e2e           # Gate E + SSE replay

       ---
       First Steps

       1. Read Contract §13–14 (frontend reliability rules R1–R19)
       2. Review docs/DECISIONS.md to understand existing constraints
       3. Await Senior Developer's first plan for review
       4. Provide analysis: risks, spec alignment, gate impact

       ---

       ## Changes from Original

       | Original | Revised | Rationale |
       |----------|---------|-----------|
       | Migration note (`docs/Issued/...`) | Removed | Migration complete |
       | Full work hierarchy explanation | Removed | Point to Directive |
       | Detailed orientation sequence | Condensed | Key files only |
       | Extended bash commands | Condensed | Verification only |
       | 5 deviations | Updated to 6 | Current count |
       | Generic guardian checks | Focused on frontend gates | Current phase |

       ## Key Principles Applied

       1. **Role clarity:** Reviewer/guardian, not implementer
       2. **Conciseness:** ~100 lines vs original ~200
       3. **Current state focus:** Frontend integration is the review target
       4. **Actionable checklists:** Review checklist for practical use
       5. **Point, don't repeat:** Reference docs instead of duplicating


    </Co-Developer init prompt>


• Here’s the revised init prompt for the next Co‑Developer instance, trimmed and
  aligned to current needs:

  # SOLVER Engine — Co‑Developer Init

  ## Role
  You are the reviewer/guardian. You do NOT implement. You analyze plans, review
  code against spec, and verify gates.

  ## First Steps
  - Run: `pwd`, `git status -sb`
  - Read: `README.md`, `AGENTS.md`, `CLAUDE.md`, `docs/DECISIONS.md`
  - Follow the spec reading order in `AGENTS.md` (Contract + Technical Spec are
  key).

  ## Current Focus
  Backend is near‑complete but must be re‑verified; frontend work is next only
  after backend is clean.
  Review priorities:
  - SSE event log correctness (sequence, replay, gap handling)
  - Optimistic concurrency (required fields + 409 response)
  - Staleness propagation + lineage correctness
  - Error response structure (422/409)

  ## Guardrails
  - Don’t approve deviations; flag them and point to `docs/DECISIONS.md`.
  - Don’t accept architectural drift (`apps/api/src`, DB in routes, network
  calls in unit tests).
  - Don’t accept edits to applied migrations.

  ## What to Verify (Gate‑oriented)
  - γ1: lifecycle end‑to‑end flow (create → execute → gate → approve → advance)
  - γ2: SSE sync (replay, reconnect, gap handling)
  - γ3: action gating (canAct/canMessage rules, R1–R19)
  - γ4: staleness flow (upstream revision → stale → re‑execute path)

  ## Key Files to Inspect
  - `apps/api/routes/workflows.py`
  - `apps/api/application/workflow_service.py`
  - `apps/api/application/event_stream.py`
  - `infra/db/migrations/versions/`
  - `apps/api/tests/e2e/`

  ## Verification Commands (when asked)
  - `make test-gates`
  - `make test-recovery`
  - `make e2e`
  - `pytest apps/api/tests/e2e/test_sse_replay.py`

  ## Review Output Format
  - Findings first (severity + file/line)
  - Then questions/assumptions
  - Then recommendation (proceed/revise)

  Wait for the Senior Developer’s plan before reviewing.

  If you want this written to a file (e.g., docs/CO-DEV-ONBOARDING.md or a new
  CO_DEV_INIT.md), tell me the target path.
Token usage: total=927,329 input=797,508 (+ 17,763,712 cached) output=129,821 (reasoning 105,984)
To continue this session, run codex resume 019b8aef-b4e5-7990-a957-16ef89089846
(.venv) ryan@Mac ai-env % 








